diff --git a/cloudflare_worker/public/404.html b/cloudflare_worker/public/404.html
index f8a8baa5..22da0bb9 100644
--- a/cloudflare_worker/public/404.html
+++ b/cloudflare_worker/public/404.html
@@ -212,6 +212,7 @@
         text-shadow: 0 0 70px rgba(150, 114, 238, 0.12);
       }
     </style>
+    <link rel="stylesheet" href="/mesh-page-theme.css" />
     <link rel="stylesheet" href="/site-header.css" />
     <script src="/site-header.js" defer></script>
   </head>
diff --git a/cloudflare_worker/public/about.html b/cloudflare_worker/public/about.html
index 630ae18b..c659a04e 100644
--- a/cloudflare_worker/public/about.html
+++ b/cloudflare_worker/public/about.html
@@ -516,6 +516,7 @@
         }
       }
     </style>
+    <link rel="stylesheet" href="/mesh-page-theme.css" />
       <link rel="stylesheet" href="/site-footer.css" />
     <script src="/site-footer.js" defer></script>
     <link rel="stylesheet" href="/site-header.css" />
diff --git a/cloudflare_worker/public/auth-page-theme.css b/cloudflare_worker/public/auth-page-theme.css
new file mode 100644
index 00000000..034ccb99
--- /dev/null
+++ b/cloudflare_worker/public/auth-page-theme.css
@@ -0,0 +1,270 @@
+:root {
+  color-scheme: dark;
+  --auth-page-bg: #050505;
+  --auth-glow: radial-gradient(
+    circle at top,
+    rgba(255, 255, 255, 0.16),
+    transparent 32rem
+  );
+  --auth-card-bg: rgba(8, 8, 8, 0.88);
+  --auth-border: rgba(255, 255, 255, 0.1);
+  --auth-text: #f5f5f5;
+  --auth-text-secondary: rgba(255, 255, 255, 0.62);
+  --auth-text-quiet: rgba(255, 255, 255, 0.52);
+  --auth-field-bg: rgba(255, 255, 255, 0.055);
+  --auth-focus: rgba(255, 255, 255, 0.34);
+  --auth-focus-ring: rgba(255, 255, 255, 0.12);
+  --auth-action-bg: #f5f5f5;
+  --auth-action-text: #050505;
+  --auth-action-hover: #d9d9d9;
+  --auth-action-disabled: #e6e6e6;
+  --auth-link: #ffffff;
+  --auth-link-hover: #d9d9d9;
+  --auth-placeholder: rgba(255, 255, 255, 0.42);
+  --auth-surface: rgba(255, 255, 255, 0.035);
+  --auth-surface-strong: rgba(255, 255, 255, 0.06);
+  --auth-good: #4ade80;
+  --auth-danger: #f87171;
+  --auth-shadow: 0 24px 80px rgba(0, 0, 0, 0.42);
+}
+
+html.light {
+  color-scheme: light;
+  --auth-page-bg: #f6f8fb;
+  --auth-glow: radial-gradient(circle at top, rgba(31, 136, 61, 0.14), transparent 32rem);
+  --auth-card-bg: #ffffff;
+  --auth-border: #d0d7de;
+  --auth-text: #1f2328;
+  --auth-text-secondary: #424a53;
+  --auth-text-quiet: #656d76;
+  --auth-field-bg: #ffffff;
+  --auth-focus: #1f883d;
+  --auth-action-bg: #1f2328;
+  --auth-action-text: #ffffff;
+  --auth-focus-ring: rgba(31, 136, 61, 0.18);
+  --auth-action-hover: #32383f;
+  --auth-action-disabled: #8c959f;
+  --auth-link: #0969da;
+  --auth-link-hover: #0550ae;
+  --auth-placeholder: #8c959f;
+  --auth-surface: #f6f8fa;
+  --auth-surface-strong: #eef2f7;
+  --auth-good: #1a7f37;
+  --auth-danger: #cf222e;
+  --auth-shadow: 0 18px 50px rgba(31, 35, 40, 0.12);
+}
+
+/*
+ * Keep the semantic mappings reusable without displacing the existing
+ * unlayered dark page styles. The scoped light rules below intentionally win
+ * when the shared theme controller applies html.light.
+ */
+@layer auth-page-theme {
+  body {
+    background: var(--auth-page-bg);
+    color: var(--auth-text);
+  }
+
+  .auth-shell {
+    background-image: var(--auth-glow);
+  }
+
+  .auth-card {
+    background: var(--auth-card-bg);
+    border-color: var(--auth-border);
+  }
+
+  .auth-sub {
+    color: var(--auth-text-secondary);
+  }
+
+  .hint {
+    color: var(--auth-text-quiet);
+  }
+
+  input, textarea, select {
+    background: var(--auth-field-bg);
+    border-color: var(--auth-border);
+    color: var(--auth-text);
+  }
+
+  input:focus, textarea:focus, select:focus {
+    border-color: var(--auth-focus);
+  }
+
+  .auth-button-primary {
+    background: var(--auth-action-bg);
+    color: var(--auth-action-text);
+  }
+}
+
+html.light body {
+  background: var(--auth-page-bg);
+  color: var(--auth-text);
+}
+
+html.light .auth-shell {
+  background-color: var(--auth-page-bg);
+  background-image: var(--auth-glow);
+}
+
+html.light .auth-card {
+  background: var(--auth-card-bg);
+  border-color: var(--auth-border);
+  box-shadow: var(--auth-shadow);
+}
+
+html.light .auth-card h1,
+html.light .join-title,
+html.light .calc h3,
+html.light .panel h3 {
+  color: var(--auth-text);
+}
+
+html.light .auth-sub,
+html.light .join-sub,
+html.light #mp-node,
+html.light .terms-check,
+html.light .flow {
+  color: var(--auth-text-secondary);
+}
+
+html.light .join-eyebrow,
+html.light .hint,
+html.light .row-links,
+html.light .calc-row .op,
+html.light .calc-cell .cap,
+html.light .calc-cell .small,
+html.light .calc-note,
+html.light .panel .meta,
+html.light .team-row .sub,
+html.light .log-row .sub,
+html.light .private-badge {
+  color: var(--auth-text-quiet);
+}
+
+html.light .field label {
+  color: var(--auth-text-secondary);
+}
+
+html.light .calc,
+html.light .flow div,
+html.light .panel,
+html.light .join-banner,
+html.light .demo-credentials {
+  background: var(--auth-surface);
+  border-color: var(--auth-border);
+}
+
+html.light .join-banner strong,
+html.light .calc-cell .big,
+html.light .flow strong,
+html.light .team-row .who,
+html.light .log-row .what .to,
+html.light .demo-credentials strong,
+html.light .demo-credentials code {
+  color: var(--auth-text);
+}
+
+html.light .join-banner span {
+  color: var(--auth-text-secondary);
+}
+
+html.light .team-row,
+html.light .log-row,
+html.light .private-badge {
+  border-color: var(--auth-border);
+}
+
+html.light input,
+html.light textarea,
+html.light select {
+  background: var(--auth-field-bg);
+  border-color: var(--auth-border);
+  color: var(--auth-text);
+}
+
+html.light input::placeholder,
+html.light textarea::placeholder {
+  color: var(--auth-placeholder);
+  opacity: 1;
+}
+
+html.light .field select option {
+  background: var(--auth-card-bg);
+  color: var(--auth-text);
+}
+
+html.light input:focus,
+html.light textarea:focus,
+html.light select:focus {
+  outline: 0;
+  border-color: var(--auth-focus);
+  box-shadow: 0 0 0 3px var(--auth-focus-ring);
+}
+
+html.light input[type="checkbox"] {
+  accent-color: var(--auth-focus);
+}
+
+html.light .auth-button-primary,
+html.light .btn-primary {
+  background: var(--auth-action-bg);
+  color: var(--auth-action-text);
+}
+
+html.light .auth-button-primary:hover,
+html.light .btn-primary:hover {
+  background: var(--auth-action-hover);
+  color: var(--auth-action-text);
+}
+
+html.light .auth-button-primary:focus-visible,
+html.light .btn-primary:focus-visible,
+html.light .btn-sm:focus-visible,
+html.light .demo-credentials button:focus-visible {
+  outline: 0;
+  box-shadow: 0 0 0 3px var(--auth-focus-ring);
+}
+
+html.light .auth-button-primary:disabled,
+html.light .btn-primary:disabled {
+  background: var(--auth-action-disabled);
+  color: var(--auth-action-text);
+}
+
+html.light .btn-sm,
+html.light .demo-credentials button {
+  background: var(--auth-field-bg);
+  border-color: var(--auth-border);
+  color: var(--auth-text);
+}
+
+html.light .btn-sm:hover,
+html.light .demo-credentials button:hover {
+  background: var(--auth-surface-strong);
+}
+
+html.light .auth-shell a {
+  color: var(--auth-link);
+  text-decoration-color: color-mix(in srgb, var(--auth-link), transparent 55%);
+}
+
+html.light .auth-shell a:hover {
+  color: var(--auth-link-hover);
+  text-decoration-color: currentColor;
+}
+
+html.light .auth-shell .brand {
+  color: inherit;
+}
+
+html.light .hint.bad,
+html.light .log-fail {
+  color: var(--auth-danger);
+}
+
+html.light .hint.good,
+html.light .log-ok {
+  color: var(--auth-good);
+}
diff --git a/cloudflare_worker/public/blog/actions/index.html b/cloudflare_worker/public/blog/actions/index.html
index 707e5fc8..c629dd1d 100644
--- a/cloudflare_worker/public/blog/actions/index.html
+++ b/cloudflare_worker/public/blog/actions/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/actions.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/actions.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/agents-can-ask-you-questions/index.html b/cloudflare_worker/public/blog/agents-can-ask-you-questions/index.html
index fb6b8445..72cd0ecb 100644
--- a/cloudflare_worker/public/blog/agents-can-ask-you-questions/index.html
+++ b/cloudflare_worker/public/blog/agents-can-ask-you-questions/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/agents-can-ask-you-questions.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/agents-can-ask-you-questions.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/ai-code-review/index.html b/cloudflare_worker/public/blog/ai-code-review/index.html
index 55bd0b0d..645f41cc 100644
--- a/cloudflare_worker/public/blog/ai-code-review/index.html
+++ b/cloudflare_worker/public/blog/ai-code-review/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/ai-code-review.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/ai-code-review.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/ai-conflict-resolution/index.html b/cloudflare_worker/public/blog/ai-conflict-resolution/index.html
index 50748fdc..467b839d 100644
--- a/cloudflare_worker/public/blog/ai-conflict-resolution/index.html
+++ b/cloudflare_worker/public/blog/ai-conflict-resolution/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/ai-conflict-resolution.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/ai-conflict-resolution.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/anonymous-accounts/index.html b/cloudflare_worker/public/blog/anonymous-accounts/index.html
index e4dd1f53..d3f03e15 100644
--- a/cloudflare_worker/public/blog/anonymous-accounts/index.html
+++ b/cloudflare_worker/public/blog/anonymous-accounts/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/anonymous-accounts.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/anonymous-accounts.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/artifacts/index.html b/cloudflare_worker/public/blog/artifacts/index.html
index 9eebccca..f7d7a0f3 100644
--- a/cloudflare_worker/public/blog/artifacts/index.html
+++ b/cloudflare_worker/public/blog/artifacts/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/artifacts.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/artifacts.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/assign-any-issue-to-an-agent/index.html b/cloudflare_worker/public/blog/assign-any-issue-to-an-agent/index.html
index 8b0cae28..7e83595f 100644
--- a/cloudflare_worker/public/blog/assign-any-issue-to-an-agent/index.html
+++ b/cloudflare_worker/public/blog/assign-any-issue-to-an-agent/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/assign-any-issue-to-an-agent.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/assign-any-issue-to-an-agent.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/binary-safe-patches/index.html b/cloudflare_worker/public/blog/binary-safe-patches/index.html
index 3455b2a6..4e1ad159 100644
--- a/cloudflare_worker/public/blog/binary-safe-patches/index.html
+++ b/cloudflare_worker/public/blog/binary-safe-patches/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/binary-safe-patches.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/binary-safe-patches.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/branch-backed-and-patch-backed-prs/index.html b/cloudflare_worker/public/blog/branch-backed-and-patch-backed-prs/index.html
index 5eabc4a8..3ab65810 100644
--- a/cloudflare_worker/public/blog/branch-backed-and-patch-backed-prs/index.html
+++ b/cloudflare_worker/public/blog/branch-backed-and-patch-backed-prs/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/branch-backed-and-patch-backed-prs.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/branch-backed-and-patch-backed-prs.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/built-to-stay-up/index.html b/cloudflare_worker/public/blog/built-to-stay-up/index.html
index ef8eee58..8d697482 100644
--- a/cloudflare_worker/public/blog/built-to-stay-up/index.html
+++ b/cloudflare_worker/public/blog/built-to-stay-up/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/built-to-stay-up.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/built-to-stay-up.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/burnup-charts/index.html b/cloudflare_worker/public/blog/burnup-charts/index.html
index e5d10fab..ae22f715 100644
--- a/cloudflare_worker/public/blog/burnup-charts/index.html
+++ b/cloudflare_worker/public/blog/burnup-charts/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/burnup-charts.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/burnup-charts.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/clean-repo-urls/index.html b/cloudflare_worker/public/blog/clean-repo-urls/index.html
index a10cf708..9078bd66 100644
--- a/cloudflare_worker/public/blog/clean-repo-urls/index.html
+++ b/cloudflare_worker/public/blog/clean-repo-urls/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/clean-repo-urls.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/clean-repo-urls.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/complete-repository-workspace/index.html b/cloudflare_worker/public/blog/complete-repository-workspace/index.html
index b2f16f88..92cb810e 100644
--- a/cloudflare_worker/public/blog/complete-repository-workspace/index.html
+++ b/cloudflare_worker/public/blog/complete-repository-workspace/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/complete-repository-workspace.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/complete-repository-workspace.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/content-addressed-release-storage/index.html b/cloudflare_worker/public/blog/content-addressed-release-storage/index.html
index 0d74e8db..00b42d7e 100644
--- a/cloudflare_worker/public/blog/content-addressed-release-storage/index.html
+++ b/cloudflare_worker/public/blog/content-addressed-release-storage/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/content-addressed-release-storage.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/content-addressed-release-storage.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/coves/index.html b/cloudflare_worker/public/blog/coves/index.html
index 9abc97b8..1104dcf6 100644
--- a/cloudflare_worker/public/blog/coves/index.html
+++ b/cloudflare_worker/public/blog/coves/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/coves.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/coves.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/custom-agent-instructions/index.html b/cloudflare_worker/public/blog/custom-agent-instructions/index.html
index 77b546eb..8f7f63a7 100644
--- a/cloudflare_worker/public/blog/custom-agent-instructions/index.html
+++ b/cloudflare_worker/public/blog/custom-agent-instructions/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/custom-agent-instructions.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/custom-agent-instructions.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/dedicated-agent-worktrees/index.html b/cloudflare_worker/public/blog/dedicated-agent-worktrees/index.html
index 13d25e0a..51948b10 100644
--- a/cloudflare_worker/public/blog/dedicated-agent-worktrees/index.html
+++ b/cloudflare_worker/public/blog/dedicated-agent-worktrees/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/dedicated-agent-worktrees.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/dedicated-agent-worktrees.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/desktop-node-mirrors/index.html b/cloudflare_worker/public/blog/desktop-node-mirrors/index.html
index d6bfd040..d0214a0c 100644
--- a/cloudflare_worker/public/blog/desktop-node-mirrors/index.html
+++ b/cloudflare_worker/public/blog/desktop-node-mirrors/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/desktop-node-mirrors.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/desktop-node-mirrors.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/direct-messages/index.html b/cloudflare_worker/public/blog/direct-messages/index.html
index 8524f9a2..49bf8ae8 100644
--- a/cloudflare_worker/public/blog/direct-messages/index.html
+++ b/cloudflare_worker/public/blog/direct-messages/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/direct-messages.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/direct-messages.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/discussions/index.html b/cloudflare_worker/public/blog/discussions/index.html
index cb894e99..e49cccd7 100644
--- a/cloudflare_worker/public/blog/discussions/index.html
+++ b/cloudflare_worker/public/blog/discussions/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/discussions.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/discussions.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/docs-with-search/index.html b/cloudflare_worker/public/blog/docs-with-search/index.html
index be055288..5df54005 100644
--- a/cloudflare_worker/public/blog/docs-with-search/index.html
+++ b/cloudflare_worker/public/blog/docs-with-search/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/docs-with-search.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/docs-with-search.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/ed25519-identity/index.html b/cloudflare_worker/public/blog/ed25519-identity/index.html
index 7fbd67aa..5c08f0b5 100644
--- a/cloudflare_worker/public/blog/ed25519-identity/index.html
+++ b/cloudflare_worker/public/blog/ed25519-identity/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/ed25519-identity.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/ed25519-identity.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/encrypted-repo-rooms/index.html b/cloudflare_worker/public/blog/encrypted-repo-rooms/index.html
index 7c8881af..e7561b45 100644
--- a/cloudflare_worker/public/blog/encrypted-repo-rooms/index.html
+++ b/cloudflare_worker/public/blog/encrypted-repo-rooms/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/encrypted-repo-rooms.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/encrypted-repo-rooms.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/everything-lives-in-git/index.html b/cloudflare_worker/public/blog/everything-lives-in-git/index.html
index d630448d..8878b02b 100644
--- a/cloudflare_worker/public/blog/everything-lives-in-git/index.html
+++ b/cloudflare_worker/public/blog/everything-lives-in-git/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/everything-lives-in-git.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/everything-lives-in-git.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/fork-with-intent/index.html b/cloudflare_worker/public/blog/fork-with-intent/index.html
index 932c0528..f1ffad10 100644
--- a/cloudflare_worker/public/blog/fork-with-intent/index.html
+++ b/cloudflare_worker/public/blog/fork-with-intent/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/fork-with-intent.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/fork-with-intent.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/full-issue-tracker/index.html b/cloudflare_worker/public/blog/full-issue-tracker/index.html
index 6abdda49..f59b5280 100644
--- a/cloudflare_worker/public/blog/full-issue-tracker/index.html
+++ b/cloudflare_worker/public/blog/full-issue-tracker/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/full-issue-tracker.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/full-issue-tracker.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/headless-mirror-nodes/index.html b/cloudflare_worker/public/blog/headless-mirror-nodes/index.html
index a5eb3c3d..b4fdc134 100644
--- a/cloudflare_worker/public/blog/headless-mirror-nodes/index.html
+++ b/cloudflare_worker/public/blog/headless-mirror-nodes/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/headless-mirror-nodes.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/headless-mirror-nodes.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/insights/index.html b/cloudflare_worker/public/blog/insights/index.html
index 880dde96..8b92e46e 100644
--- a/cloudflare_worker/public/blog/insights/index.html
+++ b/cloudflare_worker/public/blog/insights/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/insights.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/insights.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/issue-bounties/index.html b/cloudflare_worker/public/blog/issue-bounties/index.html
index fa6a3a0d..c9ecee92 100644
--- a/cloudflare_worker/public/blog/issue-bounties/index.html
+++ b/cloudflare_worker/public/blog/issue-bounties/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/issue-bounties.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/issue-bounties.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/issues-prs-that-talk/index.html b/cloudflare_worker/public/blog/issues-prs-that-talk/index.html
index ae9dedc6..7fdb77db 100644
--- a/cloudflare_worker/public/blog/issues-prs-that-talk/index.html
+++ b/cloudflare_worker/public/blog/issues-prs-that-talk/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/issues-prs-that-talk.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/issues-prs-that-talk.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/kanban-board/index.html b/cloudflare_worker/public/blog/kanban-board/index.html
index 8d0ee17e..548c6595 100644
--- a/cloudflare_worker/public/blog/kanban-board/index.html
+++ b/cloudflare_worker/public/blog/kanban-board/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/kanban-board.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/kanban-board.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/keyboard-first-navigation/index.html b/cloudflare_worker/public/blog/keyboard-first-navigation/index.html
index c40857af..cfd8031e 100644
--- a/cloudflare_worker/public/blog/keyboard-first-navigation/index.html
+++ b/cloudflare_worker/public/blog/keyboard-first-navigation/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/keyboard-first-navigation.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/keyboard-first-navigation.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/live-clone-routing/index.html b/cloudflare_worker/public/blog/live-clone-routing/index.html
index 1244466f..f828366d 100644
--- a/cloudflare_worker/public/blog/live-clone-routing/index.html
+++ b/cloudflare_worker/public/blog/live-clone-routing/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/live-clone-routing.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/live-clone-routing.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/live-usage-meter/index.html b/cloudflare_worker/public/blog/live-usage-meter/index.html
index c20bd1f3..5113b3ca 100644
--- a/cloudflare_worker/public/blog/live-usage-meter/index.html
+++ b/cloudflare_worker/public/blog/live-usage-meter/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/live-usage-meter.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/live-usage-meter.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/make-it-rain/index.html b/cloudflare_worker/public/blog/make-it-rain/index.html
index 8cb1d26d..3603f74c 100644
--- a/cloudflare_worker/public/blog/make-it-rain/index.html
+++ b/cloudflare_worker/public/blog/make-it-rain/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/make-it-rain.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/make-it-rain.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/markdown-everywhere/index.html b/cloudflare_worker/public/blog/markdown-everywhere/index.html
index 6a859e59..8e3c08d5 100644
--- a/cloudflare_worker/public/blog/markdown-everywhere/index.html
+++ b/cloudflare_worker/public/blog/markdown-everywhere/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/markdown-everywhere.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/markdown-everywhere.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/merge-from-anywhere/index.html b/cloudflare_worker/public/blog/merge-from-anywhere/index.html
index 2ac2900d..572b787a 100644
--- a/cloudflare_worker/public/blog/merge-from-anywhere/index.html
+++ b/cloudflare_worker/public/blog/merge-from-anywhere/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/merge-from-anywhere.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/merge-from-anywhere.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/milestones-with-progress/index.html b/cloudflare_worker/public/blog/milestones-with-progress/index.html
index 5bed0448..760de103 100644
--- a/cloudflare_worker/public/blog/milestones-with-progress/index.html
+++ b/cloudflare_worker/public/blog/milestones-with-progress/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/milestones-with-progress.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/milestones-with-progress.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/mirror-failover/index.html b/cloudflare_worker/public/blog/mirror-failover/index.html
index f7bbbdfd..5cb2d9fd 100644
--- a/cloudflare_worker/public/blog/mirror-failover/index.html
+++ b/cloudflare_worker/public/blog/mirror-failover/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/mirror-failover.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/mirror-failover.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/mirror-rewards/index.html b/cloudflare_worker/public/blog/mirror-rewards/index.html
index 194a21b5..f77d2dbe 100644
--- a/cloudflare_worker/public/blog/mirror-rewards/index.html
+++ b/cloudflare_worker/public/blog/mirror-rewards/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/mirror-rewards.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/mirror-rewards.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/mobile-app/index.html b/cloudflare_worker/public/blog/mobile-app/index.html
index c8ceea95..6d6a8d78 100644
--- a/cloudflare_worker/public/blog/mobile-app/index.html
+++ b/cloudflare_worker/public/blog/mobile-app/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/mobile-app.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/mobile-app.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/native-terminal/index.html b/cloudflare_worker/public/blog/native-terminal/index.html
index cbce558c..a8e59489 100644
--- a/cloudflare_worker/public/blog/native-terminal/index.html
+++ b/cloudflare_worker/public/blog/native-terminal/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/native-terminal.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/native-terminal.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/one-line-installer/index.html b/cloudflare_worker/public/blog/one-line-installer/index.html
index 2801d1d8..f8313778 100644
--- a/cloudflare_worker/public/blog/one-line-installer/index.html
+++ b/cloudflare_worker/public/blog/one-line-installer/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/one-line-installer.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/one-line-installer.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/parallel-agents/index.html b/cloudflare_worker/public/blog/parallel-agents/index.html
index 31bae9e4..93a7345c 100644
--- a/cloudflare_worker/public/blog/parallel-agents/index.html
+++ b/cloudflare_worker/public/blog/parallel-agents/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/parallel-agents.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/parallel-agents.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/presence-done-right/index.html b/cloudflare_worker/public/blog/presence-done-right/index.html
index 3918b431..574238e5 100644
--- a/cloudflare_worker/public/blog/presence-done-right/index.html
+++ b/cloudflare_worker/public/blog/presence-done-right/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/presence-done-right.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/presence-done-right.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/public-network-map/index.html b/cloudflare_worker/public/blog/public-network-map/index.html
index 3597f604..f1509684 100644
--- a/cloudflare_worker/public/blog/public-network-map/index.html
+++ b/cloudflare_worker/public/blog/public-network-map/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/public-network-map.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/public-network-map.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/public-node-profiles/index.html b/cloudflare_worker/public/blog/public-node-profiles/index.html
index 5cf59fde..fee0e747 100644
--- a/cloudflare_worker/public/blog/public-node-profiles/index.html
+++ b/cloudflare_worker/public/blog/public-node-profiles/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/public-node-profiles.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/public-node-profiles.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/qr-handoffs/index.html b/cloudflare_worker/public/blog/qr-handoffs/index.html
index 1a79136a..5aa10fb3 100644
--- a/cloudflare_worker/public/blog/qr-handoffs/index.html
+++ b/cloudflare_worker/public/blog/qr-handoffs/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/qr-handoffs.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/qr-handoffs.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/quality-and-security-tabs/index.html b/cloudflare_worker/public/blog/quality-and-security-tabs/index.html
index c7579f27..721b8df3 100644
--- a/cloudflare_worker/public/blog/quality-and-security-tabs/index.html
+++ b/cloudflare_worker/public/blog/quality-and-security-tabs/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/quality-and-security-tabs.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/quality-and-security-tabs.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/quick-add-from-anywhere/index.html b/cloudflare_worker/public/blog/quick-add-from-anywhere/index.html
index 5d701bb5..87056315 100644
--- a/cloudflare_worker/public/blog/quick-add-from-anywhere/index.html
+++ b/cloudflare_worker/public/blog/quick-add-from-anywhere/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/quick-add-from-anywhere.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/quick-add-from-anywhere.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/real-mergeability-checks/index.html b/cloudflare_worker/public/blog/real-mergeability-checks/index.html
index bb72bbbc..125616bc 100644
--- a/cloudflare_worker/public/blog/real-mergeability-checks/index.html
+++ b/cloudflare_worker/public/blog/real-mergeability-checks/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/real-mergeability-checks.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/real-mergeability-checks.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/releases-and-tags/index.html b/cloudflare_worker/public/blog/releases-and-tags/index.html
index 9df2137a..6793fd15 100644
--- a/cloudflare_worker/public/blog/releases-and-tags/index.html
+++ b/cloudflare_worker/public/blog/releases-and-tags/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/releases-and-tags.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/releases-and-tags.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/resumable-sessions/index.html b/cloudflare_worker/public/blog/resumable-sessions/index.html
index d6c7458e..9df45e2e 100644
--- a/cloudflare_worker/public/blog/resumable-sessions/index.html
+++ b/cloudflare_worker/public/blog/resumable-sessions/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/resumable-sessions.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/resumable-sessions.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/review-conversations/index.html b/cloudflare_worker/public/blog/review-conversations/index.html
index 89236308..df8c6c51 100644
--- a/cloudflare_worker/public/blog/review-conversations/index.html
+++ b/cloudflare_worker/public/blog/review-conversations/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/review-conversations.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/review-conversations.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/rich-diffs/index.html b/cloudflare_worker/public/blog/rich-diffs/index.html
index 8572501c..d999b78a 100644
--- a/cloudflare_worker/public/blog/rich-diffs/index.html
+++ b/cloudflare_worker/public/blog/rich-diffs/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/rich-diffs.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/rich-diffs.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/rich-live-transcripts/index.html b/cloudflare_worker/public/blog/rich-live-transcripts/index.html
index 40e996b3..e727830f 100644
--- a/cloudflare_worker/public/blog/rich-live-transcripts/index.html
+++ b/cloudflare_worker/public/blog/rich-live-transcripts/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/rich-live-transcripts.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/rich-live-transcripts.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/root-commit-identity/index.html b/cloudflare_worker/public/blog/root-commit-identity/index.html
index cb9f2c02..544ed3a2 100644
--- a/cloudflare_worker/public/blog/root-commit-identity/index.html
+++ b/cloudflare_worker/public/blog/root-commit-identity/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/root-commit-identity.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/root-commit-identity.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/screenshot-and-markup/index.html b/cloudflare_worker/public/blog/screenshot-and-markup/index.html
index 6a462147..16a17d9d 100644
--- a/cloudflare_worker/public/blog/screenshot-and-markup/index.html
+++ b/cloudflare_worker/public/blog/screenshot-and-markup/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/screenshot-and-markup.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/screenshot-and-markup.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/signed-issue-events/index.html b/cloudflare_worker/public/blog/signed-issue-events/index.html
index f75ec1b4..d604d432 100644
--- a/cloudflare_worker/public/blog/signed-issue-events/index.html
+++ b/cloudflare_worker/public/blog/signed-issue-events/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/signed-issue-events.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/signed-issue-events.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/signed-patch-pull-requests/index.html b/cloudflare_worker/public/blog/signed-patch-pull-requests/index.html
index 51d230e7..44b45cce 100644
--- a/cloudflare_worker/public/blog/signed-patch-pull-requests/index.html
+++ b/cloudflare_worker/public/blog/signed-patch-pull-requests/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/signed-patch-pull-requests.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/signed-patch-pull-requests.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/solana-donations/index.html b/cloudflare_worker/public/blog/solana-donations/index.html
index 3acfcfa8..f8c24657 100644
--- a/cloudflare_worker/public/blog/solana-donations/index.html
+++ b/cloudflare_worker/public/blog/solana-donations/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/solana-donations.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/solana-donations.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/source-attested-integrity/index.html b/cloudflare_worker/public/blog/source-attested-integrity/index.html
index 94980e6d..b279085b 100644
--- a/cloudflare_worker/public/blog/source-attested-integrity/index.html
+++ b/cloudflare_worker/public/blog/source-attested-integrity/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/source-attested-integrity.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/source-attested-integrity.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/source-control-and-worktrees/index.html b/cloudflare_worker/public/blog/source-control-and-worktrees/index.html
index 62cda58f..86c942be 100644
--- a/cloudflare_worker/public/blog/source-control-and-worktrees/index.html
+++ b/cloudflare_worker/public/blog/source-control-and-worktrees/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/source-control-and-worktrees.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/source-control-and-worktrees.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/status-blog-and-changelog/index.html b/cloudflare_worker/public/blog/status-blog-and-changelog/index.html
index 80e2e97e..1e62c32b 100644
--- a/cloudflare_worker/public/blog/status-blog-and-changelog/index.html
+++ b/cloudflare_worker/public/blog/status-blog-and-changelog/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/status-blog-and-changelog.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/status-blog-and-changelog.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/streaming-transfers/index.html b/cloudflare_worker/public/blog/streaming-transfers/index.html
index 3c537021..62c612bd 100644
--- a/cloudflare_worker/public/blog/streaming-transfers/index.html
+++ b/cloudflare_worker/public/blog/streaming-transfers/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/streaming-transfers.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/streaming-transfers.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/voice-dictation/index.html b/cloudflare_worker/public/blog/voice-dictation/index.html
index dbf29535..6fb210a1 100644
--- a/cloudflare_worker/public/blog/voice-dictation/index.html
+++ b/cloudflare_worker/public/blog/voice-dictation/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/voice-dictation.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/voice-dictation.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/vs-code-extension/index.html b/cloudflare_worker/public/blog/vs-code-extension/index.html
index 18ab4df7..96d5d99e 100644
--- a/cloudflare_worker/public/blog/vs-code-extension/index.html
+++ b/cloudflare_worker/public/blog/vs-code-extension/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/vs-code-extension.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/vs-code-extension.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/web-chat/index.html b/cloudflare_worker/public/blog/web-chat/index.html
index b58cfd80..194550e5 100644
--- a/cloudflare_worker/public/blog/web-chat/index.html
+++ b/cloudflare_worker/public/blog/web-chat/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/web-chat.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/web-chat.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/web-contribution-inbox/index.html b/cloudflare_worker/public/blog/web-contribution-inbox/index.html
index b7f83521..d2c9afd6 100644
--- a/cloudflare_worker/public/blog/web-contribution-inbox/index.html
+++ b/cloudflare_worker/public/blog/web-contribution-inbox/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/web-contribution-inbox.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/web-contribution-inbox.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/web-repo-browser/index.html b/cloudflare_worker/public/blog/web-repo-browser/index.html
index c441a8d1..8e6b14a9 100644
--- a/cloudflare_worker/public/blog/web-repo-browser/index.html
+++ b/cloudflare_worker/public/blog/web-repo-browser/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/web-repo-browser.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/web-repo-browser.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/your-app-is-the-ide/index.html b/cloudflare_worker/public/blog/your-app-is-the-ide/index.html
index 84e5a073..9bacb158 100644
--- a/cloudflare_worker/public/blog/your-app-is-the-ide/index.html
+++ b/cloudflare_worker/public/blog/your-app-is-the-ide/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/your-app-is-the-ide.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/your-app-is-the-ide.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/blog/zero-custody-relay/index.html b/cloudflare_worker/public/blog/zero-custody-relay/index.html
index 66d10e45..5db41566 100644
--- a/cloudflare_worker/public/blog/zero-custody-relay/index.html
+++ b/cloudflare_worker/public/blog/zero-custody-relay/index.html
@@ -27,35 +27,7 @@
     <meta property="og:image" content="https://forkmesh.com/assets/blog/features/zero-custody-relay.webp">
     <meta name="twitter:card" content="summary_large_image">
     <meta name="twitter:image" content="https://forkmesh.com/assets/blog/features/zero-custody-relay.webp">
-    <style>
-      :root { color-scheme: dark; --bg:#030303; --paper:#080808; --line:#252525; --soft:#141414; --text:#f6f6f2; --muted:#9a9a94; --green:#3fb950; --cyan:#76d8f6; --serif: Georgia, "Times New Roman", serif; --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
-      * { box-sizing: border-box; }
-      body { margin:0; background:#000; color:var(--text); font-family:var(--sans); -webkit-font-smoothing:antialiased; }
-      a { color:inherit; text-decoration:none; }
-      .shell { width:min(100%, 1120px); margin:0 auto; min-height:100vh; border-left:1px solid var(--line); border-right:1px solid var(--line); background:var(--paper); }
-      .top { height:82px; display:flex; align-items:center; justify-content:space-between; padding:0 32px; border-bottom:1px solid var(--line); }
-      .brand { display:inline-flex; gap:12px; align-items:center; font-weight:900; font-size:24px; letter-spacing:0; }
-      .brand img { width:32px; height:36px; object-fit:contain; }
-      .top a.link { color:var(--muted); font-size:14px; }
-      .top a.link:hover { color:var(--text); }
-      main { padding:44px 32px 80px; }
-      .back { color:var(--muted); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; }
-      .back:hover { color:var(--cyan); }
-      .hero { margin-top:30px; display:grid; grid-template-columns:minmax(0,1fr); gap:30px; }
-      .eyebrow { color:var(--cyan); font-family:var(--mono); font-size:13px; text-transform:uppercase; letter-spacing:0; margin:0 0 18px; }
-      h1 { font-family:var(--serif); font-weight:500; font-size:64px; line-height:1.02; letter-spacing:0; margin:0; max-width:870px; }
-      .lede { color:var(--muted); font-size:20px; line-height:1.6; max-width:760px; margin:24px 0 0; }
-      .meta { display:flex; flex-wrap:wrap; gap:10px; align-items:center; margin-top:24px; color:var(--muted); font-size:14px; }
-      .pill { border:1px solid var(--line); border-radius:999px; padding:6px 10px; color:var(--text); background:var(--soft); }
-      .hero-img { width:100%; aspect-ratio:16/9; object-fit:cover; border:1px solid var(--line); border-radius:14px; display:block; background:#050505; }
-      .body { max-width:790px; margin:44px auto 0; font-size:18px; line-height:1.78; }
-      .body h2 { margin:42px 0 8px; font-size:28px; line-height:1.2; letter-spacing:0; }
-      .body p { margin:14px 0; color:#e5e5df; }
-      .body .note { margin:34px 0; border:1px solid var(--line); background:#0f0f0f; border-radius:14px; padding:22px; color:var(--muted); }
-      .next { margin-top:56px; padding-top:26px; border-top:1px solid var(--line); display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap; color:var(--muted); }
-      .next a:hover { color:var(--cyan); }
-      @media (max-width: 760px) { .top { padding:0 20px; } main { padding:30px 20px 64px; } .brand { font-size:20px; } h1 { font-size:42px; } }
-</style>
+    <link rel="stylesheet" href="/feature-post.css">
   </head>
   <body>
     <div class="shell">
diff --git a/cloudflare_worker/public/docs.html b/cloudflare_worker/public/docs.html
index 0cc97dfa..ab4c2886 100644
--- a/cloudflare_worker/public/docs.html
+++ b/cloudflare_worker/public/docs.html
@@ -172,36 +172,6 @@
         display: block;
       }
 
-      .icon-button {
-        display: inline-grid;
-        place-items: center;
-        width: 2.5rem;
-        height: 2.5rem;
-        border: 0;
-        border-radius: 999px;
-        background: transparent;
-        color: var(--docs-soft);
-        cursor: pointer;
-      }
-
-      .icon-button:hover {
-        color: var(--docs-fg);
-        background: var(--docs-hover);
-      }
-
-      .icon-button svg {
-        width: 1rem;
-        height: 1rem;
-      }
-
-      html.dark .moon {
-        display: none;
-      }
-
-      html:not(.dark) .sun {
-        display: none;
-      }
-
       .docs-scrollbar {
         scrollbar-width: thin;
         scrollbar-color: var(--docs-line-strong) transparent;
@@ -303,26 +273,16 @@
     </style>
     <link rel="stylesheet" href="/site-header.css" />
     <script src="/site-header.js" defer></script>
-    <style>
-      /* Map the docs theme onto the universal header's variable names so it
-         follows the docs light/dark toggle (docs styles use --docs-*). */
-      :root {
-        --muted: var(--docs-muted);
-        --foreground: var(--docs-fg);
-        --border: var(--docs-line);
-        --card: var(--docs-panel);
-      }
-    </style>
   </head>
   <body class="min-h-screen bg-[var(--docs-bg)] text-[var(--docs-fg)]">
     <div data-forkmesh-header="simple"></div>
     <header
-      class="sticky top-0 z-50 border-b border-[var(--docs-line)] bg-[color-mix(in_srgb,var(--docs-bg)_94%,transparent)] backdrop-blur-xl"
+      class="sticky top-0 z-30 border-b border-[var(--docs-line)] bg-[color-mix(in_srgb,var(--docs-bg)_94%,transparent)] backdrop-blur-xl"
     >
       <div
-        class="mx-auto grid h-20 max-w-[1540px] grid-cols-[minmax(220px,520px)_minmax(0,1fr)] items-center gap-5 px-5 sm:px-8 lg:px-12 xl:grid-cols-[minmax(260px,560px)_minmax(0,1fr)] xl:gap-6"
+        class="mx-auto hidden h-20 max-w-[1540px] grid-cols-1 items-center px-5 sm:px-8 md:grid lg:px-12"
       >
-        <div class="relative hidden min-w-0 md:block">
+        <div class="relative min-w-0 max-w-[560px]">
           <label
             class="flex h-11 min-w-0 items-center justify-between gap-4 rounded-2xl border border-[var(--docs-line-strong)] bg-[var(--docs-panel)] px-4 text-left text-[0.92rem] font-semibold text-[var(--docs-soft)] shadow-[inset_0_0_0_1px_rgba(0,0,0,0.01)]"
             aria-label="Search documentation"
@@ -367,28 +327,6 @@
           </div>
         </div>
 
-        <div
-          class="flex min-w-0 items-center justify-end gap-3 sm:gap-4 xl:gap-5"
-        >
-          <button
-            id="theme-toggle"
-            class="icon-button"
-            type="button"
-            aria-label="Toggle theme"
-          >
-            <svg class="moon" viewBox="0 0 24 24" aria-hidden="true">
-              <path
-                d="M21 12.8A8.5 8.5 0 1 1 11.2 3 6.7 6.7 0 0 0 21 12.8Z"
-              ></path>
-            </svg>
-            <svg class="sun" viewBox="0 0 24 24" aria-hidden="true">
-              <circle cx="12" cy="12" r="4"></circle>
-              <path
-                d="M12 2v2M12 20v2M4.93 4.93l1.42 1.42M17.65 17.65l1.42 1.42M2 12h2M20 12h2M4.93 19.07l1.42-1.42M17.65 6.35l1.42-1.42"
-              ></path>
-            </svg>
-          </button>
-        </div>
       </div>
 
       <div
@@ -1956,12 +1894,12 @@ uvx --from workers-py pywrangler dev
             </p>
           </div>
           <button
-            class="icon-button -mr-2 -mt-2"
+            class="-mr-2 -mt-2 grid h-10 w-10 flex-none cursor-pointer place-items-center rounded-full text-[var(--docs-soft)] hover:bg-[var(--docs-hover)] hover:text-[var(--docs-fg)]"
             type="button"
             data-feedback-close
             aria-label="Close feedback"
           >
-            <svg viewBox="0 0 24 24" aria-hidden="true">
+            <svg class="h-4 w-4" viewBox="0 0 24 24" aria-hidden="true">
               <path d="M18 6 6 18"></path>
               <path d="m6 6 12 12"></path>
             </svg>
@@ -1993,33 +1931,6 @@ uvx --from workers-py pywrangler dev
 
     <script>
       (() => {
-        function applyTheme(mode) {
-          const chosen =
-            mode ||
-            localStorage.getItem("forkmesh.dashboard.theme") ||
-            localStorage.getItem("forkmesh.theme") ||
-            (window.matchMedia &&
-            window.matchMedia("(prefers-color-scheme: dark)").matches
-              ? "dark"
-              : "light");
-          document.documentElement.classList.toggle("dark", chosen === "dark");
-          localStorage.setItem("forkmesh.theme", chosen);
-          localStorage.setItem("forkmesh.dashboard.theme", chosen);
-        }
-
-        applyTheme();
-
-        const themeToggle = document.querySelector("#theme-toggle");
-        if (themeToggle) {
-          themeToggle.addEventListener("click", () => {
-            applyTheme(
-              document.documentElement.classList.contains("dark")
-                ? "light"
-                : "dark",
-            );
-          });
-        }
-
         const docsSearch = document.querySelector("#docs-search");
         const docsSearchPanel = document.querySelector("#docs-search-panel");
         const docsSearchResults = document.querySelector(
diff --git a/cloudflare_worker/public/docs/index.html b/cloudflare_worker/public/docs/index.html
index 0f85deb3..be7fa322 100644
--- a/cloudflare_worker/public/docs/index.html
+++ b/cloudflare_worker/public/docs/index.html
@@ -172,36 +172,6 @@
         display: block;
       }
 
-      .icon-button {
-        display: inline-grid;
-        place-items: center;
-        width: 2.5rem;
-        height: 2.5rem;
-        border: 0;
-        border-radius: 999px;
-        background: transparent;
-        color: var(--docs-soft);
-        cursor: pointer;
-      }
-
-      .icon-button:hover {
-        color: var(--docs-fg);
-        background: var(--docs-hover);
-      }
-
-      .icon-button svg {
-        width: 1rem;
-        height: 1rem;
-      }
-
-      html.dark .moon {
-        display: none;
-      }
-
-      html:not(.dark) .sun {
-        display: none;
-      }
-
       .docs-scrollbar {
         scrollbar-width: thin;
         scrollbar-color: var(--docs-line-strong) transparent;
@@ -303,26 +273,16 @@
     </style>
     <link rel="stylesheet" href="/site-header.css" />
     <script src="/site-header.js" defer></script>
-    <style>
-      /* Map the docs theme onto the universal header's variable names so it
-         follows the docs light/dark toggle (docs styles use --docs-*). */
-      :root {
-        --muted: var(--docs-muted);
-        --foreground: var(--docs-fg);
-        --border: var(--docs-line);
-        --card: var(--docs-panel);
-      }
-    </style>
   </head>
   <body class="min-h-screen bg-[var(--docs-bg)] text-[var(--docs-fg)]">
     <div data-forkmesh-header="simple"></div>
     <header
-      class="sticky top-0 z-50 border-b border-[var(--docs-line)] bg-[color-mix(in_srgb,var(--docs-bg)_94%,transparent)] backdrop-blur-xl"
+      class="sticky top-0 z-30 border-b border-[var(--docs-line)] bg-[color-mix(in_srgb,var(--docs-bg)_94%,transparent)] backdrop-blur-xl"
     >
       <div
-        class="mx-auto grid h-20 max-w-[1540px] grid-cols-[minmax(220px,520px)_minmax(0,1fr)] items-center gap-5 px-5 sm:px-8 lg:px-12 xl:grid-cols-[minmax(260px,560px)_minmax(0,1fr)] xl:gap-6"
+        class="mx-auto hidden h-20 max-w-[1540px] grid-cols-1 items-center px-5 sm:px-8 md:grid lg:px-12"
       >
-        <div class="relative hidden min-w-0 md:block">
+        <div class="relative min-w-0 max-w-[560px]">
           <label
             class="flex h-11 min-w-0 items-center justify-between gap-4 rounded-2xl border border-[var(--docs-line-strong)] bg-[var(--docs-panel)] px-4 text-left text-[0.92rem] font-semibold text-[var(--docs-soft)] shadow-[inset_0_0_0_1px_rgba(0,0,0,0.01)]"
             aria-label="Search documentation"
@@ -367,28 +327,6 @@
           </div>
         </div>
 
-        <div
-          class="flex min-w-0 items-center justify-end gap-3 sm:gap-4 xl:gap-5"
-        >
-          <button
-            id="theme-toggle"
-            class="icon-button"
-            type="button"
-            aria-label="Toggle theme"
-          >
-            <svg class="moon" viewBox="0 0 24 24" aria-hidden="true">
-              <path
-                d="M21 12.8A8.5 8.5 0 1 1 11.2 3 6.7 6.7 0 0 0 21 12.8Z"
-              ></path>
-            </svg>
-            <svg class="sun" viewBox="0 0 24 24" aria-hidden="true">
-              <circle cx="12" cy="12" r="4"></circle>
-              <path
-                d="M12 2v2M12 20v2M4.93 4.93l1.42 1.42M17.65 17.65l1.42 1.42M2 12h2M20 12h2M4.93 19.07l1.42-1.42M17.65 6.35l1.42-1.42"
-              ></path>
-            </svg>
-          </button>
-        </div>
       </div>
 
       <div
@@ -1970,12 +1908,12 @@ uvx --from workers-py pywrangler dev
             </p>
           </div>
           <button
-            class="icon-button -mr-2 -mt-2"
+            class="-mr-2 -mt-2 grid h-10 w-10 flex-none cursor-pointer place-items-center rounded-full text-[var(--docs-soft)] hover:bg-[var(--docs-hover)] hover:text-[var(--docs-fg)]"
             type="button"
             data-feedback-close
             aria-label="Close feedback"
           >
-            <svg viewBox="0 0 24 24" aria-hidden="true">
+            <svg class="h-4 w-4" viewBox="0 0 24 24" aria-hidden="true">
               <path d="M18 6 6 18"></path>
               <path d="m6 6 12 12"></path>
             </svg>
@@ -2007,33 +1945,6 @@ uvx --from workers-py pywrangler dev
 
     <script>
       (() => {
-        function applyTheme(mode) {
-          const chosen =
-            mode ||
-            localStorage.getItem("forkmesh.dashboard.theme") ||
-            localStorage.getItem("forkmesh.theme") ||
-            (window.matchMedia &&
-            window.matchMedia("(prefers-color-scheme: dark)").matches
-              ? "dark"
-              : "light");
-          document.documentElement.classList.toggle("dark", chosen === "dark");
-          localStorage.setItem("forkmesh.theme", chosen);
-          localStorage.setItem("forkmesh.dashboard.theme", chosen);
-        }
-
-        applyTheme();
-
-        const themeToggle = document.querySelector("#theme-toggle");
-        if (themeToggle) {
-          themeToggle.addEventListener("click", () => {
-            applyTheme(
-              document.documentElement.classList.contains("dark")
-                ? "light"
-                : "dark",
-            );
-          });
-        }
-
         const docsSearch = document.querySelector("#docs-search");
         const docsSearchPanel = document.querySelector("#docs-search-panel");
         const docsSearchResults = document.querySelector(
diff --git a/cloudflare_worker/public/feature-post.css b/cloudflare_worker/public/feature-post.css
new file mode 100644
index 00000000..6e002388
--- /dev/null
+++ b/cloudflare_worker/public/feature-post.css
@@ -0,0 +1,241 @@
+:root {
+  color-scheme: dark;
+  --bg: #000;
+  --paper: #080808;
+  --line: #252525;
+  --soft: #141414;
+  --text: #f6f6f2;
+  --muted: #9a9a94;
+  --green: #3fb950;
+  --cyan: #76d8f6;
+  --paragraph: #e5e5df;
+  --note: #0f0f0f;
+  --image-placeholder: #050505;
+  --serif: Georgia, "Times New Roman", serif;
+  --sans: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
+  --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
+}
+
+html.light {
+  color-scheme: light;
+  --bg: #f6f8fb;
+  --paper: #ffffff;
+  --line: #d0d7de;
+  --soft: #eef2f7;
+  --text: #1f2328;
+  --muted: #656d76;
+  --green: #1a7f37;
+  --cyan: #0969da;
+  --paragraph: #24292f;
+  --note: #f6f8fa;
+  --image-placeholder: #eaeef2;
+}
+
+* {
+  box-sizing: border-box;
+}
+
+body {
+  margin: 0;
+  background: var(--bg);
+  color: var(--text);
+  font-family: var(--sans);
+  -webkit-font-smoothing: antialiased;
+}
+
+a {
+  color: inherit;
+  text-decoration: none;
+}
+
+.shell {
+  width: min(100%, 1120px);
+  margin: 0 auto;
+  min-height: 100vh;
+  border-left: 1px solid var(--line);
+  border-right: 1px solid var(--line);
+  border-color: var(--line);
+  background: var(--paper);
+}
+
+.top {
+  height: 82px;
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  padding: 0 32px;
+  border-bottom: 1px solid var(--line);
+}
+
+.brand {
+  display: inline-flex;
+  gap: 12px;
+  align-items: center;
+  font-weight: 900;
+  font-size: 24px;
+  letter-spacing: 0;
+}
+
+.brand img {
+  width: 32px;
+  height: 36px;
+  object-fit: contain;
+}
+
+.top a.link {
+  color: var(--muted);
+  font-size: 14px;
+}
+
+.top a.link:hover {
+  color: var(--text);
+}
+
+main {
+  padding: 44px 32px 80px;
+}
+
+.back {
+  color: var(--muted);
+  font-family: var(--mono);
+  font-size: 13px;
+  text-transform: uppercase;
+  letter-spacing: 0;
+}
+
+.back:hover {
+  color: var(--cyan);
+}
+
+.hero {
+  margin-top: 30px;
+  display: grid;
+  grid-template-columns: minmax(0, 1fr);
+  gap: 30px;
+}
+
+.eyebrow {
+  color: var(--cyan);
+  font-family: var(--mono);
+  font-size: 13px;
+  text-transform: uppercase;
+  letter-spacing: 0;
+  margin: 0 0 18px;
+}
+
+h1 {
+  font-family: var(--serif);
+  font-weight: 500;
+  font-size: 64px;
+  line-height: 1.02;
+  letter-spacing: 0;
+  margin: 0;
+  max-width: 870px;
+}
+
+.lede {
+  color: var(--muted);
+  font-size: 20px;
+  line-height: 1.6;
+  max-width: 760px;
+  margin: 24px 0 0;
+}
+
+.meta {
+  display: flex;
+  flex-wrap: wrap;
+  gap: 10px;
+  align-items: center;
+  margin-top: 24px;
+  color: var(--muted);
+  font-size: 14px;
+}
+
+.pill {
+  border: 1px solid var(--line);
+  border-color: var(--line);
+  border-radius: 999px;
+  padding: 6px 10px;
+  color: var(--text);
+  background: var(--soft);
+}
+
+.hero-img {
+  width: 100%;
+  aspect-ratio: 16/9;
+  object-fit: cover;
+  border: 1px solid var(--line);
+  border-color: var(--line);
+  border-radius: 14px;
+  display: block;
+  background: var(--image-placeholder);
+}
+
+.body {
+  max-width: 790px;
+  margin: 44px auto 0;
+  font-size: 18px;
+  line-height: 1.78;
+}
+
+.body h2 {
+  margin: 42px 0 8px;
+  font-size: 28px;
+  line-height: 1.2;
+  letter-spacing: 0;
+}
+
+.body p {
+  margin: 14px 0;
+  color: var(--paragraph);
+}
+
+.body a {
+  color: var(--green);
+}
+
+html:not(.light) .body a {
+  color: var(--paragraph);
+}
+
+.body .note {
+  margin: 34px 0;
+  border: 1px solid var(--line);
+  background: var(--note);
+  border-radius: 14px;
+  padding: 22px;
+  color: var(--muted);
+}
+
+.next {
+  margin-top: 56px;
+  padding-top: 26px;
+  border-top: 1px solid var(--line);
+  display: flex;
+  justify-content: space-between;
+  gap: 16px;
+  flex-wrap: wrap;
+  color: var(--muted);
+}
+
+.next a:hover {
+  color: var(--cyan);
+}
+
+@media (max-width: 760px) {
+  .top {
+    padding: 0 20px;
+  }
+
+  main {
+    padding: 30px 20px 64px;
+  }
+
+  .brand {
+    font-size: 20px;
+  }
+
+  h1 {
+    font-size: 42px;
+  }
+}
diff --git a/cloudflare_worker/public/forgot-password.html b/cloudflare_worker/public/forgot-password.html
index 0d9474b7..201c7985 100644
--- a/cloudflare_worker/public/forgot-password.html
+++ b/cloudflare_worker/public/forgot-password.html
@@ -131,6 +131,7 @@
       .row-links { text-align: center; margin-top: 16px; color: rgba(255, 255, 255, 0.58); font-size: 13px; }
       .row-links a { color: #ffffff; }
     </style>
+    <link rel="stylesheet" href="/auth-page-theme.css" />
     <link rel="stylesheet" href="/site-header.css" />
     <script src="/site-header.js" defer></script>
   </head>
diff --git a/cloudflare_worker/public/login.html b/cloudflare_worker/public/login.html
index 1453dbd4..b637f90b 100644
--- a/cloudflare_worker/public/login.html
+++ b/cloudflare_worker/public/login.html
@@ -159,6 +159,7 @@
       }
       #totp-field { display: none; }
     </style>
+    <link rel="stylesheet" href="/auth-page-theme.css" />
     <link rel="stylesheet" href="/site-header.css" />
     <script src="/site-header.js" defer></script>
   </head>
diff --git a/cloudflare_worker/public/mesh-page-theme.css b/cloudflare_worker/public/mesh-page-theme.css
new file mode 100644
index 00000000..476ec823
--- /dev/null
+++ b/cloudflare_worker/public/mesh-page-theme.css
@@ -0,0 +1,228 @@
+:root {
+  color-scheme: dark;
+  --mesh-page-bg: #090909;
+  --mesh-text: #f5f5f5;
+  --mesh-muted: #a3a3a3;
+  --mesh-border: #313131;
+  --mesh-background:
+    radial-gradient(circle at 18% 10%, rgba(150, 114, 238, 0.2), transparent 28rem),
+    radial-gradient(circle at 84% 8%, rgba(63, 185, 80, 0.14), transparent 30rem),
+    #090909;
+  --mesh-grid:
+    linear-gradient(rgba(255, 255, 255, 0.065) 1px, transparent 1px),
+    linear-gradient(90deg, rgba(255, 255, 255, 0.065) 1px, transparent 1px);
+  --mesh-vignette: linear-gradient(
+    to bottom,
+    rgba(9, 9, 9, 0.92),
+    rgba(9, 9, 9, 0.56) 34%,
+    #090909 78%
+  );
+  --mesh-card-bg: rgba(12, 12, 12, 0.78);
+  --mesh-note-bg: rgba(255, 255, 255, 0.045);
+  --mesh-button-bg: #e8e8e8;
+  --mesh-button-text: #1f1f1f;
+  --mesh-shadow: 0 24px 90px rgba(0, 0, 0, 0.42);
+  --mesh-link: #f5f5f5;
+  --mesh-link-hover: #ffffff;
+  --mesh-ghost-bg: rgba(255, 255, 255, 0.04);
+  --mesh-ghost-hover: rgba(255, 255, 255, 0.075);
+  --mesh-button-hover: #ffffff;
+  --mesh-surface-soft: rgba(255, 255, 255, 0.04);
+}
+
+html.light {
+  color-scheme: light;
+  --mesh-page-bg: #f6f8fb;
+  --mesh-text: #1f2328;
+  --mesh-muted: #656d76;
+  --mesh-border: #d0d7de;
+  --mesh-background: radial-gradient(circle at top, #ffffff, #f6f8fb 70%);
+  --mesh-grid: linear-gradient(rgba(31, 35, 40, 0.08) 1px, transparent 1px), linear-gradient(90deg, rgba(31, 35, 40, 0.08) 1px, transparent 1px);
+  --mesh-vignette: radial-gradient(circle, transparent 35%, rgba(246, 248, 251, 0.92));
+  --mesh-card-bg: #ffffff;
+  --mesh-note-bg: #eef2f7;
+  --mesh-button-bg: #1f2328;
+  --mesh-button-text: #ffffff;
+  --mesh-shadow: 0 18px 50px rgba(31, 35, 40, 0.12);
+  --mesh-link: #0969da;
+  --mesh-link-hover: #0550ae;
+  --mesh-ghost-bg: #ffffff;
+  --mesh-ghost-hover: #eef2f7;
+  --mesh-button-hover: #32383f;
+  --mesh-surface-soft: #f6f8fa;
+  --background: var(--mesh-page-bg);
+  --foreground: var(--mesh-text);
+  --muted: var(--mesh-note-bg);
+  --muted-foreground: var(--mesh-muted);
+  --border: var(--mesh-border);
+  --card: var(--mesh-card-bg);
+  --card-foreground: var(--mesh-text);
+  --primary: var(--mesh-button-bg);
+  --primary-foreground: var(--mesh-button-text);
+}
+
+/*
+ * These shared mappings sit below the pages' unlayered dark styles. The
+ * html.light rules that follow opt each family into the semantic palette.
+ */
+@layer mesh-page-theme {
+  .about-shell, .legal-shell, .page-shell {
+    background: var(--mesh-page-bg);
+    color: var(--mesh-text);
+  }
+
+  .mesh-bg {
+    background: var(--mesh-background);
+  }
+
+  .mesh-grid {
+    background-image: var(--mesh-grid);
+  }
+
+  .mesh-vignette {
+    background: var(--mesh-vignette);
+  }
+
+  .about-card, .legal-card, .status-card {
+    background: var(--mesh-card-bg);
+    border-color: var(--mesh-border);
+    box-shadow: var(--mesh-shadow);
+  }
+
+  .legal-note {
+    background: var(--mesh-note-bg);
+    color: var(--mesh-muted);
+  }
+
+  .button-primary {
+    background: var(--mesh-button-bg);
+    color: var(--mesh-button-text);
+  }
+}
+
+html.light body {
+  background: var(--mesh-page-bg);
+  color: var(--mesh-text);
+}
+
+html.light .about-shell,
+html.light .legal-shell,
+html.light .page-shell {
+  background: var(--mesh-page-bg);
+  color: var(--mesh-text);
+}
+
+html.light .mesh-bg {
+  background: var(--mesh-background);
+}
+
+html.light .mesh-bg::after {
+  content: "";
+  position: absolute;
+  inset: 0;
+  background:
+    radial-gradient(circle at 18% 10%, rgba(130, 80, 223, 0.12), transparent 28rem),
+    radial-gradient(circle at 84% 8%, rgba(31, 136, 61, 0.11), transparent 30rem);
+}
+
+html.light .mesh-grid {
+  background-image: var(--mesh-grid);
+  opacity: 0.46;
+}
+
+html.light .mesh-vignette {
+  background: var(--mesh-vignette);
+}
+
+html.light .about-card,
+html.light .legal-card,
+html.light .status-card {
+  background: var(--mesh-card-bg);
+  border-color: var(--mesh-border);
+  box-shadow: var(--mesh-shadow);
+}
+
+html.light .about-intro-eyebrow,
+html.light .about-intro p:not(.about-intro-eyebrow),
+html.light .about-story p,
+html.light .member-role,
+html.light .legal-content p,
+html.light .legal-content li {
+  color: var(--mesh-muted);
+}
+
+html.light .about-intro h1,
+html.light .about-story h2,
+html.light .about-story strong,
+html.light .member-name,
+html.light .legal-content h2 {
+  color: var(--mesh-text);
+}
+
+html.light .about-story a,
+html.light .legal-content a {
+  color: var(--mesh-link);
+  text-decoration-color: color-mix(in srgb, var(--mesh-link), transparent 55%);
+}
+
+html.light .about-story a:hover,
+html.light .legal-content a:hover {
+  color: var(--mesh-link-hover);
+  text-decoration-color: currentColor;
+}
+
+html.light .pullquote {
+  border-color: var(--mesh-text);
+  color: var(--mesh-text);
+}
+
+html.light .member-photo {
+  background: var(--mesh-surface-soft);
+  border-color: var(--mesh-border);
+}
+
+html.light .legal-note {
+  background: var(--mesh-note-bg);
+  border-color: var(--mesh-border);
+  color: var(--mesh-muted);
+}
+
+html.light .legal-note strong {
+  color: var(--mesh-text);
+}
+
+html.light .legal-content code {
+  border-radius: 0.25rem;
+  background: var(--mesh-note-bg);
+  color: var(--mesh-text);
+  padding: 0.08em 0.28em;
+}
+
+html.light .page-shell .button-primary {
+  border-color: var(--mesh-button-bg);
+  background: var(--mesh-button-bg);
+  color: var(--mesh-button-text);
+}
+
+html.light .page-shell .button-primary:hover {
+  border-color: var(--mesh-button-hover);
+  background: var(--mesh-button-hover);
+  color: var(--mesh-button-text);
+}
+
+html.light .page-shell .button-ghost {
+  border-color: var(--mesh-border);
+  background: var(--mesh-ghost-bg);
+  color: var(--mesh-text);
+}
+
+html.light .page-shell .button-ghost:hover {
+  border-color: #8c959f;
+  background: var(--mesh-ghost-hover);
+  color: var(--mesh-text);
+}
+
+html.light .page-shell .footer-word {
+  color: rgba(31, 35, 40, 0.055);
+  text-shadow: 0 0 70px rgba(130, 80, 223, 0.1);
+}
diff --git a/cloudflare_worker/public/mirror-payouts.html b/cloudflare_worker/public/mirror-payouts.html
index 3b915bf4..d8df8e9f 100644
--- a/cloudflare_worker/public/mirror-payouts.html
+++ b/cloudflare_worker/public/mirror-payouts.html
@@ -100,6 +100,7 @@
         .flow { grid-template-columns: 1fr; }
       }
     </style>
+    <link rel="stylesheet" href="/auth-page-theme.css" />
     <link rel="stylesheet" href="/site-header.css" />
     <script src="/site-header.js" defer></script>
   </head>
diff --git a/cloudflare_worker/public/outreach.html b/cloudflare_worker/public/outreach.html
index f3b58448..1e64c428 100644
--- a/cloudflare_worker/public/outreach.html
+++ b/cloudflare_worker/public/outreach.html
@@ -138,6 +138,7 @@
 
       [hidden] { display: none !important; }
     </style>
+    <link rel="stylesheet" href="/auth-page-theme.css" />
     <link rel="stylesheet" href="/site-header.css" />
     <script src="/site-header.js" defer></script>
   </head>
diff --git a/cloudflare_worker/public/press.html b/cloudflare_worker/public/press.html
index 6d335ee9..23b0cc39 100644
--- a/cloudflare_worker/public/press.html
+++ b/cloudflare_worker/public/press.html
@@ -63,7 +63,7 @@
       .press-hero h1 {
         max-width: 11ch;
         margin: 0;
-        color: #fff;
+        color: var(--foreground);
         font-size: clamp(42px, 8vw, 92px);
         letter-spacing: 0;
         line-height: 0.94;
@@ -134,7 +134,7 @@
 
       .press-panel h2 {
         margin: 0;
-        color: #fff;
+        color: var(--foreground);
         font-size: 19px;
         line-height: 1.2;
       }
@@ -184,7 +184,7 @@
       .press-swatch-chip {
         width: 44px;
         height: 34px;
-        border: 1px solid rgba(255, 255, 255, 0.18);
+        border: 1px solid var(--border);
         border-radius: var(--radius);
         background: var(--swatch);
       }
@@ -216,7 +216,7 @@
         border: 1px solid color-mix(in srgb, var(--warning) 44%, transparent);
         border-radius: 999px;
         background: color-mix(in srgb, var(--warning) 12%, transparent);
-        color: #f5d78e;
+        color: var(--warning);
         font-size: 13px;
         font-weight: 700;
         white-space: nowrap;
diff --git a/cloudflare_worker/public/pricing-theme.css b/cloudflare_worker/public/pricing-theme.css
new file mode 100644
index 00000000..9db5fefb
--- /dev/null
+++ b/cloudflare_worker/public/pricing-theme.css
@@ -0,0 +1,291 @@
+:root {
+  color-scheme: dark;
+  --pricing-page-bg: #090909;
+  --pricing-text: #f5f5f5;
+  --pricing-muted: #a3a3a3;
+  --pricing-border: #313131;
+  --pricing-spotlight: radial-gradient(
+    ellipse at center,
+    rgba(255, 255, 255, 0.08),
+    transparent 64%
+  );
+  --pricing-card-bg: #0d0d0d;
+  --pricing-card-featured: #101010;
+  --pricing-divider: rgba(255, 255, 255, 0.18);
+  --pricing-icon-bg: #242424;
+  --pricing-button-bg: #242424;
+  --pricing-button-text: #ffffff;
+  --pricing-check: rgba(255, 255, 255, 0.82);
+  --pricing-cta-bg: #e8e8e8;
+  --pricing-cta-text: #1f1f1f;
+  --pricing-copy: rgba(255, 255, 255, 0.9);
+  --pricing-quiet: rgba(255, 255, 255, 0.48);
+  --pricing-surface: rgba(255, 255, 255, 0.055);
+  --pricing-card-shadow: 0 1rem 2.8rem rgba(0, 0, 0, 0.72);
+  --pricing-button-hover: #343434;
+  --pricing-cta-hover: #ffffff;
+}
+
+html.light {
+  color-scheme: light;
+  --pricing-page-bg: #f6f8fb;
+  --pricing-text: #1f2328;
+  --pricing-muted: #656d76;
+  --pricing-border: #d0d7de;
+  --pricing-spotlight: radial-gradient(ellipse at center, rgba(9, 105, 218, 0.14), transparent 64%);
+  --pricing-card-bg: #ffffff;
+  --pricing-card-featured: #f0f6fc;
+  --pricing-divider: #d8dee4;
+  --pricing-icon-bg: #24292f;
+  --pricing-button-bg: #24292f;
+  --pricing-button-text: #ffffff;
+  --pricing-check: #1f883d;
+  --pricing-cta-bg: #1f2328;
+  --pricing-cta-text: #ffffff;
+  --pricing-copy: #424a53;
+  --pricing-quiet: #8c959f;
+  --pricing-surface: #eef2f7;
+  --pricing-card-shadow: 0 18px 48px rgba(31, 35, 40, 0.1);
+  --pricing-button-hover: #32383f;
+  --pricing-cta-hover: #32383f;
+  --background: var(--pricing-page-bg);
+  --foreground: var(--pricing-text);
+  --card: var(--pricing-card-bg);
+  --card-foreground: var(--pricing-text);
+  --primary: var(--pricing-cta-bg);
+  --primary-foreground: var(--pricing-cta-text);
+  --muted: var(--pricing-surface);
+  --muted-foreground: var(--pricing-muted);
+  --accent: #0969da;
+  --accent-foreground: #ffffff;
+  --border: var(--pricing-border);
+  --border-subtle: var(--pricing-border);
+  --border-muted: var(--pricing-divider);
+  --ring: #0969da;
+}
+
+/*
+ * Layered semantic mappings leave the current unlayered dark design in
+ * control. The scoped light rules below take over only for html.light.
+ */
+@layer pricing-theme {
+  body {
+    background: var(--pricing-page-bg);
+    color: var(--pricing-text);
+  }
+
+  .pricing-shell {
+    background: var(--pricing-page-bg);
+  }
+
+  .pricing-spotlight {
+    background: var(--pricing-spotlight);
+  }
+
+  .plan-card {
+    background: var(--pricing-card-bg);
+    border-color: var(--pricing-border);
+  }
+
+  .plan-card[data-featured-plan="true"] {
+    background: var(--pricing-card-featured);
+  }
+
+  .plan-card p {
+    color: var(--pricing-muted);
+  }
+
+  .plan-divider {
+    background: var(--pricing-divider);
+  }
+
+  .plan-icon {
+    background: var(--pricing-icon-bg);
+  }
+
+  .plan-button {
+    background: var(--pricing-button-bg);
+    color: var(--pricing-button-text);
+  }
+
+  .feature-check {
+    border-color: var(--pricing-check);
+  }
+
+  .button-primary {
+    background: var(--pricing-cta-bg);
+    color: var(--pricing-cta-text);
+  }
+}
+
+html.light body {
+  background: var(--pricing-page-bg);
+  color: var(--pricing-text);
+}
+
+html.light .pricing-shell {
+  background: var(--pricing-page-bg);
+  color: var(--pricing-text);
+}
+
+html.light .pricing-shell::before {
+  opacity: 0.62;
+  background:
+    linear-gradient(112deg, transparent 0 8%, rgba(9, 105, 218, 0.08) 12%, transparent 20% 100%),
+    linear-gradient(106deg, transparent 0 26%, rgba(31, 136, 61, 0.06) 31%, transparent 40% 100%),
+    linear-gradient(110deg, transparent 0 72%, rgba(130, 80, 223, 0.07) 80%, transparent 89% 100%);
+  filter: blur(13px);
+}
+
+html.light .pricing-shell::after {
+  background:
+    radial-gradient(circle at 49% 18%, transparent 0, rgba(246, 248, 251, 0.24) 24rem, rgba(246, 248, 251, 0.72) 48rem),
+    radial-gradient(circle at 50% 92%, transparent 0, rgba(246, 248, 251, 0.9) 40rem);
+  background-size: auto;
+  opacity: 0.72;
+  mix-blend-mode: normal;
+}
+
+html.light .pricing-grid-overlay {
+  opacity: 0.52;
+  background-image:
+    linear-gradient(rgba(31, 35, 40, 0.08) 1px, transparent 1px),
+    linear-gradient(90deg, rgba(31, 35, 40, 0.08) 1px, transparent 1px);
+}
+
+html.light .pricing-spotlight {
+  background: var(--pricing-spotlight);
+  opacity: 0.8;
+}
+
+html.light .pricing-shell [class~="text-white"] {
+  color: var(--pricing-text);
+}
+
+html.light .pricing-shell [class~="text-white/90"] {
+  color: var(--pricing-copy);
+}
+
+html.light .pricing-shell [class~="text-white/85"],
+html.light .pricing-shell [class~="text-white/78"],
+html.light .pricing-shell [class~="text-white/75"],
+html.light .pricing-shell [class~="text-white/74"],
+html.light .pricing-shell [class~="text-white/72"] {
+  color: var(--pricing-muted);
+}
+
+html.light .pricing-shell [class~="text-white/48"] {
+  color: var(--pricing-quiet);
+}
+
+html.light .price-badge {
+  border-color: var(--pricing-border);
+  background: var(--pricing-surface);
+  color: var(--pricing-copy);
+  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.8);
+}
+
+html.light .pricing-title,
+html.light .plan-card h2,
+html.light .plan-card h3,
+html.light .plan-card strong,
+html.light .pricing-shell main > section:last-child h2 {
+  color: var(--pricing-text);
+}
+
+html.light .pricing-title span:last-child {
+  color: var(--pricing-muted);
+}
+
+html.light .pricing-shell main > section:first-child > p:not(.price-badge),
+html.light .pricing-shell main > section:last-child p {
+  color: var(--pricing-copy);
+}
+
+html.light .billing-toggle {
+  border-color: var(--pricing-border);
+  background: var(--pricing-surface);
+  color: var(--pricing-muted);
+  box-shadow: 0 12px 32px rgba(31, 35, 40, 0.1);
+}
+
+html.light .billing-option[aria-pressed="true"] {
+  background: var(--pricing-button-bg);
+  color: var(--pricing-button-text);
+  box-shadow: 0 2px 8px rgba(31, 35, 40, 0.18);
+}
+
+html.light .plan-card {
+  background: var(--pricing-card-bg);
+  border-color: var(--pricing-border);
+  box-shadow: var(--pricing-card-shadow);
+}
+
+html.light .plan-card::before {
+  background:
+    linear-gradient(90deg, rgba(31, 35, 40, 0.035) 0 1px, transparent 1px 2rem),
+    linear-gradient(180deg, rgba(31, 35, 40, 0.025) 0 1px, transparent 1px 2rem);
+  background-size: 2rem 2rem;
+  opacity: 0.4;
+  mix-blend-mode: normal;
+}
+
+html.light .plan-card[data-featured-plan="true"] {
+  background: var(--pricing-card-featured);
+  border-color: #8c959f;
+  box-shadow:
+    inset 0 1px 0 rgba(255, 255, 255, 0.9),
+    0 20px 54px rgba(9, 105, 218, 0.12);
+}
+
+html.light .plan-card p,
+html.light .plan-card ul {
+  color: var(--pricing-muted);
+}
+
+html.light .plan-divider {
+  background: var(--pricing-divider);
+}
+
+html.light .plan-icon {
+  background: var(--pricing-icon-bg);
+  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.22);
+}
+
+html.light .plan-icon::before {
+  border-color: #ffffff;
+  background: var(--pricing-icon-bg);
+}
+
+html.light .pricing-shell .plan-button,
+html.light .pricing-shell .plan-card[data-featured-plan="true"] .plan-button {
+  background: var(--pricing-button-bg);
+  color: var(--pricing-button-text);
+  box-shadow:
+    inset 0 1px 0 rgba(255, 255, 255, 0.16),
+    0 8px 20px rgba(31, 35, 40, 0.14);
+}
+
+html.light .pricing-shell .plan-button:hover {
+  background: var(--pricing-button-hover);
+  color: var(--pricing-button-text);
+}
+
+html.light .feature-check {
+  border-color: var(--pricing-check);
+}
+
+html.light .feature-check::after {
+  border-left-color: var(--pricing-check);
+  border-bottom-color: var(--pricing-check);
+}
+
+html.light .pricing-shell .button-primary {
+  background: var(--pricing-cta-bg);
+  color: var(--pricing-cta-text);
+}
+
+html.light .pricing-shell .button-primary:hover {
+  background: var(--pricing-cta-hover);
+  color: var(--pricing-cta-text);
+}
diff --git a/cloudflare_worker/public/pricing.html b/cloudflare_worker/public/pricing.html
index 804bc281..d9c5af11 100644
--- a/cloudflare_worker/public/pricing.html
+++ b/cloudflare_worker/public/pricing.html
@@ -715,6 +715,7 @@
         }
       }
     </style>
+    <link rel="stylesheet" href="/pricing-theme.css" />
       <link rel="stylesheet" href="/site-footer.css" />
     <script src="/site-footer.js" defer></script>
     <link rel="stylesheet" href="/site-header.css" />
diff --git a/cloudflare_worker/public/privacy.html b/cloudflare_worker/public/privacy.html
index 33e1fd89..7deacc78 100644
--- a/cloudflare_worker/public/privacy.html
+++ b/cloudflare_worker/public/privacy.html
@@ -93,6 +93,7 @@
       .site-footer-status-dot { background: var(--green); box-shadow: 0 0 18px rgba(63,185,80,.55); }
 
     </style>
+    <link rel="stylesheet" href="/mesh-page-theme.css" />
       <link rel="stylesheet" href="/site-footer.css" />
     <script src="/site-footer.js" defer></script>
     <link rel="stylesheet" href="/site-header.css" />
diff --git a/cloudflare_worker/public/reset-password.html b/cloudflare_worker/public/reset-password.html
index 2a9f29e2..7c347e75 100644
--- a/cloudflare_worker/public/reset-password.html
+++ b/cloudflare_worker/public/reset-password.html
@@ -131,6 +131,7 @@
       .row-links { text-align: center; margin-top: 16px; color: rgba(255, 255, 255, 0.58); font-size: 13px; }
       .row-links a { color: #ffffff; }
     </style>
+    <link rel="stylesheet" href="/auth-page-theme.css" />
     <link rel="stylesheet" href="/site-header.css" />
     <script src="/site-header.js" defer></script>
   </head>
diff --git a/cloudflare_worker/public/security-report.html b/cloudflare_worker/public/security-report.html
index 7bb283be..bf55357d 100644
--- a/cloudflare_worker/public/security-report.html
+++ b/cloudflare_worker/public/security-report.html
@@ -91,6 +91,7 @@
       }
       .row-links a { color: rgba(255,255,255,.78); }
     </style>
+    <link rel="stylesheet" href="/auth-page-theme.css" />
     <link rel="stylesheet" href="/site-header.css" />
     <script src="/site-header.js" defer></script>
   </head>
diff --git a/cloudflare_worker/public/signup.html b/cloudflare_worker/public/signup.html
index 3c3ebbb0..e5c2f4e0 100644
--- a/cloudflare_worker/public/signup.html
+++ b/cloudflare_worker/public/signup.html
@@ -240,6 +240,7 @@
         }
       }
     </style>
+    <link rel="stylesheet" href="/auth-page-theme.css" />
     <link rel="stylesheet" href="/site-header.css" />
     <script src="/site-header.js" defer></script>
   </head>
diff --git a/cloudflare_worker/public/site-footer.css b/cloudflare_worker/public/site-footer.css
index 38ea2a36..e64c35ab 100644
--- a/cloudflare_worker/public/site-footer.css
+++ b/cloudflare_worker/public/site-footer.css
@@ -1,12 +1,33 @@
 .forkmesh-footer {
+  --fm-footer-panel: #000;
+  --fm-footer-fg: #f5f5f5;
+  --fm-footer-muted: #a3a3a3;
+  --fm-footer-border: #3b3b3b;
+  --fm-footer-social-border: var(--border, #313131);
+  --fm-footer-social-hover-border: rgba(232, 232, 232, 0.3);
+  --fm-footer-wordmark: #0d0d0d;
+  --fm-footer-glow: rgba(232, 232, 232, 0.74);
+  --fm-footer-glow-filter: rgba(255, 255, 255, 0.18);
   position: relative;
   isolation: isolate;
   overflow: hidden;
   margin-top: 0;
-  color: var(--foreground, #f5f5f5);
+  color: var(--fm-footer-fg);
   background: transparent;
 }
 
+html.light .forkmesh-footer {
+  --fm-footer-panel: #ffffff;
+  --fm-footer-fg: #1f2328;
+  --fm-footer-muted: #656d76;
+  --fm-footer-border: #d0d7de;
+  --fm-footer-social-border: #d0d7de;
+  --fm-footer-social-hover-border: #8c959f;
+  --fm-footer-wordmark: #eef2f7;
+  --fm-footer-glow: color-mix(in srgb, #1f2328 72%, #ffffff);
+  --fm-footer-glow-filter: rgba(31, 35, 40, 0.18);
+}
+
 .forkmesh-footer-standard {
   margin-top: 4rem;
 }
@@ -36,15 +57,15 @@
 }
 
 .footer-glow-base {
-  color: #0d0d0d;
+  color: var(--fm-footer-wordmark);
 }
 
 .footer-glow-text {
   position: absolute;
   inset: 0;
   pointer-events: none;
-  color: rgba(232, 232, 232, 0.74);
-  filter: drop-shadow(0 0 24px rgba(255, 255, 255, 0.18));
+  color: var(--fm-footer-glow);
+  filter: drop-shadow(0 0 24px var(--fm-footer-glow-filter));
   mask-image: radial-gradient(
     7.5rem 7.5rem at var(--footer-glow-x, 50%) var(--footer-glow-y, 50%),
     black 0%,
@@ -68,8 +89,8 @@
   position: relative;
   z-index: 10;
   min-height: clamp(34rem, 47vw, 42rem);
-  border-top: 1px solid #3b3b3b;
-  background: #000;
+  border-top: 1px solid var(--fm-footer-border);
+  background: var(--fm-footer-panel);
 }
 
 .site-footer-links-grid {
@@ -108,7 +129,7 @@
 .site-footer-brand-copy {
   max-width: 13rem;
   margin: 1.25rem 0 0;
-  color: var(--muted-foreground, var(--muted, #a3a3a3));
+  color: var(--fm-footer-muted);
   font-size: 0.875rem;
   line-height: 1.6;
 }
@@ -126,17 +147,17 @@
   height: 2.25rem;
   align-items: center;
   justify-content: center;
-  border: 1px solid var(--border, #313131);
+  border: 1px solid var(--fm-footer-social-border);
   border-radius: 0.5rem;
-  color: var(--muted-foreground, var(--muted, #a3a3a3));
+  color: var(--fm-footer-muted);
   transition:
     border-color 180ms ease,
     color 180ms ease;
 }
 
 .site-footer-social-link:hover {
-  border-color: rgba(232, 232, 232, 0.3);
-  color: var(--foreground, #f5f5f5);
+  border-color: var(--fm-footer-social-hover-border);
+  color: var(--fm-footer-fg);
   text-decoration: none;
 }
 
@@ -153,15 +174,15 @@
   gap: 0.5rem;
   margin-top: 1.5rem;
   padding: 0.375rem 0.75rem;
-  border: 1px solid #3b3b3b;
+  border: 1px solid var(--fm-footer-border);
   border-radius: 0.5rem;
-  color: var(--muted-foreground, var(--muted, #a3a3a3));
+  color: var(--fm-footer-muted);
   font-size: 0.875rem;
   line-height: 1.25;
 }
 
 .site-footer-status-pill:hover {
-  color: var(--foreground, #f5f5f5);
+  color: var(--fm-footer-fg);
   text-decoration: none;
 }
 
@@ -176,7 +197,7 @@
 
 .site-footer-column-title {
   margin: 0;
-  color: var(--foreground, #f5f5f5);
+  color: var(--fm-footer-fg);
   font-size: 0.875rem;
   font-weight: 600;
 }
@@ -193,14 +214,14 @@
 .site-footer-column-link {
   display: inline-flex;
   min-width: 0;
-  color: var(--muted-foreground, var(--muted, #a3a3a3));
+  color: var(--fm-footer-muted);
   font-size: 0.875rem;
   line-height: 1.35;
   transition: color 180ms ease;
 }
 
 .site-footer-column-link:hover {
-  color: var(--foreground, #f5f5f5);
+  color: var(--fm-footer-fg);
   text-decoration: none;
 }
 
@@ -208,8 +229,8 @@
   max-width: 80rem;
   margin: 0 auto;
   padding: 1.5rem 1.25rem;
-  border-top: 1px solid #3b3b3b;
-  color: var(--muted-foreground, var(--muted, #a3a3a3));
+  border-top: 1px solid var(--fm-footer-border);
+  color: var(--fm-footer-muted);
   font-size: 0.875rem;
   line-height: 1.4;
 }
diff --git a/cloudflare_worker/public/site-header.css b/cloudflare_worker/public/site-header.css
index 3cae3e97..34d5b176 100644
--- a/cloudflare_worker/public/site-header.css
+++ b/cloudflare_worker/public/site-header.css
@@ -1,13 +1,16 @@
 /* Universal site header (see site-header.js), styled to match the dashboard
    chrome: a 56px bar with a bottom border, bordered square controls, and a
    hamburger menu holding every site link grouped and fully expanded.
-   Self-contained: colors ride the host page's CSS variables when it defines
-   them (styles.css pages: --background / --muted / --foreground / --border /
-   --card; chat.html: --bg / --muted / --fg / --border / --surface2) and fall
-   back to the site's dark palette otherwise, so the one component renders
-   correctly on every page it mounts on. */
+   Self-contained: private tokens keep the navigation chrome on the site's
+   dark palette without inheriting colors from the host page. */
 
 .forkmesh-simple-header {
+  --fm-header-bg: #090909;
+  --fm-header-surface: #141416;
+  --fm-header-fg: #f5f5f5;
+  --fm-header-muted: #a3a3a3;
+  --fm-header-border: #313134;
+  --fm-header-accent: #2ea043;
   position: relative;
   z-index: 40;
   display: flex;
@@ -16,16 +19,16 @@
   align-items: center;
   gap: 0.75rem;
   padding: 0 1rem;
-  border-bottom: 1px solid var(--border, #2a2a2a);
-  background: var(--background, var(--bg, #0a0a0a));
-  color: var(--foreground, var(--fg, #f5f5f5));
+  border-bottom: 1px solid var(--fm-header-border);
+  background: var(--fm-header-bg);
+  color: var(--fm-header-fg);
 }
 
 .forkmesh-simple-brand {
   display: inline-flex;
   align-items: center;
   gap: 0.5rem;
-  color: var(--foreground, var(--fg, #f5f5f5));
+  color: var(--fm-header-fg);
   text-decoration: none;
   flex-shrink: 0;
 }
@@ -46,9 +49,9 @@
   display: inline-flex;
   align-items: center;
   padding: 3px 6px;
-  border: 1px solid var(--border, #2a2a2a);
+  border: 1px solid var(--fm-header-border);
   border-radius: 999px;
-  color: var(--muted, var(--muted-foreground, #a3a3a3));
+  color: var(--fm-header-muted);
   font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
   font-size: 10px;
   line-height: 1;
@@ -61,7 +64,7 @@
   overflow: hidden;
   text-overflow: ellipsis;
   white-space: nowrap;
-  color: var(--foreground, var(--fg, #f5f5f5));
+  color: var(--fm-header-fg);
   font-size: 0.875rem;
   font-weight: 600;
 }
@@ -75,7 +78,7 @@
   gap: 4px;
   width: 36px;
   height: 36px;
-  border: 1px solid var(--border, #2a2a2a);
+  border: 1px solid var(--fm-header-border);
   border-radius: 8px;
   background: transparent;
   cursor: pointer;
@@ -87,12 +90,12 @@
   width: 16px;
   height: 2px;
   border-radius: 2px;
-  background: var(--muted, var(--muted-foreground, #a3a3a3));
+  background: var(--fm-header-muted);
 }
 
 .fm-header-burger:hover span,
 .fm-header-burger.is-open span {
-  background: var(--foreground, var(--fg, #f5f5f5));
+  background: var(--fm-header-fg);
 }
 
 /* ── grouped, fully-expanded site menu ── */
@@ -108,9 +111,9 @@
   max-height: min(75vh, 40rem);
   overflow-y: auto;
   padding: 10px;
-  border: 1px solid var(--border, #2a2a2a);
+  border: 1px solid var(--fm-header-border);
   border-radius: 12px;
-  background: var(--card, var(--surface2, #161616));
+  background: var(--fm-header-surface);
   box-shadow: 0 18px 50px rgba(0, 0, 0, 0.4);
 }
 
@@ -129,7 +132,7 @@
 
 .fm-nav-group-title {
   padding: 4px 10px 2px;
-  color: var(--muted, var(--muted-foreground, #a3a3a3));
+  color: var(--fm-header-muted);
   font-size: 10.5px;
   font-weight: 700;
   letter-spacing: 0.09em;
@@ -145,7 +148,7 @@
   border: none;
   border-radius: 7px;
   background: transparent;
-  color: var(--foreground, var(--fg, #e8e8e8));
+  color: var(--fm-header-fg);
   font-size: 0.875rem;
   font-family: inherit;
   text-align: left;
@@ -156,7 +159,7 @@
 }
 
 .forkmesh-simple-header-nav a:hover,
-.fm-header-mobile-account a:hover,
+.fm-header-mobile-account a:not(.fm-header-signup):hover,
 .fm-header-mobile-account button:hover {
   background: rgba(127, 127, 127, 0.14);
   text-decoration: none;
@@ -182,9 +185,9 @@
   flex-direction: column;
   min-width: 180px;
   padding: 6px;
-  border: 1px solid var(--border, #2a2a2a);
+  border: 1px solid var(--fm-header-border);
   border-radius: 10px;
-  background: var(--card, var(--surface2, #161616));
+  background: var(--fm-header-surface);
   box-shadow: 0 18px 50px rgba(0, 0, 0, 0.4);
 }
 
@@ -203,7 +206,7 @@
   border: none;
   border-radius: 7px;
   background: transparent;
-  color: var(--foreground, var(--fg, #e8e8e8));
+  color: var(--fm-header-fg);
   font-size: 0.85rem;
   text-align: left;
   text-decoration: none;
@@ -230,10 +233,10 @@
   gap: 6px;
   height: 36px;
   padding: 0 10px;
-  border: 1px solid var(--border, #2a2a2a);
+  border: 1px solid var(--fm-header-border);
   border-radius: 8px;
   background: transparent;
-  color: var(--muted, var(--muted-foreground, #a3a3a3));
+  color: var(--fm-header-muted);
   font-size: 12px;
   text-decoration: none;
   white-space: nowrap;
@@ -242,7 +245,7 @@
 
 .fm-header-payout:hover {
   background: rgba(127, 127, 127, 0.1);
-  color: var(--foreground, var(--fg, #f5f5f5));
+  color: var(--fm-header-fg);
   text-decoration: none;
 }
 
@@ -260,10 +263,10 @@
   justify-content: center;
   width: 36px;
   height: 36px;
-  border: 1px solid var(--border, #2a2a2a);
+  border: 1px solid var(--fm-header-border);
   border-radius: 8px;
   background: transparent;
-  color: var(--foreground, #f5f5f5);
+  color: var(--fm-header-fg);
   font-size: 15px;
   line-height: 1;
   cursor: pointer;
@@ -271,7 +274,7 @@
 }
 
 .fm-header-theme:hover {
-  border-color: var(--muted, #a3a3a3);
+  border-color: var(--fm-header-muted);
 }
 
 /* ── account area ── */
@@ -283,40 +286,46 @@
 }
 
 .fm-header-login {
-  color: var(--muted, var(--muted-foreground, #a3a3a3));
+  color: var(--fm-header-muted);
   text-decoration: none;
 }
 
-.fm-header-login:hover { color: var(--foreground, var(--fg, #f5f5f5)); }
+.fm-header-login:hover { color: var(--fm-header-fg); }
 
 .fm-header-signup {
   display: inline-flex;
   align-items: center;
   padding: 6px 13px;
-  border-radius: 999px;
-  background: var(--accent, #2ea043);
-  color: #06120a;
+  border-radius: 0.375rem;
+  background: #ffffff;
+  color: #090909;
   font-weight: 700;
   text-decoration: none;
 }
 
 .fm-header-signup:hover { opacity: 0.88; }
 
+.fm-header-mobile-account .fm-header-signup {
+  border-radius: 0.375rem;
+  background: #ffffff;
+  color: #090909;
+}
+
 .fm-header-account-chip {
   display: inline-flex;
   align-items: center;
   gap: 8px;
   padding: 4px 10px 4px 4px;
-  border: 1px solid var(--border, #2a2a2a);
+  border: 1px solid var(--fm-header-border);
   border-radius: 999px;
   background: transparent;
-  color: var(--foreground, var(--fg, #f0f0f0));
+  color: var(--fm-header-fg);
   font-size: 0.85rem;
   font-weight: 600;
   cursor: pointer;
 }
 
-.fm-header-account-chip:hover { border-color: var(--accent, #2ea043); }
+.fm-header-account-chip:hover { border-color: var(--fm-header-accent); }
 
 .fm-header-avatar {
   width: 24px;
diff --git a/cloudflare_worker/public/styles.css b/cloudflare_worker/public/styles.css
index 3b4a423c..4cc02253 100644
--- a/cloudflare_worker/public/styles.css
+++ b/cloudflare_worker/public/styles.css
@@ -1898,11 +1898,15 @@ button.issue-row {
 .network-console,
 .network-ops-card,
 .network-architecture-panel {
-  border: 1px solid rgba(255, 255, 255, 0.08);
+  border: 1px solid var(--border);
   border-radius: 8px;
   background:
-    linear-gradient(180deg, rgba(255, 255, 255, 0.035), rgba(255, 255, 255, 0)),
-    #111212;
+    linear-gradient(
+      180deg,
+      color-mix(in srgb, var(--foreground) 3.5%, transparent),
+      transparent
+    ),
+    var(--card);
   box-shadow: 0 24px 70px rgba(0, 0, 0, 0.22);
 }
 
@@ -1920,10 +1924,28 @@ button.issue-row {
   pointer-events: none;
   opacity: 0.45;
   background:
-    radial-gradient(circle at 82% 18%, rgba(74, 222, 128, 0.16), transparent 26%),
-    radial-gradient(circle at 8% 90%, rgba(163, 113, 247, 0.12), transparent 28%),
-    linear-gradient(90deg, transparent 0 31px, rgba(255, 255, 255, 0.035) 32px, transparent 33px),
-    linear-gradient(180deg, transparent 0 31px, rgba(255, 255, 255, 0.03) 32px, transparent 33px);
+    radial-gradient(
+      circle at 82% 18%,
+      color-mix(in srgb, var(--primary) 16%, transparent),
+      transparent 26%
+    ),
+    radial-gradient(
+      circle at 8% 90%,
+      color-mix(in srgb, var(--purple) 12%, transparent),
+      transparent 28%
+    ),
+    linear-gradient(
+      90deg,
+      transparent 0 31px,
+      color-mix(in srgb, var(--foreground) 3.5%, transparent) 32px,
+      transparent 33px
+    ),
+    linear-gradient(
+      180deg,
+      transparent 0 31px,
+      color-mix(in srgb, var(--foreground) 3%, transparent) 32px,
+      transparent 33px
+    );
   background-size: auto, auto, 64px 64px, 64px 64px;
 }
 
@@ -1957,7 +1979,7 @@ button.issue-row {
   height: 8px;
   border-radius: 999px;
   background: var(--primary);
-  box-shadow: 0 0 18px rgba(74, 222, 128, 0.62);
+  box-shadow: 0 0 18px color-mix(in srgb, var(--primary) 62%, transparent);
 }
 
 .network-console-metrics {
@@ -1969,9 +1991,9 @@ button.issue-row {
 .network-console-metrics article {
   min-width: 0;
   padding: 16px;
-  border: 1px solid rgba(255, 255, 255, 0.07);
+  border: 1px solid color-mix(in srgb, var(--border) 65%, transparent);
   border-radius: 8px;
-  background: rgba(9, 9, 9, 0.66);
+  background: color-mix(in srgb, var(--background) 66%, var(--card));
 }
 
 .network-console-metrics span,
@@ -2011,9 +2033,9 @@ button.issue-row {
   gap: 4px 12px;
   align-items: center;
   padding: 14px 16px;
-  border: 1px solid rgba(255, 255, 255, 0.065);
+  border: 1px solid color-mix(in srgb, var(--border) 60%, transparent);
   border-radius: 8px;
-  background: rgba(255, 255, 255, 0.025);
+  background: color-mix(in srgb, var(--card-soft) 24%, var(--card));
 }
 
 .network-route-step::after {
@@ -2023,7 +2045,7 @@ button.issue-row {
   bottom: -10px;
   width: 1px;
   height: 10px;
-  background: rgba(255, 255, 255, 0.12);
+  background: color-mix(in srgb, var(--border) 72%, transparent);
 }
 
 .network-route-step:last-child::after {
@@ -2031,8 +2053,8 @@ button.issue-row {
 }
 
 .network-route-step.is-relay {
-  border-color: color-mix(in srgb, var(--primary) 42%, rgba(255, 255, 255, 0.08));
-  background: color-mix(in srgb, var(--primary) 10%, rgba(255, 255, 255, 0.025));
+  border-color: color-mix(in srgb, var(--primary) 42%, var(--border));
+  background: color-mix(in srgb, var(--primary) 10%, var(--card));
 }
 
 .network-route-step>span {
@@ -2042,7 +2064,7 @@ button.issue-row {
   display: grid;
   place-items: center;
   border-radius: 999px;
-  background: rgba(255, 255, 255, 0.06);
+  background: color-mix(in srgb, var(--foreground) 6%, var(--card));
   color: var(--foreground);
   font-family: var(--mono);
   font-size: 12px;
@@ -2071,9 +2093,9 @@ button.issue-row {
   gap: 16px;
   margin-top: 14px;
   padding: 15px 16px;
-  border: 1px solid rgba(255, 255, 255, 0.07);
+  border: 1px solid color-mix(in srgb, var(--border) 65%, transparent);
   border-radius: 8px;
-  background: rgba(9, 9, 9, 0.72);
+  background: color-mix(in srgb, var(--background) 72%, var(--card));
 }
 
 .network-console-boundary strong {
@@ -2135,7 +2157,7 @@ button.issue-row {
   justify-content: space-between;
   gap: 16px;
   padding: 18px;
-  border-bottom: 1px solid rgba(255, 255, 255, 0.065);
+  border-bottom: 1px solid color-mix(in srgb, var(--border) 60%, transparent);
 }
 
 .network-card-heading h3 {
@@ -2156,9 +2178,9 @@ button.issue-row {
   align-items: center;
   min-height: 28px;
   padding: 0 10px;
-  border: 1px solid rgba(255, 255, 255, 0.08);
+  border: 1px solid color-mix(in srgb, var(--border) 70%, transparent);
   border-radius: 999px;
-  background: rgba(255, 255, 255, 0.035);
+  background: color-mix(in srgb, var(--card-soft) 32%, var(--card));
   color: var(--muted);
   font-size: 11px;
   font-weight: 700;
@@ -2220,9 +2242,9 @@ button.issue-row {
   grid-template-columns: 38px minmax(0, 1fr);
   gap: 12px;
   padding: 18px;
-  border: 1px solid rgba(255, 255, 255, 0.065);
+  border: 1px solid color-mix(in srgb, var(--border) 60%, transparent);
   border-radius: 8px;
-  background: rgba(255, 255, 255, 0.025);
+  background: color-mix(in srgb, var(--card-soft) 24%, var(--card));
 }
 
 .network-architecture-step>span {
@@ -2231,7 +2253,7 @@ button.issue-row {
   display: grid;
   place-items: center;
   border-radius: 8px;
-  background: color-mix(in srgb, var(--primary) 14%, rgba(255, 255, 255, 0.04));
+  background: color-mix(in srgb, var(--primary) 14%, var(--card));
   color: var(--primary);
   font-family: var(--mono);
   font-size: 12px;
@@ -2265,11 +2287,15 @@ button.issue-row {
   justify-content: space-between;
   gap: 20px;
   padding: 24px;
-  border: 1px solid color-mix(in srgb, var(--purple) 24%, rgba(255, 255, 255, 0.07));
+  border: 1px solid color-mix(in srgb, var(--purple) 24%, var(--border));
   border-radius: 8px;
   background:
-    radial-gradient(circle at 18% 14%, rgba(163, 113, 247, 0.2), transparent 34%),
-    rgba(9, 9, 9, 0.56);
+    radial-gradient(
+      circle at 18% 14%,
+      color-mix(in srgb, var(--purple) 20%, transparent),
+      transparent 34%
+    ),
+    color-mix(in srgb, var(--background) 56%, var(--card));
 }
 
 .network-architecture-boundary h3 {
@@ -2289,7 +2315,7 @@ button.issue-row {
 .network-boundary-list div {
   min-width: 0;
   padding: 13px 0;
-  border-top: 1px solid rgba(255, 255, 255, 0.07);
+  border-top: 1px solid color-mix(in srgb, var(--border) 65%, transparent);
 }
 
 .network-boundary-list strong {
diff --git a/cloudflare_worker/public/terms.html b/cloudflare_worker/public/terms.html
index 01e64e17..5b61b166 100644
--- a/cloudflare_worker/public/terms.html
+++ b/cloudflare_worker/public/terms.html
@@ -93,6 +93,7 @@
       .site-footer-status-dot { background: var(--green); box-shadow: 0 0 18px rgba(63,185,80,.55); }
 
     </style>
+    <link rel="stylesheet" href="/mesh-page-theme.css" />
       <link rel="stylesheet" href="/site-footer.css" />
     <script src="/site-footer.js" defer></script>
     <link rel="stylesheet" href="/site-header.css" />
diff --git a/cloudflare_worker/tests/test_dashboard_landing_migration.py b/cloudflare_worker/tests/test_dashboard_landing_migration.py
index 949c03c7..54997c31 100644
--- a/cloudflare_worker/tests/test_dashboard_landing_migration.py
+++ b/cloudflare_worker/tests/test_dashboard_landing_migration.py
@@ -729,10 +729,17 @@ def test_dashboard_has_scoped_light_dark_appearance_controls():
     static_js = _read(PUBLIC / "static-page.js")
     assert 'localStorage.getItem("forkmesh.dashboard.theme")' in static_js
     assert 'localStorage.setItem("forkmesh.dashboard.theme", chosen)' in static_js
+    site_header_js = _read(PUBLIC / "site-header.js")
+    for theme_key in ("forkmesh.dashboard.theme", "forkmesh.theme"):
+        assert theme_key in site_header_js
     for docs_page in (PUBLIC / "docs.html", PUBLIC / "docs" / "index.html"):
         docs = _read(docs_page)
-        assert 'localStorage.getItem("forkmesh.dashboard.theme")' in docs
-        assert 'localStorage.setItem("forkmesh.dashboard.theme", chosen)' in docs
+        assert 'src="/site-header.js"' in docs
+        assert 'id="theme-toggle"' not in docs
+        assert "function applyTheme" not in docs
+        assert "localStorage" not in docs
+        assert 'localStorage.getItem("forkmesh.dashboard.theme")' not in docs
+        assert 'localStorage.setItem("forkmesh.dashboard.theme", chosen)' not in docs
 
     assert "function setAppearanceModalOpen(open)" not in dashboard_js
     assert "data-appearance-settings-button" not in dashboard_js
diff --git a/cloudflare_worker/tests/test_docs_frontend.py b/cloudflare_worker/tests/test_docs_frontend.py
index d6a382ba..15532376 100644
--- a/cloudflare_worker/tests/test_docs_frontend.py
+++ b/cloudflare_worker/tests/test_docs_frontend.py
@@ -1,6 +1,7 @@
 #!/usr/bin/env python3
 """Static contract tests for the public docs page shell."""
 
+import re
 from pathlib import Path
 
 
@@ -65,25 +66,47 @@ def test_docs_pages_use_home_link_instead_of_github():
 
 
 def test_docs_pages_mount_the_universal_site_header():
-    # The docs' own brand + Home/Contact/Login row was replaced by the shared
-    # session-aware header; the docs toolbar keeps only its search, theme
-    # toggle, and product tabs. A var shim maps the docs light/dark palette
-    # onto the header's variable names so it follows the docs theme toggle.
+    # The shared header is the sole visible theme controller.
+    # The docs toolbar keeps only its search and product tabs beneath it.
     for page in DOCS_PAGES:
         html = _read(page)
 
         assert 'href="/site-header.css"' in html
         assert 'src="/site-header.js"' in html
         assert '<div data-forkmesh-header="simple"></div>' in html
-        assert "--muted: var(--docs-muted);" in html
-        assert "--foreground: var(--docs-fg);" in html
         # The duplicated chrome is gone; docs-specific tools stay.
         assert 'aria-label="Docs header"' not in html
         assert ">Contact Us<" not in html
-        assert 'id="theme-toggle"' in html
+        assert 'id="theme-toggle"' not in html
+        assert "function applyTheme" not in html
+        docs_header = re.search(r"<header\b[^>]*>", html, flags=re.DOTALL)
+        assert docs_header, page
+        class_attribute = re.search(
+            r'class="([^"]*)"',
+            docs_header.group(0),
+        )
+        assert class_attribute, page
+        header_classes = set(class_attribute.group(1).split())
+        assert {"sticky", "top-0", "z-30"} <= header_classes, page
+        assert "z-50" not in header_classes, page
         assert 'id="docs-search"' in html
 
 
+def test_docs_pages_keep_the_desktop_only_single_column_search_row():
+    for page in DOCS_PAGES:
+        html = _read(page)
+
+        assert (
+            'class="mx-auto hidden h-20 max-w-[1540px] '
+            'grid-cols-1 items-center px-5 sm:px-8 md:grid lg:px-12"'
+        ) in html
+        assert ".icon-button" not in html
+        assert "html.dark .moon" not in html
+        assert "html:not(.dark) .sun" not in html
+        assert "Map the docs theme onto the universal header" not in html
+        assert "--foreground: var(--docs-fg)" not in html
+
+
 def test_docs_pages_use_reduced_type_scale():
     for page in DOCS_PAGES:
         html = _read(page)
diff --git a/cloudflare_worker/tests/test_network_architecture_frontend.py b/cloudflare_worker/tests/test_network_architecture_frontend.py
index ade16123..a269e41a 100644
--- a/cloudflare_worker/tests/test_network_architecture_frontend.py
+++ b/cloudflare_worker/tests/test_network_architecture_frontend.py
@@ -1,6 +1,7 @@
 #!/usr/bin/env python3
 """Static contracts for the Network command-center page."""
 
+import re
 from pathlib import Path
 
 
@@ -77,6 +78,63 @@ def test_network_command_center_styles_exist():
         assert marker in STYLES
 
 
+def test_network_command_center_surfaces_use_theme_tokens():
+    network_block = STYLES[
+        STYLES.index("/* ===== Network command center ===== */"):
+        STYLES.index("/* ===== Doc content ===== */")
+    ]
+
+    assert "#111212" not in network_block
+    assert "rgba(9, 9, 9" not in network_block
+    assert "var(--card)" in network_block
+    assert "var(--card-soft)" in network_block
+    assert "var(--border)" in network_block
+    assert "color-mix(in srgb, var(--foreground)" in network_block
+
+
+def test_network_command_center_decorative_accents_use_theme_tokens():
+    network_block = STYLES[
+        STYLES.index("/* ===== Network command center ===== */"):
+        STYLES.index("/* ===== Doc content ===== */")
+    ]
+
+    assert "rgba(74, 222, 128" not in network_block
+    assert "rgba(163, 113, 247" not in network_block
+    assert "color-mix(in srgb, var(--primary)" in network_block
+    assert "color-mix(in srgb, var(--purple)" in network_block
+
+
+def test_network_decorative_accent_selectors_keep_private_theme_tokens():
+    expected = {
+        ".network-console::before": (
+            "color-mix(in srgb, var(--primary) 16%, transparent)",
+            "color-mix(in srgb, var(--purple) 12%, transparent)",
+        ),
+        ".network-console-led": (
+            "background: var(--primary);",
+            "color-mix(in srgb, var(--primary) 62%, transparent)",
+        ),
+        ".network-route-step.is-relay": (
+            "color-mix(in srgb, var(--primary) 42%, var(--border))",
+            "color-mix(in srgb, var(--primary) 10%, var(--card))",
+        ),
+        ".network-architecture-boundary": (
+            "color-mix(in srgb, var(--purple) 24%, var(--border))",
+            "color-mix(in srgb, var(--purple) 20%, transparent)",
+        ),
+    }
+
+    for selector, markers in expected.items():
+        match = re.search(
+            rf"(?ms)^{re.escape(selector)}\s*\{{(?P<body>[^}}]*)\}}",
+            STYLES,
+        )
+        assert match, f"styles.css missing {selector} rule"
+        body = match.group("body")
+        for marker in markers:
+            assert marker in body, f"{selector} must use {marker}"
+
+
 def test_network_stats_bindings_update_all_metric_instances():
     for marker in (
         'setText("#network-clients, [data-network-clients]"',
diff --git a/cloudflare_worker/tests/test_press_page_frontend.py b/cloudflare_worker/tests/test_press_page_frontend.py
index a74e4195..e42fcba8 100644
--- a/cloudflare_worker/tests/test_press_page_frontend.py
+++ b/cloudflare_worker/tests/test_press_page_frontend.py
@@ -67,8 +67,19 @@ def test_press_page_uses_solid_dark_background_without_gradients():
     assert "background: var(--background);" in html
 
 
+def test_press_page_headings_swatches_and_status_follow_theme_tokens():
+    html = _press_html()
+
+    assert "color: var(--foreground);" in html
+    assert "border: 1px solid var(--border);" in html
+    assert "color: var(--warning);" in html
+    assert "color: #fff;" not in html
+    assert "color: #f5d78e;" not in html
+
+
 if __name__ == "__main__":
     test_press_page_exists_with_press_kit_content()
     test_press_page_uses_shared_simple_header_and_standard_footer()
     test_press_page_is_discoverable_from_shared_footer()
     test_press_page_uses_solid_dark_background_without_gradients()
+    test_press_page_headings_swatches_and_status_follow_theme_tokens()
diff --git a/cloudflare_worker/tests/test_public_light_theme.py b/cloudflare_worker/tests/test_public_light_theme.py
new file mode 100644
index 00000000..63d974b3
--- /dev/null
+++ b/cloudflare_worker/tests/test_public_light_theme.py
@@ -0,0 +1,561 @@
+import re
+from pathlib import Path
+
+
+PUBLIC = Path(__file__).resolve().parents[1] / "public"
+AUTH_PAGES = tuple(
+    PUBLIC / name
+    for name in (
+        "login.html",
+        "signup.html",
+        "forgot-password.html",
+        "reset-password.html",
+        "mirror-payouts.html",
+        "outreach.html",
+        "security-report.html",
+    )
+)
+MESH_PAGES = tuple(
+    PUBLIC / name
+    for name in (
+        "about.html",
+        "terms.html",
+        "privacy.html",
+        "404.html",
+    )
+)
+FEATURE_POSTS = tuple(
+    page
+    for page in sorted((PUBLIC / "blog").glob("*/index.html"))
+    if page.parent.name != "introducing-forkmesh"
+)
+PUBLIC_THEME_PAGES = tuple(
+    page
+    for page in sorted(PUBLIC.rglob("*.html"))
+    if page != PUBLIC / "index.html" and "dashboard" not in page.parts
+)
+
+FAMILY_THEME_STYLESHEETS = (
+    "/auth-page-theme.css",
+    "/mesh-page-theme.css",
+    "/pricing-theme.css",
+    "/feature-post.css",
+)
+
+FAMILY_THEME_CONTRACTS = {
+    "auth-page-theme.css": {
+        "light": {
+            "color-scheme": "light",
+            "--auth-page-bg": "#f6f8fb",
+            "--auth-glow": (
+                "radial-gradient(circle at top, rgba(31, 136, 61, 0.14), "
+                "transparent 32rem)"
+            ),
+            "--auth-card-bg": "#ffffff",
+            "--auth-border": "#d0d7de",
+            "--auth-text": "#1f2328",
+            "--auth-text-secondary": "#424a53",
+            "--auth-text-quiet": "#656d76",
+            "--auth-field-bg": "#ffffff",
+            "--auth-focus": "#1f883d",
+            "--auth-action-bg": "#1f2328",
+            "--auth-action-text": "#ffffff",
+        },
+        "rules": {
+            "body": {
+                "background": "var(--auth-page-bg)",
+                "color": "var(--auth-text)",
+            },
+            ".auth-shell": {"background-image": "var(--auth-glow)"},
+            ".auth-card": {
+                "background": "var(--auth-card-bg)",
+                "border-color": "var(--auth-border)",
+            },
+            ".auth-sub": {"color": "var(--auth-text-secondary)"},
+            ".hint": {"color": "var(--auth-text-quiet)"},
+            "input, textarea, select": {
+                "background": "var(--auth-field-bg)",
+                "border-color": "var(--auth-border)",
+                "color": "var(--auth-text)",
+            },
+            "input:focus, textarea:focus, select:focus": {
+                "border-color": "var(--auth-focus)"
+            },
+            ".auth-button-primary": {
+                "background": "var(--auth-action-bg)",
+                "color": "var(--auth-action-text)",
+            },
+        },
+    },
+    "mesh-page-theme.css": {
+        "light": {
+            "color-scheme": "light",
+            "--mesh-page-bg": "#f6f8fb",
+            "--mesh-text": "#1f2328",
+            "--mesh-muted": "#656d76",
+            "--mesh-border": "#d0d7de",
+            "--mesh-background": (
+                "radial-gradient(circle at top, #ffffff, #f6f8fb 70%)"
+            ),
+            "--mesh-grid": (
+                "linear-gradient(rgba(31, 35, 40, 0.08) 1px, transparent 1px), "
+                "linear-gradient(90deg, rgba(31, 35, 40, 0.08) 1px, "
+                "transparent 1px)"
+            ),
+            "--mesh-vignette": (
+                "radial-gradient(circle, transparent 35%, "
+                "rgba(246, 248, 251, 0.92))"
+            ),
+            "--mesh-card-bg": "#ffffff",
+            "--mesh-note-bg": "#eef2f7",
+            "--mesh-button-bg": "#1f2328",
+            "--mesh-button-text": "#ffffff",
+            "--mesh-shadow": "0 18px 50px rgba(31, 35, 40, 0.12)",
+        },
+        "rules": {
+            ".about-shell, .legal-shell, .page-shell": {
+                "background": "var(--mesh-page-bg)",
+                "color": "var(--mesh-text)",
+            },
+            ".mesh-bg": {"background": "var(--mesh-background)"},
+            ".mesh-grid": {"background-image": "var(--mesh-grid)"},
+            ".mesh-vignette": {"background": "var(--mesh-vignette)"},
+            ".about-card, .legal-card, .status-card": {
+                "background": "var(--mesh-card-bg)",
+                "border-color": "var(--mesh-border)",
+                "box-shadow": "var(--mesh-shadow)",
+            },
+            ".legal-note": {
+                "background": "var(--mesh-note-bg)",
+                "color": "var(--mesh-muted)",
+            },
+            ".button-primary": {
+                "background": "var(--mesh-button-bg)",
+                "color": "var(--mesh-button-text)",
+            },
+        },
+    },
+    "pricing-theme.css": {
+        "light": {
+            "color-scheme": "light",
+            "--pricing-page-bg": "#f6f8fb",
+            "--pricing-text": "#1f2328",
+            "--pricing-muted": "#656d76",
+            "--pricing-border": "#d0d7de",
+            "--pricing-spotlight": (
+                "radial-gradient(ellipse at center, rgba(9, 105, 218, 0.14), "
+                "transparent 64%)"
+            ),
+            "--pricing-card-bg": "#ffffff",
+            "--pricing-card-featured": "#f0f6fc",
+            "--pricing-divider": "#d8dee4",
+            "--pricing-icon-bg": "#24292f",
+            "--pricing-button-bg": "#24292f",
+            "--pricing-button-text": "#ffffff",
+            "--pricing-check": "#1f883d",
+            "--pricing-cta-bg": "#1f2328",
+            "--pricing-cta-text": "#ffffff",
+        },
+        "rules": {
+            "body": {
+                "background": "var(--pricing-page-bg)",
+                "color": "var(--pricing-text)",
+            },
+            ".pricing-shell": {"background": "var(--pricing-page-bg)"},
+            ".pricing-spotlight": {"background": "var(--pricing-spotlight)"},
+            ".plan-card": {
+                "background": "var(--pricing-card-bg)",
+                "border-color": "var(--pricing-border)",
+            },
+            '.plan-card[data-featured-plan="true"]': {
+                "background": "var(--pricing-card-featured)"
+            },
+            ".plan-card p": {"color": "var(--pricing-muted)"},
+            ".plan-divider": {"background": "var(--pricing-divider)"},
+            ".plan-icon": {"background": "var(--pricing-icon-bg)"},
+            ".plan-button": {
+                "background": "var(--pricing-button-bg)",
+                "color": "var(--pricing-button-text)",
+            },
+            ".feature-check": {"border-color": "var(--pricing-check)"},
+            ".button-primary": {
+                "background": "var(--pricing-cta-bg)",
+                "color": "var(--pricing-cta-text)",
+            },
+        },
+    },
+    "feature-post.css": {
+        "light": {
+            "color-scheme": "light",
+            "--bg": "#f6f8fb",
+            "--paper": "#ffffff",
+            "--line": "#d0d7de",
+            "--soft": "#eef2f7",
+            "--text": "#1f2328",
+            "--muted": "#656d76",
+            "--green": "#1a7f37",
+            "--cyan": "#0969da",
+            "--paragraph": "#24292f",
+            "--note": "#f6f8fa",
+            "--image-placeholder": "#eaeef2",
+        },
+        "rules": {
+            "body": {
+                "background": "var(--bg)",
+                "color": "var(--text)",
+            },
+            ".shell": {
+                "background": "var(--paper)",
+                "border-color": "var(--line)",
+            },
+            ".pill": {
+                "background": "var(--soft)",
+                "border-color": "var(--line)",
+            },
+            ".hero-img": {
+                "background": "var(--image-placeholder)",
+                "border-color": "var(--line)",
+            },
+            ".body p": {"color": "var(--paragraph)"},
+            ".body .note": {
+                "background": "var(--note)",
+                "color": "var(--muted)",
+            },
+            ".body a": {"color": "var(--green)"},
+            ".back:hover": {"color": "var(--cyan)"},
+        },
+    },
+}
+
+
+def read(path: Path) -> str:
+    return path.read_text(encoding="utf-8")
+
+
+def css_rule(css: str, selector: str, source: str) -> str:
+    selector_pattern = r"\s+".join(
+        re.escape(part) for part in selector.split()
+    )
+    match = re.search(
+        rf"(?ms)^[ \t]*{selector_pattern}[ \t]*\{{(?P<body>[^}}]*)\}}",
+        css,
+    )
+    assert match, f"{source} missing {selector} rule"
+    return match.group("body")
+
+
+def css_declarations(css: str, selector: str, source: str) -> dict[str, str]:
+    return {
+        name: re.sub(r"\s+", " ", value.strip())
+        for name, value in re.findall(
+            r"(?m)^[ \t]*([\w-]+)\s*:\s*([^;]+);",
+            css_rule(css, selector, source),
+        )
+    }
+
+
+def assert_css_declarations(
+    css: str,
+    selector: str,
+    expected: dict[str, str],
+    source: str,
+) -> None:
+    declarations = css_declarations(css, selector, source)
+    for name, value in expected.items():
+        normalized_value = re.sub(r"\s+", " ", value.strip())
+        assert declarations.get(name) == normalized_value, (
+            f"{source} {selector} must set {name}: {value}; "
+            f"found {declarations.get(name)!r}"
+        )
+
+
+def inline_styles(html: str) -> str:
+    return "\n".join(
+        re.findall(r"<style\b[^>]*>(.*?)</style>", html, re.IGNORECASE | re.DOTALL)
+    )
+
+
+def recognized_light_theme_strategies(page: Path, html: str) -> tuple[str, ...]:
+    strategies = []
+    inline_css = inline_styles(html)
+
+    for href in FAMILY_THEME_STYLESHEETS:
+        if f'href="{href}"' in html:
+            strategies.append(href)
+
+    styles_link = re.search(
+        r'<link\b[^>]*\bhref="/?styles\.css"[^>]*>',
+        html,
+        flags=re.IGNORECASE,
+    )
+    dark_root = re.search(
+        r":root\s*\{[^{}]*color-scheme\s*:\s*dark\b",
+        inline_css,
+        flags=re.IGNORECASE | re.DOTALL,
+    )
+    dark_body = re.search(
+        r"body\s*\{[^{}]*background(?:-color)?\s*:\s*"
+        r"(?:#0{3,6}\b|#090909\b|black\b)",
+        inline_css,
+        flags=re.IGNORECASE | re.DOTALL,
+    )
+    inline_light_override = re.search(
+        r"html(?:\.light|:not\(\.dark\))[^{}]*\{[^{}]*"
+        r"(?:--[\w-]+\s*:|background(?:-color)?\s*:)",
+        inline_css,
+        flags=re.IGNORECASE | re.DOTALL,
+    )
+    unhandled_dark_style = (dark_root or dark_body) and not inline_light_override
+    if styles_link and not unhandled_dark_style:
+        strategies.append("styles.css")
+
+    docs_light = re.search(
+        r":root\s*\{(?P<body>[^{}]*)\}",
+        inline_css,
+        flags=re.IGNORECASE | re.DOTALL,
+    )
+    docs_dark = re.search(
+        r"html\.dark\s*\{(?P<body>[^{}]*)\}",
+        inline_css,
+        flags=re.IGNORECASE | re.DOTALL,
+    )
+    if (
+        page in (PUBLIC / "docs.html", PUBLIC / "docs" / "index.html")
+        and docs_light
+        and "--docs-bg:" in docs_light.group("body")
+        and re.search(r"color-scheme\s*:\s*light\b", docs_light.group("body"))
+        and docs_dark
+        and "--docs-bg:" in docs_dark.group("body")
+        and re.search(r"color-scheme\s*:\s*dark\b", docs_dark.group("body"))
+    ):
+        strategies.append("docs-inline-palette")
+
+    chat_light = re.search(
+        r"html:not\(\.dark\)\s*\{(?P<body>[^{}]*)\}",
+        inline_css,
+        flags=re.IGNORECASE | re.DOTALL,
+    )
+    if (
+        page == PUBLIC / "chat.html"
+        and chat_light
+        and re.search(r"--bg\s*:\s*#f6f8fb\s*;", chat_light.group("body"))
+        and re.search(r"--fg\s*:\s*#1f2328\s*;", chat_light.group("body"))
+    ):
+        strategies.append("chat-inline-palette")
+
+    return tuple(strategies)
+
+
+def assert_stylesheet_after_inline_styles(page: Path, href: str) -> str:
+    html = read(page)
+    marker = f'href="{href}"'
+    assert marker in html, page
+    assert html.rfind(marker) > html.rfind("</style>"), (
+        f"{page} must load {href} after its final inline style"
+    )
+    return html
+
+
+def test_every_public_page_uses_the_shared_theme_control():
+    assert PUBLIC_THEME_PAGES
+    for page in PUBLIC_THEME_PAGES:
+        html = read(page)
+        assert '<meta name="color-scheme" content="light dark"' in html, page
+        assert 'href="/site-header.css"' in html, page
+        assert 'src="/site-header.js"' in html, page
+        assert '<div data-forkmesh-header="simple"></div>' in html, page
+        assert 'id="theme-toggle"' not in html, page
+
+
+def test_every_public_page_has_a_recognized_light_theme_strategy():
+    for page in PUBLIC_THEME_PAGES:
+        strategies = recognized_light_theme_strategies(page, read(page))
+        assert strategies, (
+            f"{page} must use tokenized styles.css without an unhandled dark "
+            "inline style, a family theme stylesheet, the Docs palette, or the "
+            "Chat light palette"
+        )
+
+
+def test_hardcoded_dark_page_is_not_a_recognized_light_theme_strategy():
+    html = """
+      <link rel="stylesheet" href="/styles.css">
+      <style>
+        :root { color-scheme: dark; }
+        body { background: #000; color: #fff; }
+      </style>
+    """
+
+    assert not recognized_light_theme_strategies(PUBLIC / "new-page.html", html)
+
+
+def test_page_families_load_complete_light_theme_styles():
+    for page in AUTH_PAGES:
+        assert_stylesheet_after_inline_styles(page, "/auth-page-theme.css")
+    for page in MESH_PAGES:
+        assert_stylesheet_after_inline_styles(page, "/mesh-page-theme.css")
+    assert_stylesheet_after_inline_styles(
+        PUBLIC / "pricing.html",
+        "/pricing-theme.css",
+    )
+    assert len(FEATURE_POSTS) == 72
+    for page in FEATURE_POSTS:
+        html = assert_stylesheet_after_inline_styles(page, "/feature-post.css")
+        legacy_root = re.search(
+            r":root\s*\{[^{}]*color-scheme\s*:\s*dark\b",
+            html,
+            flags=re.IGNORECASE | re.DOTALL,
+        )
+        legacy_background = re.search(
+            r"--bg\s*:\s*#030303\b",
+            html,
+            flags=re.IGNORECASE,
+        )
+        assert legacy_root is None, page
+        assert legacy_background is None, page
+
+
+def test_shared_family_styles_define_light_palettes():
+    for name, contract in FAMILY_THEME_CONTRACTS.items():
+        stylesheet = PUBLIC / name
+        assert stylesheet.is_file(), f"missing shared family stylesheet: {name}"
+        css = read(stylesheet)
+        assert_css_declarations(css, "html.light", contract["light"], name)
+        for selector, expected in contract["rules"].items():
+            assert_css_declarations(css, selector, expected, name)
+
+
+def test_shared_footer_defines_dark_and_light_semantic_palettes():
+    footer_css = read(PUBLIC / "site-footer.css")
+
+    for token in (
+        "--fm-footer-panel",
+        "--fm-footer-fg",
+        "--fm-footer-muted",
+        "--fm-footer-border",
+        "--fm-footer-wordmark",
+        "--fm-footer-glow",
+    ):
+        assert token in footer_css
+
+    light_override = css_rule(footer_css, "html.light .forkmesh-footer", "site-footer.css")
+    expected_light = {
+        "--fm-footer-panel": "#ffffff",
+        "--fm-footer-fg": "#1f2328",
+        "--fm-footer-muted": "#656d76",
+        "--fm-footer-border": "#d0d7de",
+        "--fm-footer-wordmark": "#eef2f7",
+        "--fm-footer-glow": "color-mix(in srgb, #1f2328 72%, #ffffff)",
+    }
+    assert_css_declarations(
+        footer_css,
+        "html.light .forkmesh-footer",
+        expected_light,
+        "site-footer.css",
+    )
+    for token in (
+        "--fm-footer-panel",
+        "--fm-footer-fg",
+        "--fm-footer-muted",
+        "--fm-footer-border",
+        "--fm-footer-wordmark",
+        "--fm-footer-glow",
+    ):
+        assert token in light_override
+
+    for selector in (
+        ".site-footer-panel",
+        ".site-footer-brand-copy",
+        ".site-footer-column-title",
+        ".site-footer-column-link",
+        ".site-footer-copyright",
+    ):
+        assert "var(--fm-footer-" in css_rule(footer_css, selector, "site-footer.css")
+
+    assert_css_declarations(
+        footer_css,
+        ".forkmesh-footer",
+        {"--fm-footer-social-border": "var(--border, #313131)"},
+        "site-footer.css",
+    )
+    assert_css_declarations(
+        footer_css,
+        "html.light .forkmesh-footer",
+        {"--fm-footer-social-border": "#d0d7de"},
+        "site-footer.css",
+    )
+    assert_css_declarations(
+        footer_css,
+        ".site-footer-social-link",
+        {"border": "1px solid var(--fm-footer-social-border)"},
+        "site-footer.css",
+    )
+    assert_css_declarations(
+        footer_css,
+        ".forkmesh-footer",
+        {
+            "--fm-footer-social-hover-border": (
+                "rgba(232, 232, 232, 0.3)"
+            )
+        },
+        "site-footer.css",
+    )
+    assert_css_declarations(
+        footer_css,
+        "html.light .forkmesh-footer",
+        {"--fm-footer-social-hover-border": "#8c959f"},
+        "site-footer.css",
+    )
+    assert_css_declarations(
+        footer_css,
+        ".site-footer-social-link:hover",
+        {
+            "border-color": "var(--fm-footer-social-hover-border)",
+            "color": "var(--fm-footer-fg)",
+        },
+        "site-footer.css",
+    )
+    assert_css_declarations(
+        footer_css,
+        ".forkmesh-footer",
+        {"--fm-footer-glow-filter": "rgba(255, 255, 255, 0.18)"},
+        "site-footer.css",
+    )
+    assert_css_declarations(
+        footer_css,
+        "html.light .forkmesh-footer",
+        {"--fm-footer-glow-filter": "rgba(31, 35, 40, 0.18)"},
+        "site-footer.css",
+    )
+    assert_css_declarations(
+        footer_css,
+        ".footer-glow-text",
+        {
+            "filter": (
+                "drop-shadow(0 0 24px var(--fm-footer-glow-filter))"
+            )
+        },
+        "site-footer.css",
+    )
+
+    assert_css_declarations(
+        footer_css,
+        ".site-footer-status-pill",
+        {
+            "border": "1px solid var(--fm-footer-border)",
+            "color": "var(--fm-footer-muted)",
+        },
+        "site-footer.css",
+    )
+    assert_css_declarations(
+        footer_css,
+        ".site-footer-status-dot",
+        {"background": "var(--green, var(--accent-bright, #28c878))"},
+        "site-footer.css",
+    )
+
+
+def test_landing_page_remains_explicitly_dark():
+    assert '<html lang="en" class="dark h-full scroll-smooth">' in read(
+        PUBLIC / "index.html"
+    )
diff --git a/cloudflare_worker/tests/test_shared_header_frontend.py b/cloudflare_worker/tests/test_shared_header_frontend.py
index b5f34350..4a2ce80c 100644
--- a/cloudflare_worker/tests/test_shared_header_frontend.py
+++ b/cloudflare_worker/tests/test_shared_header_frontend.py
@@ -1,6 +1,7 @@
 #!/usr/bin/env python3
 """Static contracts for the shared simple page header."""
 
+import re
 from pathlib import Path
 
 
@@ -20,6 +21,34 @@ def _read(path: Path) -> str:
     return path.read_text(encoding="utf-8")
 
 
+def _css_declarations(css: str, selector: str) -> dict[str, str]:
+    match = re.search(
+        rf"(?ms)^[ \t]*{re.escape(selector)}[ \t]*\{{(?P<body>[^}}]*)\}}",
+        css,
+    )
+    assert match, f"missing CSS rule for {selector}"
+    return {
+        name: value.strip()
+        for name, value in re.findall(
+            r"(?m)^[ \t]*([\w-]+)\s*:\s*([^;]+);",
+            match.group("body"),
+        )
+    }
+
+
+def _assert_declarations(
+    css: str,
+    selector: str,
+    expected: dict[str, str],
+) -> None:
+    declarations = _css_declarations(css, selector)
+    for name, value in expected.items():
+        assert declarations.get(name) == value, (
+            f"{selector} must set {name}: {value}; "
+            f"found {declarations.get(name)!r}"
+        )
+
+
 def test_shared_simple_header_mounts_on_requested_pages():
     for page in SIMPLE_HEADER_PAGES:
         html = _read(page)
@@ -74,6 +103,7 @@ UNIVERSAL_HEADER_PAGES = SIMPLE_HEADER_PAGES + (
     PUBLIC / "forgot-password.html",
     PUBLIC / "reset-password.html",
     PUBLIC / "mirror-payouts.html",
+    PUBLIC / "outreach.html",
     PUBLIC / "security-report.html",
     PUBLIC / "404.html",
     PUBLIC / "docs.html",
@@ -138,6 +168,105 @@ def test_universal_header_organizes_all_pages():
     assert 'src="/assets/sol.png"' in js
 
 
+def test_universal_header_uses_private_fixed_palette():
+    css = _read(PUBLIC / "site-header.css")
+
+    _assert_declarations(
+        css,
+        ".forkmesh-simple-header",
+        {
+            "--fm-header-bg": "#090909",
+            "--fm-header-surface": "#141416",
+            "--fm-header-fg": "#f5f5f5",
+            "--fm-header-muted": "#a3a3a3",
+            "--fm-header-border": "#313134",
+            "--fm-header-accent": "#2ea043",
+            "background": "var(--fm-header-bg)",
+            "color": "var(--fm-header-fg)",
+            "border-bottom": "1px solid var(--fm-header-border)",
+        },
+    )
+    for selector in (".fm-header-mobile", ".fm-header-dropdown"):
+        _assert_declarations(
+            css,
+            selector,
+            {
+                "background": "var(--fm-header-surface)",
+                "border": "1px solid var(--fm-header-border)",
+            },
+        )
+    _assert_declarations(
+        css,
+        ".forkmesh-simple-brand",
+        {"color": "var(--fm-header-fg)"},
+    )
+    _assert_declarations(
+        css,
+        ".fm-nav-group-title",
+        {"color": "var(--fm-header-muted)"},
+    )
+    _assert_declarations(
+        css,
+        ".fm-header-account-chip:hover",
+        {"border-color": "var(--fm-header-accent)"},
+    )
+
+
+def test_universal_header_does_not_reference_host_page_palette():
+    css = _read(PUBLIC / "site-header.css")
+    host_tokens = (
+        "--background",
+        "--bg",
+        "--foreground",
+        "--fg",
+        "--card",
+        "--surface2",
+        "--muted",
+        "--muted-foreground",
+        "--border",
+        "--accent",
+    )
+    inherited_references = tuple(
+        token
+        for token in host_tokens
+        if re.search(
+            rf"var\(\s*{re.escape(token)}(?:\s*,|\s*\))",
+            css,
+        )
+    )
+
+    assert not inherited_references, (
+        f"site-header.css still inherits host page tokens: {inherited_references}"
+    )
+
+
+def test_universal_header_uses_canonical_theme_glyphs():
+    js = _read(PUBLIC / "site-header.js")
+
+    assert 'button.textContent = light ? "☾" : "☀";' in js
+    assert '>☀</button>' in js
+
+
+def test_universal_header_signup_is_a_white_rounded_rectangle():
+    css = _read(PUBLIC / "site-header.css")
+    expected = {
+        "background": "#ffffff",
+        "color": "#090909",
+        "border-radius": "0.375rem",
+    }
+
+    _assert_declarations(css, ".fm-header-signup", expected)
+    _assert_declarations(
+        css,
+        ".fm-header-mobile-account .fm-header-signup",
+        {
+            "background": "#ffffff",
+            "color": "#090909",
+            "border-radius": "0.375rem",
+        },
+    )
+
+
 def test_blog_posts_mount_universal_header():
     # Blog posts used to carry their own mini header; they now mount the same
     # universal header as the rest of the site.
