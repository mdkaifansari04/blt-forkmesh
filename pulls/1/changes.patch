diff --git a/cloudflare_worker/public/404.html b/cloudflare_worker/public/404.html
index 69cd4421..48240e92 100644
--- a/cloudflare_worker/public/404.html
+++ b/cloudflare_worker/public/404.html
@@ -4,40 +4,6 @@
     <meta charset="utf-8" />
     <script src="/posthog.js"></script>
     <meta name="viewport" content="width=device-width, initial-scale=1" />
-    <script>
-      // Clean repository URLs like /owner/repo (and /owner/repo/tree|blob/...)
-      // are not static files, so Cloudflare serves this 404 page for them. Bounce
-      // them to the dashboard's repo view from here — i.e. AFTER static asset
-      // resolution. A /:owner/:repo rule in _redirects would instead hijack real
-      // two-segment assets such as /assets/logo.png and /favicon/site.webmanifest,
-      // because Cloudflare always applies _redirects before serving a matching
-      // static file. Runs before render to avoid flashing the 404 shell.
-      (function () {
-        var parts = window.location.pathname.split("/").filter(Boolean);
-        // First-segment names that are real site sections, never repo owners.
-        var reserved = [
-          "api", "assets", "favicon", "dashboard", "docs", "blogs", "blog",
-          "login", "signup", "network", "desktop", "features", "about",
-          "careers", "changelog", "privacy", "terms",
-        ];
-        var featureTabs = ["commits", "releases", "issues", "pulls", "discussions", "mirrors"];
-        var looksLikeRepo =
-          parts.length >= 2 &&
-          reserved.indexOf(parts[0].toLowerCase()) === -1 &&
-          (parts.length === 2 ||
-            (parts.length >= 3 && (parts[2] === "tree" || parts[2] === "blob" || featureTabs.indexOf(parts[2]) >= 0)));
-        if (looksLikeRepo) {
-          // Bounce to /dashboard/owner/repo (or with /tree|/blob|/<tab> path)
-          // so the SPA sees the full path and can restore the correct view.
-          var dashboardPath = "/dashboard/" + encodeURIComponent(parts[0]) + "/" + encodeURIComponent(parts[1]);
-          if (parts.length > 2) {
-            // Preserve /tree/..., /blob/..., or /issues (etc.) suffix
-            dashboardPath += "/" + parts.slice(2).map(function(p) { return encodeURIComponent(p); }).join("/");
-          }
-          window.location.replace(dashboardPath + window.location.hash);
-        }
-      })();
-    </script>
     <title>Page not found - ForkMesh</title>
     <meta
       name="description"
diff --git a/cloudflare_worker/public/_redirects b/cloudflare_worker/public/_redirects
index b24003e8..4df0c15c 100644
--- a/cloudflare_worker/public/_redirects
+++ b/cloudflare_worker/public/_redirects
@@ -1,23 +1,20 @@
 / /index.html 200
 /dashboard /dashboard/index.html 200
-# /dashboard/:owner/:repo (and deeper tree|blob paths) are client-side routes
-# handled in the Worker (run_worker_first = /dashboard/*) to avoid the
-# Cloudflare loop-detection false-positive on /dashboard/* → /dashboard/index.html.
-/dashboard.html /dashboard 308
-/noisy-background.avif /noisy-background.avif 200
-/dashboard.js /dashboard.js 200
-/dashboard-chat.js /dashboard-chat.js 200
+# /dashboard/:owner/:repo and clean repo shortcuts (/owner/repo, /owner/repo/tree,
+# /owner/repo/blob, and feature tabs) are handled in the Worker. Static asset
+# prefixes stay asset-first through run_worker_first exceptions in wrangler.toml.
 /blogs /blog.html 200
 /login /login.html 200
 /signup /signup.html 200
+/forgot-password /forgot-password.html 200
+/reset-password /reset-password.html 200
 /mirror-payouts /mirror-payouts.html 200
+/security-report /security-report.html 200
 /docs /docs/index.html 200
-/docs/ /docs/index.html 200
-/docs.html /docs/ 308
+/docs/ /docs 308
 /blog /blogs 308
-/blog.html /blogs 308
-/network /network/index.html 200
-/network/ /network/index.html 200
+/network /network.html 200
+/network/ /network 308
 /chat /chat.html 200
 /desktop /desktop.html 200
 /about /about.html 200
@@ -27,9 +24,7 @@
 /privacy /privacy.html 200
 /terms /terms.html 200
 /status /status.html 200
-# Repo shortcut URLs (/owner/repo and /owner/repo/tree|blob/...) are deliberately
-# NOT redirected here. A /:owner/:repo rule also matches real two-segment static
-# assets like /assets/logo.png and /favicon/site.webmanifest, and Cloudflare
-# always applies _redirects BEFORE serving a matching static file, so those
-# assets would 308 instead of loading. The shortcut is handled client-side in
-# public/404.html, which only runs after asset resolution (a genuine 404).
+# Repo shortcut URLs are deliberately NOT redirected here. A /:owner/:repo rule
+# also matches real two-segment static assets like /assets/logo.png and
+# /favicon/site.webmanifest because Cloudflare applies _redirects before serving
+# matching static files.
diff --git a/cloudflare_worker/public/blog/index.html b/cloudflare_worker/public/blog/index.html
index 06ea1490..dfe8a11a 100644
--- a/cloudflare_worker/public/blog/index.html
+++ b/cloudflare_worker/public/blog/index.html
@@ -48,11 +48,11 @@
         </a>
         <nav class="global-nav" aria-label="Global">
           <a href="/dashboard">Repositories</a>
-          <a href="/network/">Network</a>
+          <a href="/network">Network</a>
           <a href="/docs/">Docs</a>
           <a href="/status">Status</a>
           <a href="/blogs">Blog</a>
-          <a href="/changelog.html">Changelog</a>
+          <a href="/changelog">Changelog</a>
           <a href="/chat">Chat</a>
           <a class="nav-cta" href="/signup">Sign Up / Log In</a>
         </nav>
diff --git a/cloudflare_worker/public/blog/introducing-forkmesh/index.html b/cloudflare_worker/public/blog/introducing-forkmesh/index.html
index 7c049dec..d025fadf 100644
--- a/cloudflare_worker/public/blog/introducing-forkmesh/index.html
+++ b/cloudflare_worker/public/blog/introducing-forkmesh/index.html
@@ -72,11 +72,11 @@
         </a>
         <nav class="global-nav" aria-label="Global">
           <a href="/dashboard">Repositories</a>
-          <a href="/network/">Network</a>
+          <a href="/network">Network</a>
           <a href="/docs/">Docs</a>
           <a href="/status">Status</a>
           <a href="/blogs">Blog</a>
-          <a href="/changelog.html">Changelog</a>
+          <a href="/changelog">Changelog</a>
           <a href="/chat">Chat</a>
           <a class="nav-cta" href="/signup">Sign Up / Log In</a>
         </nav>
diff --git a/cloudflare_worker/public/careers.html b/cloudflare_worker/public/careers.html
index 59f70c9a..1cf5a1ca 100644
--- a/cloudflare_worker/public/careers.html
+++ b/cloudflare_worker/public/careers.html
@@ -48,38 +48,13 @@
       }
       .careers-cta { display: flex; gap: 12px; flex-wrap: wrap; margin-top: 32px; }
     </style>
+      <link rel="stylesheet" href="/site-footer.css" />
+    <script src="/site-footer.js" defer></script>
+    <link rel="stylesheet" href="/site-header.css" />
+    <script src="/site-header.js" defer></script>
   </head>
   <body>
-    <header class="site-header">
-      <div class="header-inner">
-        <a class="brand" href="/" aria-label="ForkMesh home">
-          <img class="brand-mark" src="/assets/logo.png" alt="" aria-hidden="true" />
-          <span>ForkMesh</span>
-        </a>
-        <nav class="global-nav" aria-label="Global">
-          <a href="/dashboard">Repositories</a>
-          <a href="/network/">Network</a>
-          <a href="/docs/">Docs</a>
-          <a href="/status">Status</a>
-          <a href="/blogs">Blog</a>
-          <a href="/changelog.html">Changelog</a>
-          <a href="/chat">Chat</a>
-          <a class="nav-cta" href="/signup">Sign Up / Log In</a>
-        </nav>
-        <div class="header-actions">
-          <div class="clients-pill" aria-live="polite">
-            <span class="clients-relay-icon" id="clients-dot" aria-hidden="true"><svg viewBox="0 0 16 16" fill="currentColor" xmlns="http://www.w3.org/2000/svg"><path d="M8.75 8.582v5.668a.75.75 0 0 1-1.5 0V8.582a1.75 1.75 0 1 1 1.5 0Zm3.983-7.125a.75.75 0 0 1 1.06.026A7.976 7.976 0 0 1 16 7c0 2.139-.84 4.083-2.207 5.517a.75.75 0 1 1-1.086-1.034A6.474 6.474 0 0 0 14.5 7a6.474 6.474 0 0 0-1.793-4.483.75.75 0 0 1 .026-1.06Zm-9.466 0c.3.286.312.76.026 1.06A6.474 6.474 0 0 0 1.5 7a6.47 6.47 0 0 0 1.793 4.483.75.75 0 0 1-1.086 1.034A7.973 7.973 0 0 1 0 7c0-2.139.84-4.083 2.207-5.517a.75.75 0 0 1 1.06-.026Zm8.556 2.321A4.988 4.988 0 0 1 13 7a4.988 4.988 0 0 1-1.177 3.222.75.75 0 1 1-1.146-.967A3.487 3.487 0 0 0 11.5 7c0-.86-.309-1.645-.823-2.255a.75.75 0 0 1 1.146-.967Zm-6.492.958A3.48 3.48 0 0 0 4.5 7a3.48 3.48 0 0 0 .823 2.255.75.75 0 0 1-1.146.967A4.981 4.981 0 0 1 3 7a4.982 4.982 0 0 1 1.188-3.236.75.75 0 1 1 1.143.972Z"/></svg></span>
-            <span id="clients-count">Checking…</span>
-          </div>
-          <button id="theme-toggle" class="icon-button" type="button" aria-label="Toggle theme">
-            <svg class="moon" viewBox="0 0 24 24" aria-hidden="true"><path d="M21 12.8A8.5 8.5 0 1 1 11.2 3 6.7 6.7 0 0 0 21 12.8Z"></path></svg>
-            <svg class="sun" viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="4"></circle><path d="M12 2v2M12 20v2M4.93 4.93l1.42 1.42M17.65 17.65l1.42 1.42M2 12h2M20 12h2M4.93 19.07l1.42-1.42M17.65 6.35l1.42-1.42"></path></svg>
-          </button>
-          <a class="nav-admin" data-admin-link href="#" hidden>Admin</a>
-          <a class="install-link" href="/#install-banner">Install</a>
-        </div>
-      </div>
-    </header>
+    <div data-forkmesh-header="simple"></div>
 
     <main class="careers-wrap">
       <p class="eyebrow">Careers</p>
@@ -156,50 +131,10 @@
 
       <div class="careers-cta">
         <a class="primary-button" href="mailto:founders@forkmesh.com?subject=Marketing%20Lead%20application">Apply by email</a>
-        <a class="secondary-button" href="/about.html">Read our story</a>
+        <a class="secondary-button" href="/about">Read our story</a>
       </div>
     </main>
 
-    <footer class="site-footer">
-      <div class="footer-inner">
-        <div>
-          <div class="footer-brand">
-            <img src="/assets/logo.png" alt="" aria-hidden="true" />
-            <span>ForkMesh</span>
-          </div>
-          <p class="footer-tagline">
-            Code hosting that lives on the network — distributed, signed, and
-            impossible to quietly take away.
-          </p>
-        </div>
-        <nav class="footer-links" aria-label="Footer">
-          <div class="footer-col">
-            <h3>Product</h3>
-            <a href="/dashboard">Repositories</a>
-            <a href="/network/">Network</a>
-            <a href="/docs/">Docs</a>
-            <a href="/status">Status</a>
-            <a href="/changelog.html">Changelog</a>
-          </div>
-          <div class="footer-col">
-            <h3>Company</h3>
-            <a href="/about.html">About</a>
-            <a href="/careers.html">Careers</a>
-            <a href="/blogs">Blog</a>
-            <a href="/signup">Sign Up / Log In</a>
-          </div>
-          <div class="footer-col">
-            <h3>Get in touch</h3>
-            <a href="mailto:founders@forkmesh.com">founders@forkmesh.com</a>
-            <a href="/privacy.html">Privacy</a>
-            <a href="/terms.html">Terms</a>
-          </div>
-        </nav>
-      </div>
-      <div class="footer-bottom">
-        Questions or feedback? Email the founders at
-        <a href="mailto:founders@forkmesh.com">founders@forkmesh.com</a>.
-      </div>
-    </footer>
+    <div data-forkmesh-footer="standard"></div>
   </body>
 </html>
diff --git a/cloudflare_worker/public/changelog.html b/cloudflare_worker/public/changelog.html
index 603abc91..72dc8160 100644
--- a/cloudflare_worker/public/changelog.html
+++ b/cloudflare_worker/public/changelog.html
@@ -55,38 +55,13 @@
       .log-foot { margin-top: 44px; color: var(--muted); }
       .log-foot a { font-weight: 600; }
     </style>
+      <link rel="stylesheet" href="/site-footer.css" />
+    <script src="/site-footer.js" defer></script>
+    <link rel="stylesheet" href="/site-header.css" />
+    <script src="/site-header.js" defer></script>
   </head>
   <body>
-    <header class="site-header">
-      <div class="header-inner">
-        <a class="brand" href="/" aria-label="ForkMesh home">
-          <img class="brand-mark" src="/assets/logo.png" alt="" aria-hidden="true" />
-          <span>ForkMesh</span>
-        </a>
-        <nav class="global-nav" aria-label="Global">
-          <a href="/dashboard">Repositories</a>
-          <a href="/network/">Network</a>
-          <a href="/docs/">Docs</a>
-          <a href="/status">Status</a>
-          <a href="/blogs">Blog</a>
-          <a href="/changelog.html">Changelog</a>
-          <a href="/chat">Chat</a>
-          <a class="nav-cta" href="/signup">Sign Up / Log In</a>
-        </nav>
-        <div class="header-actions">
-          <div class="clients-pill" aria-live="polite">
-            <span class="clients-relay-icon" id="clients-dot" aria-hidden="true"><svg viewBox="0 0 16 16" fill="currentColor" xmlns="http://www.w3.org/2000/svg"><path d="M8.75 8.582v5.668a.75.75 0 0 1-1.5 0V8.582a1.75 1.75 0 1 1 1.5 0Zm3.983-7.125a.75.75 0 0 1 1.06.026A7.976 7.976 0 0 1 16 7c0 2.139-.84 4.083-2.207 5.517a.75.75 0 1 1-1.086-1.034A6.474 6.474 0 0 0 14.5 7a6.474 6.474 0 0 0-1.793-4.483.75.75 0 0 1 .026-1.06Zm-9.466 0c.3.286.312.76.026 1.06A6.474 6.474 0 0 0 1.5 7a6.47 6.47 0 0 0 1.793 4.483.75.75 0 0 1-1.086 1.034A7.973 7.973 0 0 1 0 7c0-2.139.84-4.083 2.207-5.517a.75.75 0 0 1 1.06-.026Zm8.556 2.321A4.988 4.988 0 0 1 13 7a4.988 4.988 0 0 1-1.177 3.222.75.75 0 1 1-1.146-.967A3.487 3.487 0 0 0 11.5 7c0-.86-.309-1.645-.823-2.255a.75.75 0 0 1 1.146-.967Zm-6.492.958A3.48 3.48 0 0 0 4.5 7a3.48 3.48 0 0 0 .823 2.255.75.75 0 0 1-1.146.967A4.981 4.981 0 0 1 3 7a4.982 4.982 0 0 1 1.188-3.236.75.75 0 1 1 1.143.972Z"/></svg></span>
-            <span id="clients-count">Checking…</span>
-          </div>
-          <button id="theme-toggle" class="icon-button" type="button" aria-label="Toggle theme">
-            <svg class="moon" viewBox="0 0 24 24" aria-hidden="true"><path d="M21 12.8A8.5 8.5 0 1 1 11.2 3 6.7 6.7 0 0 0 21 12.8Z"></path></svg>
-            <svg class="sun" viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="4"></circle><path d="M12 2v2M12 20v2M4.93 4.93l1.42 1.42M17.65 17.65l1.42 1.42M2 12h2M20 12h2M4.93 19.07l1.42-1.42M17.65 6.35l1.42-1.42"></path></svg>
-          </button>
-          <a class="nav-admin" data-admin-link href="#" hidden>Admin</a>
-          <a class="install-link" href="/#install-banner">Install</a>
-        </div>
-      </div>
-    </header>
+    <div data-forkmesh-header="simple"></div>
 
     <main class="log-wrap">
       <p class="eyebrow">Changelog</p>
@@ -191,46 +166,6 @@
       </p>
     </main>
 
-    <footer class="site-footer">
-      <div class="footer-inner">
-        <div>
-          <div class="footer-brand">
-            <img src="/assets/logo.png" alt="" aria-hidden="true" />
-            <span>ForkMesh</span>
-          </div>
-          <p class="footer-tagline">
-            Code hosting that lives on the network — distributed, signed, and
-            impossible to quietly take away.
-          </p>
-        </div>
-        <nav class="footer-links" aria-label="Footer">
-          <div class="footer-col">
-            <h3>Product</h3>
-            <a href="/dashboard">Repositories</a>
-            <a href="/network/">Network</a>
-            <a href="/docs/">Docs</a>
-            <a href="/status">Status</a>
-            <a href="/changelog.html">Changelog</a>
-          </div>
-          <div class="footer-col">
-            <h3>Company</h3>
-            <a href="/about.html">About</a>
-            <a href="/careers.html">Careers</a>
-            <a href="/blogs">Blog</a>
-            <a href="/signup">Sign Up / Log In</a>
-          </div>
-          <div class="footer-col">
-            <h3>Get in touch</h3>
-            <a href="mailto:founders@forkmesh.com">founders@forkmesh.com</a>
-            <a href="/privacy.html">Privacy</a>
-            <a href="/terms.html">Terms</a>
-          </div>
-        </nav>
-      </div>
-      <div class="footer-bottom">
-        Questions or feedback? Email the founders at
-        <a href="mailto:founders@forkmesh.com">founders@forkmesh.com</a>.
-      </div>
-    </footer>
+    <div data-forkmesh-footer="standard"></div>
   </body>
 </html>
diff --git a/cloudflare_worker/public/desktop.html b/cloudflare_worker/public/desktop.html
index e8bab40f..042e11d4 100644
--- a/cloudflare_worker/public/desktop.html
+++ b/cloudflare_worker/public/desktop.html
@@ -94,38 +94,13 @@
       }
       .install-section a { color: var(--accent-bright); }
     </style>
+      <link rel="stylesheet" href="/site-footer.css" />
+    <script src="/site-footer.js" defer></script>
+    <link rel="stylesheet" href="/site-header.css" />
+    <script src="/site-header.js" defer></script>
   </head>
   <body>
-    <header class="site-header">
-      <div class="header-inner">
-        <a class="brand" href="/" aria-label="ForkMesh home">
-          <img class="brand-mark" src="/assets/logo.png" alt="" aria-hidden="true" />
-          <span>ForkMesh</span>
-        </a>
-        <nav class="global-nav" aria-label="Global">
-          <a href="/dashboard">Repositories</a>
-          <a href="/network/">Network</a>
-          <a href="/docs/">Docs</a>
-          <a href="/status">Status</a>
-          <a href="/blogs">Blog</a>
-          <a href="/changelog.html">Changelog</a>
-          <a href="/chat">Chat</a>
-          <a class="nav-cta" href="/signup">Sign Up / Log In</a>
-        </nav>
-        <div class="header-actions">
-          <div class="clients-pill" aria-live="polite">
-            <span class="clients-relay-icon" id="clients-dot" aria-hidden="true"><svg viewBox="0 0 16 16" fill="currentColor" xmlns="http://www.w3.org/2000/svg"><path d="M8.75 8.582v5.668a.75.75 0 0 1-1.5 0V8.582a1.75 1.75 0 1 1 1.5 0Zm3.983-7.125a.75.75 0 0 1 1.06.026A7.976 7.976 0 0 1 16 7c0 2.139-.84 4.083-2.207 5.517a.75.75 0 1 1-1.086-1.034A6.474 6.474 0 0 0 14.5 7a6.474 6.474 0 0 0-1.793-4.483.75.75 0 0 1 .026-1.06Zm-9.466 0c.3.286.312.76.026 1.06A6.474 6.474 0 0 0 1.5 7a6.47 6.47 0 0 0 1.793 4.483.75.75 0 0 1-1.086 1.034A7.973 7.973 0 0 1 0 7c0-2.139.84-4.083 2.207-5.517a.75.75 0 0 1 1.06-.026Zm8.556 2.321A4.988 4.988 0 0 1 13 7a4.988 4.988 0 0 1-1.177 3.222.75.75 0 1 1-1.146-.967A3.487 3.487 0 0 0 11.5 7c0-.86-.309-1.645-.823-2.255a.75.75 0 0 1 1.146-.967Zm-6.492.958A3.48 3.48 0 0 0 4.5 7a3.48 3.48 0 0 0 .823 2.255.75.75 0 0 1-1.146.967A4.981 4.981 0 0 1 3 7a4.982 4.982 0 0 1 1.188-3.236.75.75 0 1 1 1.143.972Z"/></svg></span>
-            <span id="clients-count">Checking…</span>
-          </div>
-          <button id="theme-toggle" class="icon-button" type="button" aria-label="Toggle theme">
-            <svg class="moon" viewBox="0 0 24 24" aria-hidden="true"><path d="M21 12.8A8.5 8.5 0 1 1 11.2 3 6.7 6.7 0 0 0 21 12.8Z"></path></svg>
-            <svg class="sun" viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="4"></circle><path d="M12 2v2M12 20v2M4.93 4.93l1.42 1.42M17.65 17.65l1.42 1.42M2 12h2M20 12h2M4.93 19.07l1.42-1.42M17.65 6.35l1.42-1.42"></path></svg>
-          </button>
-          <a class="nav-admin" data-admin-link href="#" hidden>Admin</a>
-          <a class="install-link" href="#top">Install</a>
-        </div>
-      </div>
-    </header>
+    <div data-forkmesh-header="simple"></div>
 
     <main class="install-wrap" id="top">
       <p class="eyebrow">Download</p>
@@ -278,47 +253,7 @@
       </section>
     </main>
 
-    <footer class="site-footer">
-      <div class="footer-inner">
-        <div>
-          <div class="footer-brand">
-            <img src="/assets/logo.png" alt="" aria-hidden="true" />
-            <span>ForkMesh</span>
-          </div>
-          <p class="footer-tagline">
-            Code hosting that lives on the network — distributed, signed, and
-            impossible to quietly take away.
-          </p>
-        </div>
-        <nav class="footer-links" aria-label="Footer">
-          <div class="footer-col">
-            <h3>Product</h3>
-            <a href="/dashboard">Repositories</a>
-            <a href="/network/">Network</a>
-            <a href="/docs/">Docs</a>
-            <a href="/status">Status</a>
-            <a href="/changelog.html">Changelog</a>
-          </div>
-          <div class="footer-col">
-            <h3>Company</h3>
-            <a href="/about.html">About</a>
-            <a href="/careers.html">Careers</a>
-            <a href="/blogs">Blog</a>
-            <a href="/signup">Sign Up / Log In</a>
-          </div>
-          <div class="footer-col">
-            <h3>Get in touch</h3>
-            <a href="mailto:founders@forkmesh.com">founders@forkmesh.com</a>
-            <a href="/privacy.html">Privacy</a>
-            <a href="/terms.html">Terms</a>
-          </div>
-        </nav>
-      </div>
-      <div class="footer-bottom">
-        Questions or feedback? Email the founders at
-        <a href="mailto:founders@forkmesh.com">founders@forkmesh.com</a>.
-      </div>
-    </footer>
+    <div data-forkmesh-footer="standard"></div>
 
     <script>
       // Copy buttons for the per-OS command blocks. The site-wide static-page.js
diff --git a/cloudflare_worker/public/docs.html b/cloudflare_worker/public/docs.html
index d7e936ec..0e6cfcb2 100644
--- a/cloudflare_worker/public/docs.html
+++ b/cloudflare_worker/public/docs.html
@@ -376,13 +376,13 @@
             aria-label="Docs header"
           >
             <a class="hover:text-[var(--docs-fg)]" href="/">Home</a>
-            <a class="hover:text-[var(--docs-fg)]" href="/about.html"
+            <a class="hover:text-[var(--docs-fg)]" href="/about"
               >Contact Us</a
             >
           </nav>
           <a
             class="hidden h-11 items-center gap-2 rounded-2xl bg-[var(--docs-button)] px-5 text-[0.92rem] font-bold text-[var(--docs-button-fg)] no-underline shadow-sm sm:inline-flex"
-            href="/login.html"
+            href="/login"
           >
             Login
             <span aria-hidden="true">›</span>
@@ -1092,7 +1092,7 @@ sha256hex(title + NUL + base + NUL + head + NUL + patch)</code></pre>
               </a>
               <a
                 class="docs-footer-icon"
-                href="/network/"
+                href="/network"
                 aria-label="ForkMesh network"
               >
                 <svg viewBox="0 0 24 24" aria-hidden="true">
diff --git a/cloudflare_worker/public/docs/index.html b/cloudflare_worker/public/docs/index.html
index d7e936ec..0e6cfcb2 100644
--- a/cloudflare_worker/public/docs/index.html
+++ b/cloudflare_worker/public/docs/index.html
@@ -376,13 +376,13 @@
             aria-label="Docs header"
           >
             <a class="hover:text-[var(--docs-fg)]" href="/">Home</a>
-            <a class="hover:text-[var(--docs-fg)]" href="/about.html"
+            <a class="hover:text-[var(--docs-fg)]" href="/about"
               >Contact Us</a
             >
           </nav>
           <a
             class="hidden h-11 items-center gap-2 rounded-2xl bg-[var(--docs-button)] px-5 text-[0.92rem] font-bold text-[var(--docs-button-fg)] no-underline shadow-sm sm:inline-flex"
-            href="/login.html"
+            href="/login"
           >
             Login
             <span aria-hidden="true">›</span>
@@ -1092,7 +1092,7 @@ sha256hex(title + NUL + base + NUL + head + NUL + patch)</code></pre>
               </a>
               <a
                 class="docs-footer-icon"
-                href="/network/"
+                href="/network"
                 aria-label="ForkMesh network"
               >
                 <svg viewBox="0 0 24 24" aria-hidden="true">
diff --git a/cloudflare_worker/public/features.html b/cloudflare_worker/public/features.html
index 280a2a33..28d0f958 100644
--- a/cloudflare_worker/public/features.html
+++ b/cloudflare_worker/public/features.html
@@ -118,41 +118,15 @@
       .features-outro p { color: var(--muted); max-width: 56ch; margin: 0 auto 20px; line-height: 1.6; }
       .features-outro .features-cta { justify-content: center; margin: 0; }
     </style>
+      <link rel="stylesheet" href="/site-footer.css" />
+    <script src="/site-footer.js" defer></script>
+    <link rel="stylesheet" href="/site-header.css" />
+    <script src="/site-header.js" defer></script>
   </head>
   <body>
-    <header class="site-header">
-      <div class="header-inner">
-        <a class="brand" href="/" aria-label="ForkMesh home">
-          <img class="brand-mark" src="/assets/logo.png" alt="" aria-hidden="true" />
-          <span>ForkMesh</span>
-        </a>
-        <nav class="global-nav" aria-label="Global">
-          <a href="/dashboard">Repositories</a>
-          <a href="/network/">Network</a>
-          <a href="/docs/">Docs</a>
-          <a href="/status">Status</a>
-          <a href="/blogs">Blog</a>
-          <a href="/changelog.html">Changelog</a>
-          <a href="/chat">Chat</a>
-          <a class="nav-cta" href="/signup">Sign Up / Log In</a>
-        </nav>
-        <div class="header-actions">
-          <div class="clients-pill" aria-live="polite">
-            <span class="clients-relay-icon" id="clients-dot" aria-hidden="true"><svg viewBox="0 0 16 16" fill="currentColor" xmlns="http://www.w3.org/2000/svg"><path d="M8.75 8.582v5.668a.75.75 0 0 1-1.5 0V8.582a1.75 1.75 0 1 1 1.5 0Zm3.983-7.125a.75.75 0 0 1 1.06.026A7.976 7.976 0 0 1 16 7c0 2.139-.84 4.083-2.207 5.517a.75.75 0 1 1-1.086-1.034A6.474 6.474 0 0 0 14.5 7a6.474 6.474 0 0 0-1.793-4.483.75.75 0 0 1 .026-1.06Zm-9.466 0c.3.286.312.76.026 1.06A6.474 6.474 0 0 0 1.5 7a6.47 6.47 0 0 0 1.793 4.483.75.75 0 0 1-1.086 1.034A7.973 7.973 0 0 1 0 7c0-2.139.84-4.083 2.207-5.517a.75.75 0 0 1 1.06-.026Zm8.556 2.321A4.988 4.988 0 0 1 13 7a4.988 4.988 0 0 1-1.177 3.222.75.75 0 1 1-1.146-.967A3.487 3.487 0 0 0 11.5 7c0-.86-.309-1.645-.823-2.255a.75.75 0 0 1 1.146-.967Zm-6.492.958A3.48 3.48 0 0 0 4.5 7a3.48 3.48 0 0 0 .823 2.255.75.75 0 0 1-1.146.967A4.981 4.981 0 0 1 3 7a4.982 4.982 0 0 1 1.188-3.236.75.75 0 1 1 1.143.972Z"/></svg></span>
-            <span id="clients-count">Checking…</span>
-          </div>
-          <button id="theme-toggle" class="icon-button" type="button" aria-label="Toggle theme">
-            <svg class="moon" viewBox="0 0 24 24" aria-hidden="true"><path d="M21 12.8A8.5 8.5 0 1 1 11.2 3 6.7 6.7 0 0 0 21 12.8Z"></path></svg>
-            <svg class="sun" viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="4"></circle><path d="M12 2v2M12 20v2M4.93 4.93l1.42 1.42M17.65 17.65l1.42 1.42M2 12h2M20 12h2M4.93 19.07l1.42-1.42M17.65 6.35l1.42-1.42"></path></svg>
-          </button>
-          <a class="nav-admin" data-admin-link href="#" hidden>Admin</a>
-          <a class="install-link" href="/#install-banner">Install</a>
-        </div>
-      </div>
-    </header>
+    <div data-forkmesh-header="simple"></div>
 
     <main class="features-wrap">
-      <p class="eyebrow">The full tour</p>
       <h1>Every feature in the mesh.</h1>
       <p class="features-lede">
         ForkMesh is a complete code forge that lives on machines you and your
@@ -161,7 +135,7 @@
         the apps that tie it together. All of it works today.
       </p>
       <div class="features-cta">
-        <a class="primary-button" href="/signup.html">Join the mesh</a>
+        <a class="primary-button" href="/signup">Join the mesh</a>
         <a class="secondary-button" href="/docs/">Read the docs</a>
       </div>
 
@@ -835,53 +809,12 @@
           — or staying friendly.
         </p>
         <div class="features-cta">
-          <a class="primary-button" href="/signup.html">Join the mesh</a>
+          <a class="primary-button" href="/signup">Join the mesh</a>
           <a class="secondary-button" href="/dashboard">Browse repositories</a>
         </div>
       </div>
     </main>
 
-    <footer class="site-footer">
-      <div class="footer-inner">
-        <div>
-          <div class="footer-brand">
-            <img src="/assets/logo.png" alt="" aria-hidden="true" />
-            <span>ForkMesh</span>
-          </div>
-          <p class="footer-tagline">
-            Code hosting that lives on the network — distributed, signed, and
-            impossible to quietly take away.
-          </p>
-        </div>
-        <nav class="footer-links" aria-label="Footer">
-          <div class="footer-col">
-            <h3>Product</h3>
-            <a href="/features">Features</a>
-            <a href="/dashboard">Repositories</a>
-            <a href="/network/">Network</a>
-            <a href="/docs/">Docs</a>
-            <a href="/status">Status</a>
-            <a href="/changelog.html">Changelog</a>
-          </div>
-          <div class="footer-col">
-            <h3>Company</h3>
-            <a href="/about.html">About</a>
-            <a href="/careers.html">Careers</a>
-            <a href="/blogs">Blog</a>
-            <a href="/signup">Sign Up / Log In</a>
-          </div>
-          <div class="footer-col">
-            <h3>Get in touch</h3>
-            <a href="mailto:founders@forkmesh.com">founders@forkmesh.com</a>
-            <a href="/privacy.html">Privacy</a>
-            <a href="/terms.html">Terms</a>
-          </div>
-        </nav>
-      </div>
-      <div class="footer-bottom">
-        Questions or feedback? Email the founders at
-        <a href="mailto:founders@forkmesh.com">founders@forkmesh.com</a>.
-      </div>
-    </footer>
+    <div data-forkmesh-footer="standard"></div>
   </body>
 </html>
diff --git a/cloudflare_worker/public/forgot-password.html b/cloudflare_worker/public/forgot-password.html
index a2349ad3..51459061 100644
--- a/cloudflare_worker/public/forgot-password.html
+++ b/cloudflare_worker/public/forgot-password.html
@@ -147,7 +147,7 @@
       </div>
       <div class="hint" id="forgot-hint"></div>
       <button class="auth-button-primary btn-primary" id="forgot-btn">Send reset link</button>
-      <p class="row-links">Remembered it? <a href="/login.html">Back to log in</a></p>
+      <p class="row-links">Remembered it? <a href="/login">Back to log in</a></p>
       </section>
     </main>
   </body>
diff --git a/cloudflare_worker/public/login.html b/cloudflare_worker/public/login.html
index d6448785..bee85656 100644
--- a/cloudflare_worker/public/login.html
+++ b/cloudflare_worker/public/login.html
@@ -189,8 +189,8 @@
       </div>
       <div class="hint" id="login-hint"></div>
       <button class="auth-button-primary btn-primary" id="login-btn">Log in</button>
-      <p class="row-links"><a href="/forgot-password.html">Forgot your password?</a></p>
-      <p class="row-links">New to ForkMesh? <a href="/signup.html">Create an account</a></p>
+      <p class="row-links"><a href="/forgot-password">Forgot your password?</a></p>
+      <p class="row-links">New to ForkMesh? <a href="/signup">Create an account</a></p>
       </section>
     </main>
   </body>
diff --git a/cloudflare_worker/public/privacy.html b/cloudflare_worker/public/privacy.html
index e7e49b85..ceabd74f 100644
--- a/cloudflare_worker/public/privacy.html
+++ b/cloudflare_worker/public/privacy.html
@@ -93,6 +93,10 @@
       .site-footer-status-dot { background: var(--green); box-shadow: 0 0 18px rgba(63,185,80,.55); }
 
     </style>
+      <link rel="stylesheet" href="/site-footer.css" />
+    <script src="/site-footer.js" defer></script>
+    <link rel="stylesheet" href="/site-header.css" />
+    <script src="/site-header.js" defer></script>
   </head>
   <body class="h-full">
     <main class="legal-shell flex min-h-screen flex-col">
@@ -100,16 +104,7 @@
       <div class="mesh-grid" aria-hidden="true"></div>
       <div class="mesh-vignette" aria-hidden="true"></div>
 
-      <header class="relative z-10 mx-auto flex w-full max-w-7xl items-center justify-between px-5 py-6 lg:px-8">
-        <a href="/" class="brand inline-flex items-center gap-3" aria-label="ForkMesh home">
-          <img class="brand-mark h-10 w-auto" src="/assets/logo.png" alt="" aria-hidden="true" />
-        </a>
-        <nav class="hidden items-center gap-6 text-sm text-muted-foreground md:flex" aria-label="Primary">
-          <a class="transition-colors hover:text-foreground" href="/docs">Docs</a>
-          <a class="transition-colors hover:text-foreground" href="/blogs">Blog</a>
-          <a class="transition-colors hover:text-foreground" href="/login">Login</a>
-        </nav>
-      </header>
+      <div data-forkmesh-header="simple"></div>
 
       <section class="relative z-10 mx-auto w-full max-w-4xl px-5 pb-8 pt-16 lg:px-8 lg:pt-24">
         <article class="legal-card rounded-[2rem] px-6 py-8 sm:px-10 lg:px-14 lg:py-12">
@@ -146,69 +141,7 @@
         </article>
       </section>
 
-      <footer class="relative mt-24 overflow-hidden">
-        <div aria-hidden="true" class="site-footer-word-wrap relative -mb-[7.5rem] flex select-none justify-center overflow-hidden">
-          <div class="site-footer-glow relative inline-block">
-            <span class="site-footer-word footer-glow-base text-[23vw] font-semibold leading-none tracking-normal">forkmesh</span>
-            <span class="site-footer-word footer-glow-text pointer-events-none absolute inset-0 text-[23vw] font-semibold leading-none tracking-normal">forkmesh</span>
-          </div>
-        </div>
-        <div class="site-footer-panel relative z-10 border-t-[0.5px] border-[#3B3B3B] bg-black">
-          <div class="site-footer-links-grid mx-auto grid max-w-7xl grid-cols-2 gap-x-10 gap-y-12 px-5 py-14 md:grid-cols-3 lg:grid-cols-6 lg:px-8">
-            <div class="site-footer-brand col-span-2 md:col-span-3 lg:col-span-1">
-              <a href="/" aria-label="home" class="flex items-center space-x-2">
-                <img src="/assets/logo.png" alt="ForkMesh logo" class="site-footer-logo h-10 w-auto" />
-              </a>
-              <p class="site-footer-brand-copy mt-5 text-sm leading-relaxed text-muted-foreground">Local-first distributed source-code preservation and collaboration infrastructure.</p>
-              <div class="site-footer-status-pill mt-6 inline-flex items-center gap-2 rounded-lg border-[0.5px] border-[#3B3B3B] px-3 py-1.5 text-sm text-muted-foreground">
-                <span class="site-footer-status-dot size-2 rounded-full"></span>
-                Live now
-              </div>
-            </div>
-            <div>
-              <h3 class="site-footer-column-title text-sm font-medium text-foreground">Explore</h3>
-              <ul class="site-footer-link-list mt-4 flex flex-col gap-3">
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/#features">Features</a></li>
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/#network">Network discovery</a></li>
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/#solution">Solution</a></li>
-              </ul>
-            </div>
-            <div>
-              <h3 class="site-footer-column-title text-sm font-medium text-foreground">Product</h3>
-              <ul class="site-footer-link-list mt-4 flex flex-col gap-3">
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/#features">Local-first Git</a></li>
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/#features">Encrypted rooms</a></li>
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/#solution">Live node routing</a></li>
-              </ul>
-            </div>
-            <div>
-              <h3 class="site-footer-column-title text-sm font-medium text-foreground">Plans</h3>
-              <ul class="site-footer-link-list mt-4 flex flex-col gap-3">
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/#pricing">Pricing</a></li>
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/signup">Create account</a></li>
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/signup">Sign Up / Log In</a></li>
-              </ul>
-            </div>
-            <div>
-              <h3 class="site-footer-column-title text-sm font-medium text-foreground">Learn</h3>
-              <ul class="site-footer-link-list mt-4 flex flex-col gap-3">
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/#faq">FAQ</a></li>
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/docs">Read docs</a></li>
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/blogs">Blog</a></li>
-              </ul>
-            </div>
-            <div>
-              <h3 class="site-footer-column-title text-sm font-medium text-foreground">Company</h3>
-              <ul class="site-footer-link-list mt-4 flex flex-col gap-3">
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/privacy">Privacy</a></li>
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/terms">Terms</a></li>
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/login">Login</a></li>
-              </ul>
-            </div>
-          </div>
-          <div class="site-footer-copyright mx-auto max-w-7xl border-t-[0.5px] border-[#3B3B3B] px-5 py-6 text-sm text-muted-foreground lg:px-8">© 2026 ForkMesh. Built for local-first Git collaboration.</div>
-        </div>
-      </footer>
+      <div data-forkmesh-footer="standard"></div>
     </main>
   </body>
 </html>
diff --git a/cloudflare_worker/public/reset-password.html b/cloudflare_worker/public/reset-password.html
index ad7c4366..6f057f55 100644
--- a/cloudflare_worker/public/reset-password.html
+++ b/cloudflare_worker/public/reset-password.html
@@ -151,7 +151,7 @@
       </div>
       <div class="hint" id="reset-hint"></div>
       <button class="auth-button-primary btn-primary" id="reset-btn">Set new password</button>
-      <p class="row-links"><a href="/login.html">Back to log in</a></p>
+      <p class="row-links"><a href="/login">Back to log in</a></p>
       </section>
     </main>
   </body>
diff --git a/cloudflare_worker/public/reset-password.js b/cloudflare_worker/public/reset-password.js
index a5a1914a..b8073fbc 100644
--- a/cloudflare_worker/public/reset-password.js
+++ b/cloudflare_worker/public/reset-password.js
@@ -54,7 +54,7 @@
     }
     if (res.ok) {
       setHint("Password updated. Redirecting to log in…", "good");
-      setTimeout(() => (location.href = "/login.html"), 900);
+      setTimeout(() => (location.href = "/login"), 900);
       return;
     }
     btn.disabled = false;
diff --git a/cloudflare_worker/public/site-footer.css b/cloudflare_worker/public/site-footer.css
new file mode 100644
index 00000000..38ea2a36
--- /dev/null
+++ b/cloudflare_worker/public/site-footer.css
@@ -0,0 +1,244 @@
+.forkmesh-footer {
+  position: relative;
+  isolation: isolate;
+  overflow: hidden;
+  margin-top: 0;
+  color: var(--foreground, #f5f5f5);
+  background: transparent;
+}
+
+.forkmesh-footer-standard {
+  margin-top: 4rem;
+}
+
+.site-footer-word-wrap {
+  position: relative;
+  display: flex;
+  justify-content: center;
+  height: clamp(6rem, 23vw, 22rem);
+  margin-bottom: -7.5rem;
+  overflow: hidden;
+  user-select: none;
+}
+
+.site-footer-glow {
+  position: relative;
+  display: inline-block;
+}
+
+.site-footer-word {
+  display: block;
+  font-size: 23vw;
+  font-weight: 600;
+  line-height: 1;
+  letter-spacing: 0;
+  white-space: nowrap;
+}
+
+.footer-glow-base {
+  color: #0d0d0d;
+}
+
+.footer-glow-text {
+  position: absolute;
+  inset: 0;
+  pointer-events: none;
+  color: rgba(232, 232, 232, 0.74);
+  filter: drop-shadow(0 0 24px rgba(255, 255, 255, 0.18));
+  mask-image: radial-gradient(
+    7.5rem 7.5rem at var(--footer-glow-x, 50%) var(--footer-glow-y, 50%),
+    black 0%,
+    black 34%,
+    rgba(0, 0, 0, 0.7) 62%,
+    transparent 100%
+  );
+  -webkit-mask-image: radial-gradient(
+    7.5rem 7.5rem at var(--footer-glow-x, 50%) var(--footer-glow-y, 50%),
+    black 0%,
+    black 34%,
+    rgba(0, 0, 0, 0.7) 62%,
+    transparent 100%
+  );
+  opacity: var(--footer-glow-opacity, 0);
+  transition: opacity 200ms ease-out;
+  will-change: opacity, mask-image;
+}
+
+.site-footer-panel {
+  position: relative;
+  z-index: 10;
+  min-height: clamp(34rem, 47vw, 42rem);
+  border-top: 1px solid #3b3b3b;
+  background: #000;
+}
+
+.site-footer-links-grid {
+  display: grid;
+  width: 100%;
+  max-width: 80rem;
+  grid-template-columns: repeat(2, minmax(0, 1fr));
+  align-items: start;
+  gap: 3rem 2.5rem;
+  margin: 0 auto;
+  padding: clamp(5.75rem, 8vw, 8rem) 1.25rem;
+}
+
+.site-footer-brand {
+  grid-column: 1 / -1;
+  min-width: 0;
+}
+
+.site-footer-home-link {
+  display: inline-flex;
+  align-items: center;
+  color: inherit;
+  text-decoration: none;
+}
+
+.site-footer-home-link:hover {
+  text-decoration: none;
+}
+
+.site-footer-logo {
+  display: block;
+  width: auto;
+  height: 2.5rem;
+}
+
+.site-footer-brand-copy {
+  max-width: 13rem;
+  margin: 1.25rem 0 0;
+  color: var(--muted-foreground, var(--muted, #a3a3a3));
+  font-size: 0.875rem;
+  line-height: 1.6;
+}
+
+.site-footer-socials {
+  display: flex;
+  flex-wrap: wrap;
+  gap: 0.5rem;
+  margin-top: 1.5rem;
+}
+
+.site-footer-social-link {
+  display: flex;
+  width: 2.25rem;
+  height: 2.25rem;
+  align-items: center;
+  justify-content: center;
+  border: 1px solid var(--border, #313131);
+  border-radius: 0.5rem;
+  color: var(--muted-foreground, var(--muted, #a3a3a3));
+  transition:
+    border-color 180ms ease,
+    color 180ms ease;
+}
+
+.site-footer-social-link:hover {
+  border-color: rgba(232, 232, 232, 0.3);
+  color: var(--foreground, #f5f5f5);
+  text-decoration: none;
+}
+
+.site-footer-social-link svg {
+  width: 1rem;
+  height: 1rem;
+  fill: currentColor;
+  stroke: none;
+}
+
+.site-footer-status-pill {
+  display: inline-flex;
+  align-items: center;
+  gap: 0.5rem;
+  margin-top: 1.5rem;
+  padding: 0.375rem 0.75rem;
+  border: 1px solid #3b3b3b;
+  border-radius: 0.5rem;
+  color: var(--muted-foreground, var(--muted, #a3a3a3));
+  font-size: 0.875rem;
+  line-height: 1.25;
+}
+
+.site-footer-status-pill:hover {
+  color: var(--foreground, #f5f5f5);
+  text-decoration: none;
+}
+
+.site-footer-status-dot {
+  width: 0.5rem;
+  height: 0.5rem;
+  flex: 0 0 auto;
+  border-radius: 9999px;
+  background: var(--green, var(--accent-bright, #28c878));
+  box-shadow: 0 0 18px rgba(63, 185, 80, 0.48);
+}
+
+.site-footer-column-title {
+  margin: 0;
+  color: var(--foreground, #f5f5f5);
+  font-size: 0.875rem;
+  font-weight: 600;
+}
+
+.site-footer-link-list {
+  display: flex;
+  flex-direction: column;
+  gap: 0.75rem;
+  margin: 1rem 0 0;
+  padding: 0;
+  list-style: none;
+}
+
+.site-footer-column-link {
+  display: inline-flex;
+  min-width: 0;
+  color: var(--muted-foreground, var(--muted, #a3a3a3));
+  font-size: 0.875rem;
+  line-height: 1.35;
+  transition: color 180ms ease;
+}
+
+.site-footer-column-link:hover {
+  color: var(--foreground, #f5f5f5);
+  text-decoration: none;
+}
+
+.site-footer-copyright {
+  max-width: 80rem;
+  margin: 0 auto;
+  padding: 1.5rem 1.25rem;
+  border-top: 1px solid #3b3b3b;
+  color: var(--muted-foreground, var(--muted, #a3a3a3));
+  font-size: 0.875rem;
+  line-height: 1.4;
+}
+
+@media (max-width: 768px) {
+  .site-footer-word-wrap {
+    margin-bottom: -3rem;
+  }
+}
+
+@media (min-width: 768px) {
+  .site-footer-links-grid {
+    grid-template-columns: repeat(3, minmax(0, 1fr));
+  }
+}
+
+@media (min-width: 1024px) {
+  .site-footer-links-grid {
+    grid-template-columns: repeat(6, minmax(0, 1fr));
+    padding-right: 2rem;
+    padding-left: 2rem;
+  }
+
+  .site-footer-brand {
+    grid-column: auto;
+  }
+
+  .site-footer-copyright {
+    padding-right: 2rem;
+    padding-left: 2rem;
+  }
+}
diff --git a/cloudflare_worker/public/site-footer.js b/cloudflare_worker/public/site-footer.js
new file mode 100644
index 00000000..52c3d155
--- /dev/null
+++ b/cloudflare_worker/public/site-footer.js
@@ -0,0 +1,199 @@
+(() => {
+  const FOOTER_COLUMNS = [
+    {
+      title: "Explore",
+      links: [
+        { href: "/features", label: "Features" },
+        { href: "/network", label: "Network" },
+        { href: "/desktop", label: "Desktop app" },
+      ],
+    },
+    {
+      title: "Company",
+      links: [
+        { href: "/about", label: "About" },
+        { href: "/careers", label: "Careers" },
+        { href: "/changelog", label: "Changelog" },
+        { href: "/terms", label: "Terms" },
+        { href: "/privacy", label: "Privacy" },
+      ],
+    },
+    {
+      title: "Plans",
+      links: [
+        { href: "/pricing", label: "Pricing" },
+        { href: "/signup", label: "Create account" },
+        { href: "/login", label: "Log In" },
+        { href: "/mirror-payouts", label: "Mirror payouts" },
+      ],
+    },
+    {
+      title: "Learn",
+      links: [
+        { href: "/#faq", label: "FAQ" },
+        { href: "/blogs", label: "Blogs" },
+        { href: "/docs", label: "Documentation" },
+        { href: "/security-report", label: "Security report" },
+      ],
+    },
+    {
+      title: "Community",
+      links: [
+        { href: "/status", label: "System status" },
+        { href: "/chat", label: "Community chat" },
+        {
+          href: "https://reddit.com/r/forkmesh",
+          label: "Reddit",
+          external: true,
+        },
+        {
+          href: "https://x.com/forkmesh",
+          label: "X (Twitter)",
+          external: true,
+        },
+      ],
+    },
+  ];
+
+  const SOCIAL_LINKS = [
+    {
+      href: "https://x.com/forkmesh",
+      label: "X (Twitter)",
+      external: true,
+      icon:
+        '<path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"></path>',
+    },
+    {
+      href: "https://reddit.com/r/forkmesh",
+      label: "Reddit",
+      external: true,
+      icon:
+        '<path d="M12 0A12 12 0 0 0 0 12a12 12 0 0 0 12 12 12 12 0 0 0 12-12A12 12 0 0 0 12 0zm5.01 4.744c.688 0 1.25.561 1.25 1.249a1.25 1.25 0 0 1-2.498.056l-2.597-.547-.8 3.747c1.824.07 3.48.632 4.674 1.488.308-.309.73-.491 1.207-.491.968 0 1.754.786 1.754 1.754 0 .716-.435 1.333-1.01 1.614a3.111 3.111 0 0 1 .042.52c0 2.694-3.13 4.87-7.004 4.87-3.874 0-7.004-2.176-7.004-4.87 0-.183.015-.366.043-.534A1.748 1.748 0 0 1 4.028 12c0-.968.786-1.754 1.754-1.754.463 0 .898.196 1.207.49 1.207-.883 2.878-1.43 4.744-1.487l.885-4.182a.342.342 0 0 1 .14-.197.35.35 0 0 1 .238-.042l2.906.617a1.214 1.214 0 0 1 1.108-.701z"></path>',
+    },
+    {
+      href: "/chat",
+      label: "Chat",
+      icon:
+        '<path d="M4.804 21.644A6.707 6.707 0 0 0 6 21.75a6.721 6.721 0 0 0 3.583-1.029c.774.182 1.584.279 2.417.279 5.322 0 9.75-3.97 9.75-9 0-5.03-4.428-9-9.75-9s-9.75 3.97-9.75 9c0 2.409 1.025 4.587 2.674 6.192.232.226.277.428.254.543a3.73 3.73 0 0 1-.814 1.686.75.75 0 0 0 .44 1.223Z"></path>',
+    },
+  ];
+
+  function attrsForLink(link) {
+    return link.external ? ' target="_blank" rel="noopener noreferrer"' : "";
+  }
+
+  function renderColumn(column) {
+    return `
+          <div>
+            <h3 class="site-footer-column-title">${column.title}</h3>
+            <ul class="site-footer-link-list">
+              ${column.links
+                .map(
+                  (link) => `
+              <li><a class="site-footer-column-link" href="${link.href}"${attrsForLink(link)}>${link.label}</a></li>`,
+                )
+                .join("")}
+            </ul>
+          </div>`;
+  }
+
+  function renderSocialLink(link) {
+    return `
+              <a
+                href="${link.href}"
+                aria-label="${link.label}"
+                class="site-footer-social-link"
+                ${attrsForLink(link)}
+              >
+                <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
+                  ${link.icon}
+                </svg>
+              </a>`;
+  }
+
+  function renderFooter(variant) {
+    const isLanding = variant === "landing";
+    const footerId = isLanding ? ' id="signup"' : "";
+
+    return `
+    <footer${footerId} class="forkmesh-footer forkmesh-footer-${isLanding ? "landing" : "standard"}" data-rendered-footer="${isLanding ? "landing" : "standard"}">
+      <div aria-hidden="true" class="site-footer-word-wrap">
+        <div
+          id="footer-glow"
+          class="site-footer-glow"
+          style="--footer-glow-opacity: 0; --footer-glow-x: 50%; --footer-glow-y: 50%;"
+        >
+          <span class="site-footer-word footer-glow-base">forkmesh</span>
+          <span class="site-footer-word footer-glow-text">forkmesh</span>
+        </div>
+      </div>
+      <div class="site-footer-panel">
+        <div class="site-footer-links-grid">
+          <div class="site-footer-brand">
+            <a href="/" aria-label="home" class="site-footer-home-link">
+              <img src="/assets/logo.png" alt="ForkMesh logo" class="site-footer-logo" />
+            </a>
+            <p class="site-footer-brand-copy">
+              Local-first distributed source-code preservation and collaboration infrastructure.
+            </p>
+            <div class="site-footer-socials">
+              ${SOCIAL_LINKS.map(renderSocialLink).join("")}
+            </div>
+            <a href="/status" class="site-footer-status-pill">
+              <span class="site-footer-status-dot" aria-hidden="true"></span>
+              System status
+            </a>
+          </div>
+          ${FOOTER_COLUMNS.map(renderColumn).join("")}
+        </div>
+        <div class="site-footer-copyright">
+          &copy; 2026 ForkMesh. Built for local-first Git collaboration.
+        </div>
+      </div>
+    </footer>`;
+  }
+
+  function initFooterGlow(root) {
+    const glow = root.querySelector(".site-footer-glow");
+    if (!glow) return;
+
+    const move = (event) => {
+      const rect = glow.getBoundingClientRect();
+      glow.style.setProperty("--footer-glow-x", `${event.clientX - rect.left}px`);
+      glow.style.setProperty("--footer-glow-y", `${event.clientY - rect.top}px`);
+      glow.style.setProperty("--footer-glow-opacity", "1");
+    };
+
+    glow.addEventListener("pointerenter", move);
+    glow.addEventListener("pointermove", move);
+    glow.addEventListener("pointerleave", () => {
+      glow.style.setProperty("--footer-glow-opacity", "0");
+    });
+  }
+
+  function mountFooter(host) {
+    const variant = host.dataset.forkmeshFooter || "standard";
+    const template = document.createElement("template");
+    template.innerHTML = renderFooter(variant).trim();
+    const footer = template.content.firstElementChild;
+    host.replaceWith(footer);
+    initFooterGlow(footer);
+  }
+
+  function mountFooters() {
+    document
+      .querySelectorAll("[data-forkmesh-footer]")
+      .forEach((host) => mountFooter(host));
+  }
+
+  if (document.readyState === "loading") {
+    document.addEventListener("DOMContentLoaded", mountFooters);
+  } else {
+    mountFooters();
+  }
+
+  window.ForkMeshFooter = {
+    renderFooter,
+    mountFooters,
+  };
+})();
diff --git a/cloudflare_worker/public/site-header.css b/cloudflare_worker/public/site-header.css
new file mode 100644
index 00000000..afbcbdf5
--- /dev/null
+++ b/cloudflare_worker/public/site-header.css
@@ -0,0 +1,63 @@
+.forkmesh-simple-header {
+  position: relative;
+  z-index: 10;
+  display: flex;
+  width: 100%;
+  max-width: 80rem;
+  align-items: center;
+  justify-content: space-between;
+  margin-inline: auto;
+  padding: 1.5rem 1.25rem;
+}
+
+.forkmesh-simple-brand {
+  display: inline-flex;
+  align-items: center;
+  gap: 0.75rem;
+  color: inherit;
+  text-decoration: none;
+}
+
+.forkmesh-simple-brand:hover {
+  text-decoration: none;
+}
+
+.forkmesh-simple-brand-mark {
+  width: auto;
+  height: 2.5rem;
+  display: block;
+  object-fit: contain;
+  filter: drop-shadow(0 0 22px rgba(63, 185, 80, 0.16));
+}
+
+.forkmesh-simple-header-nav {
+  display: none;
+  align-items: center;
+  gap: 1.5rem;
+  color: var(--muted-foreground, #a3a3a3);
+  font-size: 0.875rem;
+  line-height: 1.25rem;
+}
+
+.forkmesh-simple-header-nav a {
+  color: inherit;
+  text-decoration: none;
+  transition: color 150ms ease;
+}
+
+.forkmesh-simple-header-nav a:hover {
+  color: var(--foreground, #f5f5f5);
+  text-decoration: none;
+}
+
+@media (min-width: 768px) {
+  .forkmesh-simple-header-nav {
+    display: flex;
+  }
+}
+
+@media (min-width: 1024px) {
+  .forkmesh-simple-header {
+    padding-inline: 2rem;
+  }
+}
diff --git a/cloudflare_worker/public/site-header.js b/cloudflare_worker/public/site-header.js
new file mode 100644
index 00000000..7265b60a
--- /dev/null
+++ b/cloudflare_worker/public/site-header.js
@@ -0,0 +1,19 @@
+(() => {
+  const SIMPLE_HEADER_HTML = `
+    <header class="forkmesh-simple-header relative z-10 mx-auto flex w-full max-w-7xl items-center justify-between px-5 py-6 lg:px-8">
+      <a href="/" class="brand forkmesh-simple-brand inline-flex items-center gap-3" aria-label="ForkMesh home">
+        <img class="brand-mark forkmesh-simple-brand-mark h-10 w-auto" src="/assets/logo.png" alt="" aria-hidden="true" />
+      </a>
+      <nav class="forkmesh-simple-header-nav hidden items-center gap-6 text-sm text-muted-foreground md:flex" aria-label="Primary">
+        <a class="transition-colors hover:text-foreground" href="/docs">Docs</a>
+        <a class="transition-colors hover:text-foreground" href="/blogs">Blog</a>
+        <a class="transition-colors hover:text-foreground" href="/login">Login</a>
+      </nav>
+    </header>
+  `;
+
+  document.querySelectorAll("[data-forkmesh-header]").forEach((mount) => {
+    if (mount.dataset.forkmeshHeader !== "simple") return;
+    mount.outerHTML = SIMPLE_HEADER_HTML;
+  });
+})();
diff --git a/cloudflare_worker/public/static-page.js b/cloudflare_worker/public/static-page.js
index 9a84919a..abd4e960 100644
--- a/cloudflare_worker/public/static-page.js
+++ b/cloudflare_worker/public/static-page.js
@@ -27,8 +27,9 @@
   }
 
   function setText(selector, value) {
-    const el = document.querySelector(selector);
-    if (el) el.textContent = value;
+    document.querySelectorAll(selector).forEach((el) => {
+      el.textContent = value;
+    });
   }
 
   // The last-known counts are cached so the header pill and stat cards render
@@ -248,9 +249,11 @@
     const hostsEl = document.querySelector("#network-hosts");
     if (location.protocol === "file:") {
       if (clientsCount) clientsCount.textContent = "Preview only";
-      setText("#network-clients", "—");
+      setText("#network-clients, [data-network-clients]", "—");
       if (reposEl) reposEl.textContent = "—";
+      setText("[data-network-repos]", "—");
       if (hostsEl) hostsEl.textContent = "—";
+      setText("[data-network-hosts]", "—");
       renderPayoutNodes([]);
       return;
     }
@@ -263,9 +266,11 @@
       const hosts = Number(data.hosts) || 0;
       const repos = Number(data.repos) || 0;
       renderOnline(clients + hosts);
-      setText("#network-clients", String(clients));
+      setText("#network-clients, [data-network-clients]", String(clients));
       if (reposEl) reposEl.textContent = String(repos);
+      setText("[data-network-repos]", String(repos));
       if (hostsEl) hostsEl.textContent = String(hosts);
+      setText("[data-network-hosts]", String(hosts));
       writeCachedStat("clients", clients);
       writeCachedStat("hosts", hosts);
       writeCachedStat("repos", repos);
@@ -273,6 +278,7 @@
     } catch (error) {
       if (clientsCount) clientsCount.textContent = "Reconnecting…";
       if (clientsDot) clientsDot.classList.remove("online");
+      renderPayoutNodes([]);
     }
   }
 
@@ -297,9 +303,9 @@
     const hosts = readCachedStat("hosts");
     const repos = readCachedStat("repos");
     if (clients !== null && hosts !== null) renderOnline(clients + hosts);
-    if (clients !== null) setText("#network-clients", String(clients));
-    if (hosts !== null) setText("#network-hosts", String(hosts));
-    if (repos !== null) setText("#network-repos", String(repos));
+    if (clients !== null) setText("#network-clients, [data-network-clients]", String(clients));
+    if (hosts !== null) setText("#network-hosts, [data-network-hosts]", String(hosts));
+    if (repos !== null) setText("#network-repos, [data-network-repos]", String(repos));
   }
 
   // Don't poll while the tab is hidden; resume (and refresh immediately) on focus.
diff --git a/cloudflare_worker/public/status.html b/cloudflare_worker/public/status.html
index ab2ca837..679b72ec 100644
--- a/cloudflare_worker/public/status.html
+++ b/cloudflare_worker/public/status.html
@@ -20,38 +20,13 @@
     <link rel="manifest" href="/favicon/site.webmanifest" />
     <link rel="stylesheet" href="styles.css" />
     <script src="static-page.js" defer></script>
+      <link rel="stylesheet" href="/site-footer.css" />
+    <script src="/site-footer.js" defer></script>
+    <link rel="stylesheet" href="/site-header.css" />
+    <script src="/site-header.js" defer></script>
   </head>
   <body>
-    <header class="site-header">
-      <div class="header-inner">
-        <a class="brand" href="/" aria-label="ForkMesh home">
-          <img class="brand-mark" src="/assets/logo.png" alt="" aria-hidden="true" />
-          <span>ForkMesh</span>
-        </a>
-        <nav class="global-nav" aria-label="Global">
-          <a href="/dashboard">Repositories</a>
-          <a href="/network/">Network</a>
-          <a href="/docs/">Docs</a>
-          <a href="/status">Status</a>
-          <a href="/blogs">Blog</a>
-          <a href="/changelog.html">Changelog</a>
-          <a href="/chat">Chat</a>
-          <a class="nav-cta" href="/signup">Sign Up / Log In</a>
-        </nav>
-        <div class="header-actions">
-          <div class="clients-pill" aria-live="polite">
-            <span class="clients-relay-icon" id="clients-dot" aria-hidden="true"><svg viewBox="0 0 16 16" fill="currentColor" xmlns="http://www.w3.org/2000/svg"><path d="M8.75 8.582v5.668a.75.75 0 0 1-1.5 0V8.582a1.75 1.75 0 1 1 1.5 0Zm3.983-7.125a.75.75 0 0 1 1.06.026A7.976 7.976 0 0 1 16 7c0 2.139-.84 4.083-2.207 5.517a.75.75 0 1 1-1.086-1.034A6.474 6.474 0 0 0 14.5 7a6.474 6.474 0 0 0-1.793-4.483.75.75 0 0 1 .026-1.06Zm-9.466 0c.3.286.312.76.026 1.06A6.474 6.474 0 0 0 1.5 7a6.47 6.47 0 0 0 1.793 4.483.75.75 0 0 1-1.086 1.034A7.973 7.973 0 0 1 0 7c0-2.139.84-4.083 2.207-5.517a.75.75 0 0 1 1.06-.026Zm8.556 2.321A4.988 4.988 0 0 1 13 7a4.988 4.988 0 0 1-1.177 3.222.75.75 0 1 1-1.146-.967A3.487 3.487 0 0 0 11.5 7c0-.86-.309-1.645-.823-2.255a.75.75 0 0 1 1.146-.967Zm-6.492.958A3.48 3.48 0 0 0 4.5 7a3.48 3.48 0 0 0 .823 2.255.75.75 0 0 1-1.146.967A4.981 4.981 0 0 1 3 7a4.982 4.982 0 0 1 1.188-3.236.75.75 0 1 1 1.143.972Z"/></svg></span>
-            <span id="clients-count">Checking…</span>
-          </div>
-          <button id="theme-toggle" class="icon-button" type="button" aria-label="Toggle theme">
-            <svg class="moon" viewBox="0 0 24 24" aria-hidden="true"><path d="M21 12.8A8.5 8.5 0 1 1 11.2 3 6.7 6.7 0 0 0 21 12.8Z"></path></svg>
-            <svg class="sun" viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="4"></circle><path d="M12 2v2M12 20v2M4.93 4.93l1.42 1.42M17.65 17.65l1.42 1.42M2 12h2M20 12h2M4.93 19.07l1.42-1.42M17.65 6.35l1.42-1.42"></path></svg>
-          </button>
-          <a class="nav-admin" data-admin-link href="#" hidden>Admin</a>
-          <a class="install-link" href="/#install-banner">Install</a>
-        </div>
-      </div>
-    </header>
+    <div data-forkmesh-header="simple"></div>
 
     <main class="page-shell">
       <section class="page-hero" aria-labelledby="status-title">
@@ -119,46 +94,7 @@
       </section>
     </main>
 
-    <footer class="site-footer">
-      <div class="footer-inner">
-        <div>
-          <div class="footer-brand">
-            <img src="/assets/logo.png" alt="" aria-hidden="true" />
-            <span>ForkMesh</span>
-          </div>
-          <p class="footer-tagline">
-            Code hosting that lives on the network — distributed, signed, and
-            impossible to quietly take away.
-          </p>
-        </div>
-        <nav class="footer-links" aria-label="Footer">
-          <div class="footer-col">
-            <h3>Product</h3>
-            <a href="/dashboard">Repositories</a>
-            <a href="/network/">Network</a>
-            <a href="/docs/">Docs</a>
-            <a href="/status">Status</a>
-          </div>
-          <div class="footer-col">
-            <h3>Company</h3>
-            <a href="/about.html">About</a>
-            <a href="/careers.html">Careers</a>
-            <a href="/blogs">Blog</a>
-            <a href="/signup">Sign Up / Log In</a>
-          </div>
-          <div class="footer-col">
-            <h3>Get in touch</h3>
-            <a href="mailto:founders@forkmesh.com">founders@forkmesh.com</a>
-            <a href="/privacy.html">Privacy</a>
-            <a href="/terms.html">Terms</a>
-          </div>
-        </nav>
-      </div>
-      <div class="footer-bottom">
-        Questions or feedback? Email the founders at
-        <a href="mailto:founders@forkmesh.com">founders@forkmesh.com</a>.
-      </div>
-    </footer>
+    <div data-forkmesh-footer="standard"></div>
 
     <script>
       (() => {
diff --git a/cloudflare_worker/public/terms.html b/cloudflare_worker/public/terms.html
index b63da85c..159f0654 100644
--- a/cloudflare_worker/public/terms.html
+++ b/cloudflare_worker/public/terms.html
@@ -93,6 +93,10 @@
       .site-footer-status-dot { background: var(--green); box-shadow: 0 0 18px rgba(63,185,80,.55); }
 
     </style>
+      <link rel="stylesheet" href="/site-footer.css" />
+    <script src="/site-footer.js" defer></script>
+    <link rel="stylesheet" href="/site-header.css" />
+    <script src="/site-header.js" defer></script>
   </head>
   <body class="h-full">
     <main class="legal-shell flex min-h-screen flex-col">
@@ -100,16 +104,7 @@
       <div class="mesh-grid" aria-hidden="true"></div>
       <div class="mesh-vignette" aria-hidden="true"></div>
 
-      <header class="relative z-10 mx-auto flex w-full max-w-7xl items-center justify-between px-5 py-6 lg:px-8">
-        <a href="/" class="brand inline-flex items-center gap-3" aria-label="ForkMesh home">
-          <img class="brand-mark h-10 w-auto" src="/assets/logo.png" alt="" aria-hidden="true" />
-        </a>
-        <nav class="hidden items-center gap-6 text-sm text-muted-foreground md:flex" aria-label="Primary">
-          <a class="transition-colors hover:text-foreground" href="/docs">Docs</a>
-          <a class="transition-colors hover:text-foreground" href="/blogs">Blog</a>
-          <a class="transition-colors hover:text-foreground" href="/login">Login</a>
-        </nav>
-      </header>
+      <div data-forkmesh-header="simple"></div>
 
       <section class="relative z-10 mx-auto w-full max-w-4xl px-5 pb-8 pt-16 lg:px-8 lg:pt-24">
         <article class="legal-card rounded-[2rem] px-6 py-8 sm:px-10 lg:px-14 lg:py-12">
@@ -144,69 +139,7 @@
         </article>
       </section>
 
-      <footer class="relative mt-24 overflow-hidden">
-        <div aria-hidden="true" class="site-footer-word-wrap relative -mb-[7.5rem] flex select-none justify-center overflow-hidden">
-          <div class="site-footer-glow relative inline-block">
-            <span class="site-footer-word footer-glow-base text-[23vw] font-semibold leading-none tracking-normal">forkmesh</span>
-            <span class="site-footer-word footer-glow-text pointer-events-none absolute inset-0 text-[23vw] font-semibold leading-none tracking-normal">forkmesh</span>
-          </div>
-        </div>
-        <div class="site-footer-panel relative z-10 border-t-[0.5px] border-[#3B3B3B] bg-black">
-          <div class="site-footer-links-grid mx-auto grid max-w-7xl grid-cols-2 gap-x-10 gap-y-12 px-5 py-14 md:grid-cols-3 lg:grid-cols-6 lg:px-8">
-            <div class="site-footer-brand col-span-2 md:col-span-3 lg:col-span-1">
-              <a href="/" aria-label="home" class="flex items-center space-x-2">
-                <img src="/assets/logo.png" alt="ForkMesh logo" class="site-footer-logo h-10 w-auto" />
-              </a>
-              <p class="site-footer-brand-copy mt-5 text-sm leading-relaxed text-muted-foreground">Local-first distributed source-code preservation and collaboration infrastructure.</p>
-              <div class="site-footer-status-pill mt-6 inline-flex items-center gap-2 rounded-lg border-[0.5px] border-[#3B3B3B] px-3 py-1.5 text-sm text-muted-foreground">
-                <span class="site-footer-status-dot size-2 rounded-full"></span>
-                Live now
-              </div>
-            </div>
-            <div>
-              <h3 class="site-footer-column-title text-sm font-medium text-foreground">Explore</h3>
-              <ul class="site-footer-link-list mt-4 flex flex-col gap-3">
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/#features">Features</a></li>
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/#network">Network discovery</a></li>
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/#solution">Solution</a></li>
-              </ul>
-            </div>
-            <div>
-              <h3 class="site-footer-column-title text-sm font-medium text-foreground">Product</h3>
-              <ul class="site-footer-link-list mt-4 flex flex-col gap-3">
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/#features">Local-first Git</a></li>
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/#features">Encrypted rooms</a></li>
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/#solution">Live node routing</a></li>
-              </ul>
-            </div>
-            <div>
-              <h3 class="site-footer-column-title text-sm font-medium text-foreground">Plans</h3>
-              <ul class="site-footer-link-list mt-4 flex flex-col gap-3">
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/#pricing">Pricing</a></li>
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/signup">Create account</a></li>
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/signup">Sign Up / Log In</a></li>
-              </ul>
-            </div>
-            <div>
-              <h3 class="site-footer-column-title text-sm font-medium text-foreground">Learn</h3>
-              <ul class="site-footer-link-list mt-4 flex flex-col gap-3">
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/#faq">FAQ</a></li>
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/docs">Read docs</a></li>
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/blogs">Blog</a></li>
-              </ul>
-            </div>
-            <div>
-              <h3 class="site-footer-column-title text-sm font-medium text-foreground">Company</h3>
-              <ul class="site-footer-link-list mt-4 flex flex-col gap-3">
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/privacy">Privacy</a></li>
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/terms">Terms</a></li>
-                <li><a class="site-footer-column-link text-sm text-muted-foreground transition-colors hover:text-foreground" href="/login">Login</a></li>
-              </ul>
-            </div>
-          </div>
-          <div class="site-footer-copyright mx-auto max-w-7xl border-t-[0.5px] border-[#3B3B3B] px-5 py-6 text-sm text-muted-foreground lg:px-8">© 2026 ForkMesh. Built for local-first Git collaboration.</div>
-        </div>
-      </footer>
+      <div data-forkmesh-footer="standard"></div>
     </main>
   </body>
 </html>
diff --git a/cloudflare_worker/src/entry.py b/cloudflare_worker/src/entry.py
index d08c0b3e..8ae64a51 100644
--- a/cloudflare_worker/src/entry.py
+++ b/cloudflare_worker/src/entry.py
@@ -183,6 +183,14 @@ from dashboard_bundle import (  # noqa: E402
     fragment_path,
 )
 
+# Static route ownership rules: repo shortcuts are Python-owned so hard refresh
+# on /owner/repo serves the dashboard shell; direct implementation-file URLs
+# such as /login.html stay non-public.
+from static_routes import (  # noqa: E402
+    BLOCKED_STATIC_HTML_PATHS,
+    looks_like_repo_route,
+)
+
 # Release manifest + content-addressed blob helpers (tag/asset validation, the
 # CAS blob path layout, the canonical signable manifest body, semver ordering)
 # live in releases.py — another pure, js-free sibling module the runtime bundles
@@ -5363,7 +5371,7 @@ async def _send_password_reset_email(env, request, name, email, pass_hash):
     expires = int(Date.now()) + PASSWORD_RESET_TTL_MS
     token = await _password_reset_token(env, name, email, pass_hash, expires)
     link = (_public_base_url(env, request) +
-            "/reset-password.html?node=" + quote(name) +
+            "/reset-password?node=" + quote(name) +
             "&exp=" + str(expires) + "&token=" + token)
     subject = "Reset your ForkMesh password"
     text = ("A password reset was requested for your ForkMesh node \"" + name +
@@ -5478,7 +5486,7 @@ async def _verify_email(env, request):
                     env, "DELETE FROM pending_verifications WHERE name_bi=?", name_bi)
             return _verify_email_page(
                 "<h1>Email confirmed</h1><p>Your ForkMesh email is verified. "
-                "You can close this tab and <a href=\"/login.html\">log in</a>.</p>",
+                "You can close this tab and <a href=\"/login\">log in</a>.</p>",
                 200)
     return _verify_email_page(
         "<h1>Verification link invalid</h1><p>This confirmation link is invalid "
@@ -8679,16 +8687,8 @@ class Default(WorkerEntrypoint):
         if git_recv and method_name(request) == "POST":
             return await self._git_push(request, git_recv.group(1), git_recv.group(2))
 
-        # Static docs/network directories are canonical with a trailing slash.
-        # Keep this as routing support only; all persistent v0.3.0 APIs stay below.
-        if url.path == "/network":
-            return Response("", status=308, headers={"location": "/network/"})
-        if url.path == "/docs":
-            return Response("", status=308, headers={"location": "/docs/"})
-        if url.path == "/blog":
-            return Response("", status=308, headers={"location": "/blogs"})
-        if url.path == "/blog.html":
-            return Response("", status=308, headers={"location": "/blogs"})
+        if url.path in BLOCKED_STATIC_HTML_PATHS:
+            return await self._serve_not_found_page(url)
 
         if url.path in ("/health", "/api/mainnode"):
             return json_response(
@@ -9033,11 +9033,27 @@ class Default(WorkerEntrypoint):
         if url.path == "/dashboard.js":
             return await self._serve_dashboard_bundle(url)
 
+        if looks_like_repo_route(url.path):
+            return await self._serve_dashboard_shell(url)
+
         if url.path == "/dashboard" or url.path.startswith("/dashboard/"):
             return await self._serve_dashboard_shell(url)
 
         return json_response({"error": "not_found"}, status=404)
 
+    async def _serve_not_found_page(self, url):
+        base = url.scheme + "://" + url.netloc + "/"
+        try:
+            resp = await self.env.ASSETS.fetch(base + "404.html")
+            body = await resp.text()
+        except Exception:
+            body = "not found"
+        return Response(
+            body,
+            status=404,
+            headers={"content-type": "text/html; charset=utf-8"},
+        )
+
     async def _serve_dashboard_bundle(self, url):
         # Concatenate /dashboard.js from its ordered fragments (see
         # dashboard_bundle.py). Each fragment is a real static asset, so
diff --git a/cloudflare_worker/src/static_routes.py b/cloudflare_worker/src/static_routes.py
new file mode 100644
index 00000000..1f503834
--- /dev/null
+++ b/cloudflare_worker/src/static_routes.py
@@ -0,0 +1,91 @@
+"""Static URL ownership for the ForkMesh public site.
+
+Clean marketing/auth routes are served by Workers Static Assets from
+``public/_redirects``. Repo shortcuts are Worker-owned so hard-refreshing
+``/owner/repo`` and tab/tree/blob deep links can serve the dashboard shell
+without relying on a client-side 404 bounce. Direct implementation-file URLs
+such as ``/login.html`` are intentionally blocked.
+"""
+
+from urllib.parse import unquote
+
+
+REPO_TAB_ROUTES = frozenset({
+    "commits",
+    "releases",
+    "issues",
+    "pulls",
+    "discussions",
+    "mirrors",
+    "agents",
+})
+
+RESERVED_ROUTE_PREFIXES = frozenset({
+    "api",
+    "assets",
+    "favicon",
+    "dashboard",
+    "docs",
+    "blogs",
+    "blog",
+    "login",
+    "signup",
+    "forgot-password",
+    "reset-password",
+    "mirror-payouts",
+    "security-report",
+    "network",
+    "chat",
+    "desktop",
+    "about",
+    "features",
+    "careers",
+    "changelog",
+    "privacy",
+    "terms",
+    "status",
+})
+
+BLOCKED_STATIC_HTML_PATHS = frozenset({
+    "/index.html",
+    "/dashboard.html",
+    "/dashboard/index.html",
+    "/blog.html",
+    "/login.html",
+    "/signup.html",
+    "/forgot-password.html",
+    "/reset-password.html",
+    "/mirror-payouts.html",
+    "/security-report.html",
+    "/docs.html",
+    "/network.html",
+    "/chat.html",
+    "/desktop.html",
+    "/about.html",
+    "/features.html",
+    "/careers.html",
+    "/changelog.html",
+    "/privacy.html",
+    "/terms.html",
+    "/status.html",
+})
+
+
+def path_parts(pathname):
+    return [part for part in (pathname or "").split("/") if part]
+
+
+def looks_like_repo_route(pathname):
+    parts = path_parts(pathname)
+    if len(parts) < 2:
+        return False
+
+    owner = unquote(parts[0]).lower()
+    if owner in RESERVED_ROUTE_PREFIXES:
+        return False
+
+    if len(parts) == 2:
+        return True
+
+    kind = unquote(parts[2])
+    return kind in ("tree", "blob") or kind in REPO_TAB_ROUTES
diff --git a/cloudflare_worker/tests/test_account_profile_endpoints.py b/cloudflare_worker/tests/test_account_profile_endpoints.py
index 4d2735c2..020f6e06 100644
--- a/cloudflare_worker/tests/test_account_profile_endpoints.py
+++ b/cloudflare_worker/tests/test_account_profile_endpoints.py
@@ -273,7 +273,8 @@ def test_reset_password_validates_token_expiry_and_hashes_new_password():
 def test_login_page_links_to_password_reset():
     login_html = (Path(__file__).resolve().parents[1] /
                   "public" / "login.html").read_text(encoding="utf-8")
-    assert "/forgot-password.html" in login_html
+    assert "/forgot-password" in login_html
+    assert "/forgot-password.html" not in login_html
     for asset in ("forgot-password.html", "forgot-password.js",
                   "reset-password.html", "reset-password.js"):
         assert (Path(__file__).resolve().parents[1] / "public" / asset).exists()
@@ -285,7 +286,7 @@ if __name__ == "__main__":
         test_signup_endpoint_is_not_the_solana_payment_flow,
         test_worker_exposes_password_authenticated_profile_endpoint_for_wallet_and_verification,
         test_worker_profile_contract_includes_avatar_updates,
-        test_qt_client_publishes_effective_avatar_with_signed_heartbeat,
+        test_qt_client_publishes_effective_avatar_to_peers,
         test_profile_endpoint_supports_verified_node_rename_and_hard_delete,
         test_hard_delete_removes_account_identity_and_owned_namespace_state,
         test_namespace_rename_moves_account_repo_and_repo_scoped_state,
@@ -339,4 +340,5 @@ def test_reset_token_bound_to_current_hash_and_expiry():
 def test_login_page_links_to_password_reset():
     login_html = (Path(__file__).resolve().parents[1] /
                   "public" / "login.html").read_text(encoding="utf-8")
-    assert "/forgot-password.html" in login_html
+    assert "/forgot-password" in login_html
+    assert "/forgot-password.html" not in login_html
diff --git a/cloudflare_worker/tests/test_auth_pages_frontend.py b/cloudflare_worker/tests/test_auth_pages_frontend.py
index 37a40aed..7d2c7db6 100644
--- a/cloudflare_worker/tests/test_auth_pages_frontend.py
+++ b/cloudflare_worker/tests/test_auth_pages_frontend.py
@@ -81,8 +81,10 @@ def test_login_and_signup_cross_links_remain():
     login = _read(PUBLIC / "login.html")
     signup = _read(PUBLIC / "signup.html")
 
-    assert ('href="/signup.html"' in login) or ('href="/signup"' in login)
-    assert ('href="/login.html"' in signup) or ('href="/login"' in signup)
+    assert 'href="/signup"' in login
+    assert 'href="/login"' in signup
+    assert 'href="/signup.html"' not in login
+    assert 'href="/login.html"' not in signup
 
 
 def test_signup_card_is_centered_vertically_like_login():
@@ -200,7 +202,7 @@ def test_login_persists_returned_session_details():
 def test_login_has_forgot_password_link():
     login = _read(PUBLIC / "login.html")
 
-    assert 'href="/forgot-password.html"' in login
+    assert 'href="/forgot-password"' in login
     assert "Forgot" in login
 
 
@@ -210,7 +212,7 @@ def test_forgot_password_page_exists_and_links_back():
     assert 'id="identifier"' in forgot
     assert 'id="forgot-btn"' in forgot
     assert 'src="/forgot-password.js' in forgot
-    assert 'href="/login.html"' in forgot
+    assert 'href="/login"' in forgot
 
 
 def test_forgot_password_js_posts_to_api():
@@ -228,7 +230,7 @@ def test_reset_password_page_exists():
     assert 'id="confirm"' in reset
     assert 'id="reset-btn"' in reset
     assert 'src="/reset-password.js' in reset
-    assert 'href="/login.html"' in reset
+    assert 'href="/login"' in reset
 
 
 def test_reset_password_js_posts_to_api_and_redirects():
@@ -239,7 +241,7 @@ def test_reset_password_js_posts_to_api_and_redirects():
     assert "node" in reset_js
     assert "token" in reset_js
     assert "exp" in reset_js
-    assert 'location.href = "/login.html"' in reset_js
+    assert 'location.href = "/login"' in reset_js
 
 
 def test_worker_handles_forgot_and_reset_password_routes():
diff --git a/cloudflare_worker/tests/test_blog_frontend.py b/cloudflare_worker/tests/test_blog_frontend.py
index 4b3b9083..a80eca72 100644
--- a/cloudflare_worker/tests/test_blog_frontend.py
+++ b/cloudflare_worker/tests/test_blog_frontend.py
@@ -223,26 +223,25 @@ def test_blog_page_uses_blog_banner_assets():
         assert marker in html
 
 
-def test_blog_paths_are_owned_by_python_worker():
-    assert 'if url.path == "/blog":' in ENTRY_TEXT
-    assert 'if url.path == "/blog.html":' in ENTRY_TEXT
-    assert 'headers={"location": "/blogs"}' in ENTRY_TEXT
+def test_blog_paths_are_owned_by_redirects_not_python_worker():
+    assert 'if url.path == "/blog":' not in ENTRY_TEXT
+    assert 'if url.path == "/blog.html":' not in ENTRY_TEXT
     assert REDIRECTS.exists()
     redirects = _read(REDIRECTS)
     assert "/blogs /blog.html 200" in redirects
     assert "/blog /blogs 308" in redirects
-    assert "/blog.html /blogs 308" in redirects
+    assert "/blog.html /blogs 308" not in redirects
 
     run_worker_first = WRANGLER["assets"]["run_worker_first"]
     assert WRANGLER["assets"]["html_handling"] == "none"
-    assert "/blog" in run_worker_first
+    assert "/blog" not in run_worker_first
     assert "/blog.html" in run_worker_first
     assert "/blogs" not in run_worker_first
 
 
 if __name__ == "__main__":
     test_blog_page_uses_screenshot_editorial_shell()
-    test_blog_page_nav_only_links_login()
+    test_blog_page_nav_only_links_auth()
     test_blog_page_uses_homepage_logo_markup()
     test_blog_page_footer_keeps_only_twitter_social_link()
     test_blog_page_articles_open_in_page_detail_view()
@@ -253,4 +252,4 @@ if __name__ == "__main__":
     test_blog_interaction_states_use_neutral_dark_not_green()
     test_blog_search_result_copy_is_compact()
     test_blog_page_uses_blog_banner_assets()
-    test_blog_paths_are_owned_by_python_worker()
+    test_blog_paths_are_owned_by_redirects_not_python_worker()
diff --git a/cloudflare_worker/tests/test_dashboard_landing_migration.py b/cloudflare_worker/tests/test_dashboard_landing_migration.py
index 2774f283..c335b6db 100644
--- a/cloudflare_worker/tests/test_dashboard_landing_migration.py
+++ b/cloudflare_worker/tests/test_dashboard_landing_migration.py
@@ -1086,26 +1086,29 @@ def test_dashboard_deep_link_assets_binding_is_wired_up():
 def test_clean_marketing_routes_target_static_pages():
     for redirect in (
         "/dashboard /dashboard/index.html 200",
-        "/dashboard.html /dashboard 308",
         "/desktop /desktop.html 200",
         "/docs /docs/index.html 200",
+        "/docs/ /docs 308",
+        "/network /network.html 200",
+        "/network/ /network 308",
         "/blog /blogs 308",
     ):
         assert redirect in REDIRECTS
 
     run_worker_first = WRANGLER["assets"]["run_worker_first"]
-    for route in ("/blog", "/docs"):
-        assert route in run_worker_first
-    for route in ("/desktop", "/blogs"):
+    for route in ("/blog", "/docs", "/network", "/desktop", "/blogs"):
         assert route not in run_worker_first
+    for route in ("/dashboard.html", "/docs.html", "/network.html"):
+        assert route in run_worker_first
 
 
 def test_repo_shortcut_is_not_a_redirects_rule_so_assets_are_not_hijacked():
     # A /:owner/:repo rule in _redirects matches real two-segment static assets
     # (e.g. /assets/logo.png, /favicon/site.webmanifest) because Cloudflare always
     # applies _redirects before serving a matching static file — that 308'd those
-    # assets and broke the deploy's public-asset check. The shortcut must live in
-    # 404.html (post-asset-resolution) instead, so guard against the rule's return.
+    # assets and broke the deploy's public-asset check. The shortcut must be
+    # Worker-owned with explicit asset-prefix exceptions, so guard against the
+    # redirect rule's return.
     rules = [
         line for line in REDIRECTS.splitlines()
         if line.strip() and not line.lstrip().startswith("#")
@@ -1120,20 +1123,14 @@ def test_repo_shortcut_is_not_a_redirects_rule_so_assets_are_not_hijacked():
         assert not rule.startswith("/favicon/*")
 
 
-def test_repo_shortcut_urls_redirect_to_dashboard_from_404_page():
+def test_repo_shortcut_urls_are_worker_owned_not_404_page_scripted():
     html = _read(PUBLIC / "404.html")
 
-    # The 404 page bounces /owner/repo (and tree/blob deep links) to the dashboard.
-    # Feature tabs (/issues, /pulls, etc.) are preserved in the bounce.
-    assert '"/dashboard/"' in html
-    assert 'var dashboardPath = "/dashboard/' in html
-    assert "window.location.pathname.split" in html
-    assert 'parts[2] === "tree" || parts[2] === "blob"' in html
-    assert 'featureTabs' in html
-    assert '["commits", "releases", "issues", "pulls", "discussions", "mirrors"]' in html
-    # Real site sections must not be treated as repo owners.
-    for reserved in ("assets", "favicon", "dashboard", "docs", "blogs"):
-        assert '"%s"' % reserved in html
+    assert "window.location.replace" not in html
+    assert "window.location.pathname.split" not in html
+    assert 'var dashboardPath = "/dashboard/' not in html
+    assert "looks_like_repo_route" in ENTRY_TEXT
+    assert 'if looks_like_repo_route(url.path):' in ENTRY_TEXT
 
 
 def test_dashboard_restores_feature_tab_on_hard_refresh():
diff --git a/cloudflare_worker/tests/test_legal_pages_frontend.py b/cloudflare_worker/tests/test_legal_pages_frontend.py
index 3e33f978..3b8a5f6f 100644
--- a/cloudflare_worker/tests/test_legal_pages_frontend.py
+++ b/cloudflare_worker/tests/test_legal_pages_frontend.py
@@ -1,7 +1,6 @@
 #!/usr/bin/env python3
 """Static contracts for terms/privacy pages."""
 
-from html.parser import HTMLParser
 from pathlib import Path
 
 
@@ -12,39 +11,6 @@ LEGAL_PAGES = (
 )
 
 
-class PrimaryNavParser(HTMLParser):
-    def __init__(self):
-        super().__init__()
-        self._in_nav = False
-        self._depth = 0
-        self.links = []
-
-    def handle_starttag(self, tag, attrs):
-        attr_map = dict(attrs)
-        if tag == "nav" and attr_map.get("aria-label") == "Primary":
-            self._in_nav = True
-            self._depth = 1
-            return
-        if self._in_nav:
-            if tag not in {"br", "hr", "img", "input", "link", "meta"}:
-                self._depth += 1
-            if tag == "a":
-                self.links.append((attr_map.get("href"), ""))
-
-    def handle_data(self, data):
-        if self._in_nav and self.links and self.links[-1][1] == "":
-            href, text = self.links[-1]
-            self.links[-1] = (href, data.strip())
-
-    def handle_endtag(self, tag):
-        if not self._in_nav:
-            return
-        if tag not in {"br", "hr", "img", "input", "link", "meta"}:
-            self._depth -= 1
-        if self._depth <= 0:
-            self._in_nav = False
-
-
 def _read(page: Path) -> str:
     return page.read_text(encoding="utf-8")
 
@@ -52,33 +18,35 @@ def _read(page: Path) -> str:
 def test_legal_pages_use_404_style_simple_nav_only_docs_blog_login():
     for page in LEGAL_PAGES:
         html = _read(page)
-        parser = PrimaryNavParser()
-        parser.feed(html)
+        body = html[html.index("<body") :]
 
-        assert parser.links == [("/docs", "Docs"), ("/blogs", "Blog"), ("/login", "Login")]
+        assert 'href="/site-header.css"' in html
+        assert 'src="/site-header.js"' in html
+        assert '<div data-forkmesh-header="simple"></div>' in body
         assert 'class="site-header"' not in html
         assert 'class="global-nav"' not in html
+        assert 'aria-label="Primary"' not in body
         assert 'id="theme-toggle"' not in html
         assert 'class="mesh-bg"' in html
         assert 'class="mesh-grid"' in html
         assert 'class="legal-card' in html
 
 
-def test_legal_pages_include_landing_footer_markup():
+def test_legal_pages_include_standard_footer_mount():
     for page in LEGAL_PAGES:
         html = _read(page)
+        body = html[html.index("<body"):]
 
         for marker in (
-            'class="site-footer-word-wrap',
-            'class="site-footer-panel',
-            'class="site-footer-links-grid',
-            'class="site-footer-brand',
-            'class="site-footer-copyright',
-            'src="/assets/logo.png"',
-            '© 2026 ForkMesh. Built for local-first Git collaboration.',
+            'href="/site-footer.css"',
+            'src="/site-footer.js"',
+            '<div data-forkmesh-footer="standard"></div>',
         ):
             assert marker in html
 
+        assert "site-footer-links-grid" not in body
+        assert '<footer id="signup"' not in body
+
 
 def test_legal_pages_preserve_core_content_and_cross_links():
     terms = _read(PUBLIC / "terms.html")
@@ -94,5 +62,5 @@ def test_legal_pages_preserve_core_content_and_cross_links():
 
 if __name__ == "__main__":
     test_legal_pages_use_404_style_simple_nav_only_docs_blog_login()
-    test_legal_pages_include_landing_footer_markup()
+    test_legal_pages_include_standard_footer_mount()
     test_legal_pages_preserve_core_content_and_cross_links()
diff --git a/cloudflare_worker/tests/test_public_logo.py b/cloudflare_worker/tests/test_public_logo.py
index 9e34c067..b6d26283 100644
--- a/cloudflare_worker/tests/test_public_logo.py
+++ b/cloudflare_worker/tests/test_public_logo.py
@@ -67,6 +67,15 @@ def test_all_public_html_pages_use_logo_in_brand_link():
             html = assembled_dashboard()
         else:
             html = page.read_text(encoding="utf-8")
+        if '<div data-forkmesh-header="simple"></div>' in html:
+            header_js = (PUBLIC_DIR / "site-header.js").read_text(encoding="utf-8")
+            if (
+                'href="/"' in header_js
+                and 'src="/assets/logo.png"' in header_js
+                and 'aria-hidden="true"' in header_js
+            ):
+                continue
+
         parser = BrandLogoParser()
         parser.feed(html)
 
diff --git a/cloudflare_worker/tests/test_shared_header_frontend.py b/cloudflare_worker/tests/test_shared_header_frontend.py
new file mode 100644
index 00000000..3ab11876
--- /dev/null
+++ b/cloudflare_worker/tests/test_shared_header_frontend.py
@@ -0,0 +1,62 @@
+#!/usr/bin/env python3
+"""Static contracts for the shared simple page header."""
+
+from pathlib import Path
+
+
+PUBLIC = Path(__file__).resolve().parents[1] / "public"
+SIMPLE_HEADER_PAGES = (
+    PUBLIC / "features.html",
+    PUBLIC / "terms.html",
+    PUBLIC / "careers.html",
+    PUBLIC / "changelog.html",
+    PUBLIC / "privacy.html",
+    PUBLIC / "status.html",
+    PUBLIC / "desktop.html",
+)
+
+
+def _read(path: Path) -> str:
+    return path.read_text(encoding="utf-8")
+
+
+def test_shared_simple_header_mounts_on_requested_pages():
+    for page in SIMPLE_HEADER_PAGES:
+        html = _read(page)
+        body = html[html.index("<body") :]
+
+        assert 'href="/site-header.css"' in html, f"{page.name} missing header CSS"
+        assert 'src="/site-header.js"' in html, f"{page.name} missing header JS"
+        assert '<div data-forkmesh-header="simple"></div>' in body
+
+        assert 'class="site-header"' not in body
+        assert 'class="global-nav"' not in body
+        assert 'aria-label="Primary"' not in body
+        assert "<header" not in body
+
+
+def test_shared_simple_header_renderer_contains_about_header_contract():
+    js = _read(PUBLIC / "site-header.js")
+    css = _read(PUBLIC / "site-header.css")
+
+    for marker in (
+        'querySelectorAll("[data-forkmesh-header]")',
+        'aria-label="ForkMesh home"',
+        'src="/assets/logo.png"',
+        'aria-label="Primary"',
+        'href="/docs"',
+        ">Docs</a>",
+        'href="/blogs"',
+        ">Blog</a>",
+        'href="/login"',
+        ">Login</a>",
+    ):
+        assert marker in js
+
+    for marker in (
+        ".forkmesh-simple-header",
+        ".forkmesh-simple-header-nav",
+        ".forkmesh-simple-brand",
+        ".forkmesh-simple-brand-mark",
+    ):
+        assert marker in css
diff --git a/cloudflare_worker/tests/test_static_route_canonicalization.py b/cloudflare_worker/tests/test_static_route_canonicalization.py
new file mode 100644
index 00000000..41556851
--- /dev/null
+++ b/cloudflare_worker/tests/test_static_route_canonicalization.py
@@ -0,0 +1,212 @@
+#!/usr/bin/env python3
+"""Canonical route contracts for public static pages."""
+
+import re
+import sys
+import tomllib
+from pathlib import Path
+
+
+ROOT = Path(__file__).resolve().parents[1]
+PUBLIC = ROOT / "public"
+REDIRECTS = PUBLIC / "_redirects"
+WRANGLER = tomllib.loads((ROOT / "wrangler.toml").read_text(encoding="utf-8"))
+ENTRY_TEXT = (ROOT / "src" / "entry.py").read_text(encoding="utf-8")
+SRC = ROOT / "src"
+if str(SRC) not in sys.path:
+    sys.path.insert(0, str(SRC))
+
+import static_routes  # noqa: E402
+
+CANONICAL_PAGE_ROUTES = {
+    "/": "/index.html",
+    "/dashboard": "/dashboard/index.html",
+    "/blogs": "/blog.html",
+    "/login": "/login.html",
+    "/signup": "/signup.html",
+    "/forgot-password": "/forgot-password.html",
+    "/reset-password": "/reset-password.html",
+    "/mirror-payouts": "/mirror-payouts.html",
+    "/security-report": "/security-report.html",
+    "/docs": "/docs/index.html",
+    "/network": "/network.html",
+    "/chat": "/chat.html",
+    "/desktop": "/desktop.html",
+    "/about": "/about.html",
+    "/features": "/features.html",
+    "/careers": "/careers.html",
+    "/changelog": "/changelog.html",
+    "/privacy": "/privacy.html",
+    "/terms": "/terms.html",
+    "/status": "/status.html",
+}
+
+BLOCKED_HTML_ALIASES = {
+    "/index.html",
+    "/dashboard.html",
+    "/blog.html",
+    "/login.html",
+    "/signup.html",
+    "/forgot-password.html",
+    "/reset-password.html",
+    "/mirror-payouts.html",
+    "/security-report.html",
+    "/docs.html",
+    "/network.html",
+    "/chat.html",
+    "/desktop.html",
+    "/about.html",
+    "/features.html",
+    "/careers.html",
+    "/changelog.html",
+    "/privacy.html",
+    "/terms.html",
+    "/status.html",
+}
+
+NON_ROUTED_HTML_ASSETS = {
+    # Served by Cloudflare only for misses, not a navigable product route.
+    "404.html",
+    # Duplicate static copies kept for compatibility or source parity; their
+    # public routes are canonicalized to the index-backed routes above.
+    "dashboard.html",
+    "docs.html",
+    # Legacy blog pages are superseded by /blogs.
+    "blog/index.html",
+    "blog/introducing-forkmesh/index.html",
+    "network/index.html",
+}
+
+
+def _redirect_rules():
+    rules = []
+    for raw_line in REDIRECTS.read_text(encoding="utf-8").splitlines():
+        line = raw_line.strip()
+        if not line or line.startswith("#"):
+            continue
+        source, target, status = line.split()[:3]
+        rules.append((source, target, status))
+    return rules
+
+
+def test_public_redirects_have_one_canonical_route_per_public_html_page():
+    rules = _redirect_rules()
+    assert len({source for source, _, _ in rules}) == len(rules)
+
+    html_200_rules = {
+        source: target
+        for source, target, status in rules
+        if status == "200" and target.endswith(".html")
+    }
+
+    assert html_200_rules == CANONICAL_PAGE_ROUTES
+
+
+def test_every_public_html_file_is_accounted_for_by_routes_or_explicit_exclusions():
+    routed_assets = {target.lstrip("/") for target in CANONICAL_PAGE_ROUTES.values()}
+    html_assets = {
+        path.relative_to(PUBLIC).as_posix()
+        for path in PUBLIC.rglob("*.html")
+        if "/dashboard/partials/" not in path.as_posix()
+    }
+
+    assert html_assets == routed_assets | NON_ROUTED_HTML_ASSETS
+
+
+def test_html_asset_aliases_are_not_public_redirects():
+    for source, target, status in _redirect_rules():
+        if source.endswith(".html"):
+            raise AssertionError(
+                "%s should be a 404-only implementation file, not %s %s"
+                % (source, target, status)
+            )
+
+    assert static_routes.BLOCKED_STATIC_HTML_PATHS >= BLOCKED_HTML_ALIASES
+
+
+def test_internal_links_and_redirects_do_not_point_at_html_routes():
+    href_pattern = re.compile(r'\b(?:href|action)="(/[^"#?]+\.html)(?:[#?][^"]*)?"')
+    js_route_pattern = re.compile(
+        r'location\.(?:href|replace)\s*(?:=|\()\s*"(/[^"#?]+\.html)(?:[#?][^"]*)?"'
+    )
+    violations = []
+
+    migrated_pages = {
+        "404.html",
+        "blog.html",
+        "blog/index.html",
+        "blog/introducing-forkmesh/index.html",
+        "careers.html",
+        "changelog.html",
+        "desktop.html",
+        "docs.html",
+        "docs/index.html",
+        "features.html",
+        "forgot-password.html",
+        "login.html",
+        "privacy.html",
+        "reset-password.html",
+        "status.html",
+        "terms.html",
+    }
+
+    for path in sorted(PUBLIC.rglob("*.html")):
+        if "/dashboard/partials/" in path.as_posix():
+            continue
+        if path.relative_to(PUBLIC).as_posix() not in migrated_pages:
+            continue
+        for match in href_pattern.finditer(path.read_text(encoding="utf-8")):
+            violations.append(f"{path.relative_to(ROOT)} -> {match.group(1)}")
+
+    for path in sorted(PUBLIC.rglob("*.js")):
+        for match in js_route_pattern.finditer(path.read_text(encoding="utf-8")):
+            violations.append(f"{path.relative_to(ROOT)} -> {match.group(1)}")
+
+    assert violations == []
+
+
+def test_worker_does_not_own_static_page_alias_routes():
+    run_worker_first = WRANGLER["assets"]["run_worker_first"]
+
+    for route in ("/blog", "/blog.html", "/docs", "/docs.html", "/network", "/network.html"):
+        if route.endswith(".html"):
+            assert route in run_worker_first
+        else:
+            assert route not in run_worker_first
+
+    assert 'if url.path == "/blog.html":' not in ENTRY_TEXT
+    assert 'headers={"location": "/docs/"}' not in ENTRY_TEXT
+    assert 'headers={"location": "/network/"}' not in ENTRY_TEXT
+
+
+def test_repo_shortcuts_are_worker_owned_without_hijacking_static_assets():
+    run_worker_first = WRANGLER["assets"]["run_worker_first"]
+
+    assert "/*/*" in run_worker_first
+    for exception in ("/assets/*", "/favicon/*", "/blog/*", "/docs/*"):
+        assert "!" + exception in run_worker_first
+
+    assert "!/network/*" not in run_worker_first
+
+    assert static_routes.looks_like_repo_route("/kaif/forkmesh")
+    assert static_routes.looks_like_repo_route("/kaif/forkmesh/issues")
+    assert static_routes.looks_like_repo_route("/kaif/forkmesh/tree/src/main.py")
+    assert static_routes.looks_like_repo_route("/kaif/forkmesh/blob/README.md")
+
+    for path in (
+        "/assets/logo.png",
+        "/favicon/site.webmanifest",
+        "/docs/guide",
+        "/blog/introducing-forkmesh/index.html",
+        "/dashboard/partials/header.html",
+    ):
+        assert not static_routes.looks_like_repo_route(path)
+
+
+if __name__ == "__main__":
+    test_public_redirects_have_one_canonical_route_per_public_html_page()
+    test_every_public_html_file_is_accounted_for_by_routes_or_explicit_exclusions()
+    test_html_asset_aliases_are_not_public_redirects()
+    test_internal_links_and_redirects_do_not_point_at_html_routes()
+    test_worker_does_not_own_static_page_alias_routes()
+    test_repo_shortcuts_are_worker_owned_without_hijacking_static_assets()
diff --git a/cloudflare_worker/wrangler.toml b/cloudflare_worker/wrangler.toml
index 3d72dfa5..d56c6b5d 100644
--- a/cloudflare_worker/wrangler.toml
+++ b/cloudflare_worker/wrangler.toml
@@ -24,7 +24,39 @@ html_handling = "none"
 not_found_handling = "404-page"
 # /admin-* always hits the worker so the secret admin path (ADMIN_PATH) is
 # handled in code rather than served as a static asset.
-run_worker_first = ["/api/*", "/health", "/network", "/docs", "/docs.html", "/blog", "/blog.html", "/*/info/refs", "/*/git-upload-pack", "/admin-*", "/dashboard", "/dashboard.js", "/dashboard/*"]
+run_worker_first = [
+  "/api/*",
+  "/health",
+  "/admin-*",
+  "/dashboard",
+  "/dashboard.js",
+  "/dashboard/*",
+  "/index.html",
+  "/dashboard.html",
+  "/blog.html",
+  "/login.html",
+  "/signup.html",
+  "/forgot-password.html",
+  "/reset-password.html",
+  "/mirror-payouts.html",
+  "/security-report.html",
+  "/docs.html",
+  "/network.html",
+  "/chat.html",
+  "/desktop.html",
+  "/about.html",
+  "/features.html",
+  "/careers.html",
+  "/changelog.html",
+  "/privacy.html",
+  "/terms.html",
+  "/status.html",
+  "/*/*",
+  "!/assets/*",
+  "!/favicon/*",
+  "!/blog/*",
+  "!/docs/*",
+]
 
 [vars]
 NODE_NAME = "forkmesh-mainnode"
diff --git a/pulls/1/changes.patch b/pulls/1/changes.patch
deleted file mode 100644
index 67d15b44..00000000
--- a/pulls/1/changes.patch
+++ /dev/null
@@ -1,267 +0,0 @@
-diff --git a/qt_client/src/MainWindow.h b/qt_client/src/MainWindow.h
-index c066b25f..9bff5702 100644
---- a/qt_client/src/MainWindow.h
-+++ b/qt_client/src/MainWindow.h
-@@ -291,6 +291,12 @@ public:
-                    ? QString()
-                    : repoAgentGitDir(m_repositories.at(index));
-     }
-+    void testSetActiveAccountSession(const QString &accountName)
-+    {
-+        m_accountName = accountName;
-+        m_accountAuthenticated = true;
-+        m_accountTier = QStringLiteral("active");
-+    }
-     void testPublishRepository(int index) { publishRepository(index, false); }
-     void testStartRepoHosts() { startRepoHosts(); }
-     void testStopRepoHosts() { stopRepoHosts(); }
-@@ -2543,6 +2549,8 @@ private:
-     void changeMirrorLocation();
-     void changePreviewCacheLocation();
-     void publishRepository(int index, bool showDialogOnError = true);
-+    void publishRepositoryWithRootCommit(int index, bool showDialogOnError,
-+                                         const QString &rootCommit);
-     void updateRepoActionMenus();
-     void deleteCurrentMirror();
-     void updateRepoDetailStatus();
-diff --git a/qt_client/src/MainWindowRepos.cpp b/qt_client/src/MainWindowRepos.cpp
-index f627f708..6988fb42 100644
---- a/qt_client/src/MainWindowRepos.cpp
-+++ b/qt_client/src/MainWindowRepos.cpp
-@@ -1864,6 +1864,51 @@ void MainWindow::saveRepoStats() const
-                          QJsonDocument(obj).toJson(QJsonDocument::Compact));
- }
- 
-+namespace {
-+
-+QString resolvePublishRootCommit(const QString &gitDir)
-+{
-+    // Resolve the earliest root commit. Prefer HEAD; but a bare mirror cloned
-+    // from the relay can carry an unset/dangling HEAD (the relay serves
-+    // git-upload-pack without advertising a symref HEAD), so "rev-list ... HEAD"
-+    // fails and leaves rootCommit empty. A wrong/empty root drops that mirror
-+    // into a different group key (worker repo_mirror_group_key), so the owner's
-+    // mirror-nodes panel never lists it next to the source of truth — the node
-+    // shows up on the mirror but not on the source (issue #243, adhoc #134).
-+    auto firstRoot = [&](const QStringList &args) -> QString {
-+        QByteArray out;
-+        if (gitDir.trimmed().isEmpty() ||
-+            !runGitCapture(gitDir, args, &out, nullptr))
-+            return QString();
-+        const QStringList roots =
-+            QString::fromUtf8(out).split('\n', Qt::SkipEmptyParts);
-+        return roots.isEmpty() ? QString() : roots.last().trimmed();
-+    };
-+
-+    QString rootCommit = firstRoot({"rev-list", "--max-parents=0", "HEAD"});
-+    if (rootCommit.isEmpty()) {
-+        // HEAD is unset. Compute the root of the branch this mirror actually
-+        // serves (mirrorHeadBranch: the same default branch the source's HEAD
-+        // points to), NOT a blind "--all" walk. This repo has several root
-+        // commits — one per independent history the mirror also holds (agent
-+        // branches, imported subtrees). "rev-list --max-parents=0 --all" returns
-+        // ALL of them and "roots.last()" would pick whichever an unrelated
-+        // history contributes, so the mirror published a different rootCommit
-+        // than the source and landed in its own group (adhoc #134). Resolving
-+        // the served branch yields the source's root regardless of HEAD's state.
-+        const QString branch = mirrorHeadBranch(gitDir);
-+        if (!branch.isEmpty())
-+            rootCommit = firstRoot({"rev-list", "--max-parents=0", branch});
-+    }
-+    // Last resort (no HEAD and no resolvable served branch, e.g. a truly empty
-+    // ref set): fall back to --all so a single-root mirror still groups.
-+    if (rootCommit.isEmpty())
-+        rootCommit = firstRoot({"rev-list", "--max-parents=0", "--all"});
-+    return rootCommit;
-+}
-+
-+} // namespace
-+
- void MainWindow::publishRepository(int index, bool showDialogOnError)
- {
-     if (index < 0 || index >= m_repositories.size())
-@@ -1890,59 +1935,60 @@ void MainWindow::publishRepository(int index, bool showDialogOnError)
-         return;
-     }
- 
--    const qint64 now = QDateTime::currentMSecsSinceEpoch();
-     // Always publish under the registered account name so the catalog dedups by
-     // account/name (one entry per fork) and the server can verify ownership.
-     // catalogOwner() is shared with the live host tunnel so the website browses
-     // the same owner the host registers under.
-     const QString owner = catalogOwner(repo);
-     const QString name = repoSegment(repo.name, QStringLiteral("repository"));
--    const QString updatedAt = QString::number(now);
-     // A stable identity for the logical repo: its first (root) commit, shared by
-     // every node mirroring it. The network page groups mirrors by this so the same
-     // repo under different owners shows as one card. Not part of the signature.
--    QString rootCommit;
--    {
--        const QString gitDir =
--            (!repo.localPath.trimmed().isEmpty() && QDir(repo.localPath).exists(".git"))
--                ? repo.localPath
--                : repo.mirrorPath;
--        // Resolve the earliest root commit. Prefer HEAD; but a bare mirror cloned
--        // from the relay can carry an unset/dangling HEAD (the relay serves
--        // git-upload-pack without advertising a symref HEAD), so "rev-list ... HEAD"
--        // fails and leaves rootCommit empty. A wrong/empty root drops that mirror
--        // into a different group key (worker repo_mirror_group_key), so the owner's
--        // mirror-nodes panel never lists it next to the source of truth — the node
--        // shows up on the mirror but not on the source (issue #243, adhoc #134).
--        auto firstRoot = [&](const QStringList &args) -> QString {
--            QByteArray out;
--            if (gitDir.trimmed().isEmpty() ||
--                !runGitCapture(gitDir, args, &out, nullptr))
--                return QString();
--            const QStringList roots =
--                QString::fromUtf8(out).split('\n', Qt::SkipEmptyParts);
--            return roots.isEmpty() ? QString() : roots.last().trimmed();
--        };
--        rootCommit = firstRoot({"rev-list", "--max-parents=0", "HEAD"});
--        if (rootCommit.isEmpty()) {
--            // HEAD is unset. Compute the root of the branch this mirror actually
--            // serves (mirrorHeadBranch: the same default branch the source's HEAD
--            // points to), NOT a blind "--all" walk. This repo has several root
--            // commits — one per independent history the mirror also holds (agent
--            // branches, imported subtrees). "rev-list --max-parents=0 --all" returns
--            // ALL of them and "roots.last()" would pick whichever an unrelated
--            // history contributes, so the mirror published a different rootCommit
--            // than the source and landed in its own group (adhoc #134). Resolving
--            // the served branch yields the source's root regardless of HEAD's state.
--            const QString branch = mirrorHeadBranch(gitDir);
--            if (!branch.isEmpty())
--                rootCommit = firstRoot({"rev-list", "--max-parents=0", branch});
--        }
--        // Last resort (no HEAD and no resolvable served branch, e.g. a truly empty
--        // ref set): fall back to --all so a single-root mirror still groups.
--        if (rootCommit.isEmpty())
--            rootCommit = firstRoot({"rev-list", "--max-parents=0", "--all"});
-+    const QString gitDir =
-+        (!repo.localPath.trimmed().isEmpty() && QDir(repo.localPath).exists(".git"))
-+            ? repo.localPath
-+            : repo.mirrorPath;
-+    const QString localPath = repo.localPath;
-+    const QString mirrorPath = repo.mirrorPath;
-+    auto rootCommit = std::make_shared<QString>();
-+    QThread *rootLookup = QThread::create([gitDir, rootCommit] {
-+        *rootCommit = resolvePublishRootCommit(gitDir);
-+    });
-+    connect(rootLookup, &QThread::finished, rootLookup, &QObject::deleteLater);
-+    connect(rootLookup, &QThread::finished, this,
-+            [this, index, showDialogOnError, owner, name, localPath, mirrorPath,
-+             rootCommit] {
-+                if (index < 0 || index >= m_repositories.size())
-+                    return;
-+                const RepositoryRecord &repo = m_repositories.at(index);
-+                if (catalogOwner(repo) != owner ||
-+                    repoSegment(repo.name, QStringLiteral("repository")) != name ||
-+                    repo.localPath != localPath || repo.mirrorPath != mirrorPath)
-+                    return;
-+                publishRepositoryWithRootCommit(index, showDialogOnError, *rootCommit);
-+            });
-+    rootLookup->start();
-+}
-+
-+void MainWindow::publishRepositoryWithRootCommit(int index, bool showDialogOnError,
-+                                                 const QString &rootCommit)
-+{
-+    if (index < 0 || index >= m_repositories.size())
-+        return;
-+    if (m_repositories.at(index).previewOnly)
-+        return;
-+    RepositoryRecord &repo = m_repositories[index];
-+    if (!hasActiveAccountSession())
-+        return;
-+    if (!m_profileIdentity.isValid() && !m_profileIdentity.load()) {
-+        logSystem("Catalog: could not load identity for repository publishing.");
-+        return;
-     }
-+
-+    const qint64 now = QDateTime::currentMSecsSinceEpoch();
-+    const QString owner = catalogOwner(repo);
-+    const QString name = repoSegment(repo.name, QStringLiteral("repository"));
-+    const QString updatedAt = QString::number(now);
-     // Owner-signed fingerprint of the refs this node serves (sha256 over the
-     // canonical heads+tags advertisement). The relay pins this and refuses to
-     // serve any mirror whose live advertisement doesn't hash to it, so a tampered
-@@ -3018,4 +3064,3 @@ void MainWindow::uninstallForkMesh()
- 
-     QCoreApplication::exit(0);
- }
--
-diff --git a/qt_client/tests/test_window_resize.cpp b/qt_client/tests/test_window_resize.cpp
-index 025d803a..74b038a8 100644
---- a/qt_client/tests/test_window_resize.cpp
-+++ b/qt_client/tests/test_window_resize.cpp
-@@ -17,6 +17,7 @@
- #include <QPushButton>
- #include <QSettings>
- #include <QStandardPaths>
-+#include <QTextStream>
- #include <QTemporaryDir>
- #include <QWidget>
- 
-@@ -177,6 +178,26 @@ bool initGitRepo(QTemporaryDir &repo)
-            runGitChecked(repo.path(), {"commit", "--allow-empty", "-m", "initial"});
- }
- 
-+bool writeSlowGitWrapper(const QString &dir, const QString &realGit)
-+{
-+    QDir().mkpath(dir);
-+    QFile file(QDir(dir).filePath(QStringLiteral("git")));
-+    if (!file.open(QIODevice::WriteOnly | QIODevice::Truncate | QIODevice::Text))
-+        return false;
-+    QTextStream stream(&file);
-+    stream << "#!/bin/sh\n"
-+           << "if [ \"$1\" = \"-C\" ] && [ \"$3\" = \"rev-list\" ] && "
-+              "[ \"$4\" = \"--max-parents=0\" ]; then\n"
-+           << "  sleep 1\n"
-+           << "fi\n"
-+           << "exec \"" << realGit << "\" \"$@\"\n";
-+    file.close();
-+    return file.setPermissions(QFile::ReadOwner | QFile::WriteOwner |
-+                               QFile::ExeOwner | QFile::ReadGroup |
-+                               QFile::ExeGroup | QFile::ReadOther |
-+                               QFile::ExeOther);
-+}
-+
- MemberInfo testMember(const QString &id, const QString &name, bool self = false)
- {
-     MemberInfo member;
-@@ -880,6 +901,36 @@ int main(int argc, char *argv[])
-         }
-     }
- 
-+    // adhoc: publishing must not block the GUI thread while walking the repo's
-+    // root commit. Force `git rev-list --max-parents=0 HEAD` to sleep and assert
-+    // that the publish call returns before that subprocess can finish.
-+    {
-+        QTemporaryDir publishRepo;
-+        QTemporaryDir fakeBin;
-+        const QString realGit = QStandardPaths::findExecutable(QStringLiteral("git"));
-+        if (initGitRepo(publishRepo) && fakeBin.isValid() && !realGit.isEmpty() &&
-+            writeSlowGitWrapper(fakeBin.path(), realGit)) {
-+            window.testSetActiveAccountSession(QStringLiteral("me"));
-+            const int idx =
-+                window.testAddLocalRepository("me", "publishrepo", publishRepo.path());
-+            const QByteArray oldPath = qgetenv("PATH");
-+            qputenv("PATH",
-+                    (fakeBin.path() + QLatin1Char(':') +
-+                     QString::fromUtf8(oldPath))
-+                        .toUtf8());
-+
-+            QElapsedTimer timer;
-+            timer.start();
-+            window.testPublishRepository(idx);
-+            const qint64 elapsedMs = timer.elapsed();
-+            qputenv("PATH", oldPath);
-+
-+            check(elapsedMs < 500,
-+                  QStringLiteral("publishing returns without waiting for root "
-+                                 "commit git walk"));
-+        }
-+    }
-+
-     // issue #251: the Settings "Default agent" choice should seed the agent
-     // pickers. A window built while the default is Claude Code must start both
-     // the quick-add and issue-detail pickers there (not the OpenAI fallback),
diff --git a/pulls/1/pull.md b/pulls/1/pull.md
deleted file mode 100644
index 3050bf3d..00000000
--- a/pulls/1/pull.md
+++ /dev/null
@@ -1,14 +0,0 @@
----
-schema: forkmesh-pull-v1
-number: 1
-title: Agent: ForkMesh's GUI thread stalled for ~4397 ms — the event loop was blocked, whic…
-base: main
-head: agent/adhoc-76-forkmesh-s-gui-thread-stalled-for-4397-ms-the-ev
-status: open
-ts: 1783156390360
-author: F4rlozoS0ls2-Evs6Q7mLsSsZR1TVwMAsan0tdgaJoM
-authorName: mdkaifansari04
-sig: UhOUl7wDd5EfEQ741WtA-8rpRAs7OopbEs87nHPrFNGX2v39ONv4mbsrlOYJZWpIq6EBtZkUPA99vPJmNIxZBg
----
-
-Created from a OpenAI API agent session.
diff --git a/pulls/2/changes.patch b/pulls/2/changes.patch
deleted file mode 100644
index 0e5d1867..00000000
--- a/pulls/2/changes.patch
+++ /dev/null
@@ -1,259 +0,0 @@
-diff --git a/qt_client/src/MainWindow.h b/qt_client/src/MainWindow.h
-index c066b25f..c8dc75c4 100644
---- a/qt_client/src/MainWindow.h
-+++ b/qt_client/src/MainWindow.h
-@@ -394,6 +394,13 @@ public:
-     }
-     QString testAgentStatusCellText(int sessionId) const;
-     bool testAgentSessionMerged(int sessionId) const;
-+    void testSetBackend(ChatBackend *backend)
-+    {
-+        m_backend = backend;
-+        m_mirrorAdvertSig.clear();
-+        m_mirrorAdvertBuildSig.clear();
-+    }
-+    void testRefreshRepositoryList() { refreshRepositoryList(); }
- #endif
- 
-     // --- Headless / CLI support (HeadlessConsole) ------------------------------
-@@ -4215,6 +4222,7 @@ private:
-     // onRequestServed (adhoc #83); skip the rebuild while the signature is
-     // unchanged. Reset by attachBackend so a freshly attached backend is re-pushed.
-     QString m_mirrorAdvertSig;
-+    QString m_mirrorAdvertBuildSig;
-     int m_repoOpenPending = -1;        // repo index queued by openRepoDetailDeferred
-     // True while a user-driven repo load (a node switch or opening a repo) runs,
-     // so nodeSwitchStep narrates progress for both, not just node switches.
-diff --git a/qt_client/src/MainWindowRepos.cpp b/qt_client/src/MainWindowRepos.cpp
-index f627f708..16799b61 100644
---- a/qt_client/src/MainWindowRepos.cpp
-+++ b/qt_client/src/MainWindowRepos.cpp
-@@ -473,44 +473,76 @@ void MainWindow::refreshRepositoryList()
-                         .toMSecsSinceEpoch()) +
-                 QLatin1Char('\n');
-         }
--        if (advertSig != m_mirrorAdvertSig) {
--        GitKeepAlive keepAlive;
--        QList<MirrorAdvert> ours;
--        for (const RepositoryRecord &repo : std::as_const(m_repositories)) {
--            if (repo.previewOnly)
--                continue;
--            MirrorAdvert advert;
--            advert.ownerName = catalogOwner(repo) + "/" +
--                               repoSegment(repo.name, QStringLiteral("repository"));
--            // Shared upstream identity: every node mirroring the same source repo
--            // carries the same "<sourceOwner>/name", so the mirror-nodes view can
--            // group them even though each advertises its own clone (catalog) owner.
--            advert.source = repoSegment(repo.owner, QStringLiteral("owner")) + "/" +
-+        if (advertSig != m_mirrorAdvertSig &&
-+            advertSig != m_mirrorAdvertBuildSig) {
-+            m_mirrorAdvertBuildSig = advertSig;
-+            const QList<RepositoryRecord> repos = m_repositories;
-+            const QString account = accountOwner();
-+            QPointer<ChatBackend> backend = m_backend;
-+            runOffThread<QList<MirrorAdvert>>(
-+                [repos, account] {
-+                    QList<MirrorAdvert> ours;
-+                    for (const RepositoryRecord &repo : repos) {
-+                        if (repo.previewOnly)
-+                            continue;
-+                        MirrorAdvert advert;
-+                        const QString owner = account.isEmpty()
-+                                                  ? repoSegment(
-+                                                        repo.owner,
-+                                                        QStringLiteral("owner"))
-+                                                  : account;
-+                        advert.ownerName =
-+                            owner + "/" +
-                             repoSegment(repo.name, QStringLiteral("repository"));
--            // Advertise the HEAD this node currently holds so peers can see how
--            // fresh our mirror is relative to theirs.
--            advert.branch = mirrorHeadBranch(repo.mirrorPath);
--            advert.commit = mirrorBranchCommit(repo.mirrorPath, advert.branch);
--            advert.updatedMs = repo.lastSyncMs;
--            // On-disk mirror size so peers can show how much data we're holding.
--            advert.sizeBytes = mirrorRepoSizeBytes(repo.mirrorPath);
--            // Issues we're mirroring, so peers can show the count per node.
--            advert.issueCount = mirrorIssueCount(repo.mirrorPath, advert.branch);
--            // More tallies the Mirror nodes view shows per node: history depth,
--            // branch/PR/discussion counts, and our live worktree (agent task) count.
--            advert.commitCount = mirrorCommitCount(repo.mirrorPath, advert.branch);
--            advert.branchCount = mirrorBranchCount(repo.mirrorPath);
--            advert.pullCount = mirrorPullCount(repo.mirrorPath, advert.branch);
--            advert.discussionCount =
--                mirrorDiscussionCount(repo.mirrorPath, advert.branch);
--            advert.worktreeCount = mirrorWorktreeCount(repo.localPath);
--            // Release artifacts we're actually hosting for download (issue #304 CAS
--            // blobs), so peers can see which nodes can serve a binary.
--            advert.artifactCount = mirrorArtifactCount(repo.mirrorPath);
--            ours.append(advert);
--        }
--        m_backend->setMirroredRepos(ours);
--        m_mirrorAdvertSig = advertSig;
-+                        // Shared upstream identity: every node mirroring the same
-+                        // source repo carries the same "<sourceOwner>/name", so
-+                        // the mirror-nodes view can group them even though each
-+                        // advertises its own clone (catalog) owner.
-+                        advert.source =
-+                            repoSegment(repo.owner, QStringLiteral("owner")) + "/" +
-+                            repoSegment(repo.name, QStringLiteral("repository"));
-+                        // Advertise the HEAD this node currently holds so peers can
-+                        // see how fresh our mirror is relative to theirs.
-+                        advert.branch = mirrorHeadBranch(repo.mirrorPath);
-+                        advert.commit =
-+                            mirrorBranchCommit(repo.mirrorPath, advert.branch);
-+                        advert.updatedMs = repo.lastSyncMs;
-+                        // On-disk mirror size so peers can show how much data
-+                        // we're holding. This shells out to git count-objects and
-+                        // must stay off the GUI thread.
-+                        advert.sizeBytes = mirrorRepoSizeBytes(repo.mirrorPath);
-+                        // Issues we're mirroring, so peers can show the count per
-+                        // node.
-+                        advert.issueCount =
-+                            mirrorIssueCount(repo.mirrorPath, advert.branch);
-+                        // More tallies the Mirror nodes view shows per node:
-+                        // history depth, branch/PR/discussion counts, and our live
-+                        // worktree (agent task) count.
-+                        advert.commitCount =
-+                            mirrorCommitCount(repo.mirrorPath, advert.branch);
-+                        advert.branchCount = mirrorBranchCount(repo.mirrorPath);
-+                        advert.pullCount =
-+                            mirrorPullCount(repo.mirrorPath, advert.branch);
-+                        advert.discussionCount =
-+                            mirrorDiscussionCount(repo.mirrorPath, advert.branch);
-+                        advert.worktreeCount = mirrorWorktreeCount(repo.localPath);
-+                        // Release artifacts we're actually hosting for download
-+                        // (issue #304 CAS blobs), so peers can see which nodes can
-+                        // serve a binary.
-+                        advert.artifactCount = mirrorArtifactCount(repo.mirrorPath);
-+                        ours.append(advert);
-+                    }
-+                    return ours;
-+                },
-+                [this, advertSig, backend](QList<MirrorAdvert> ours) {
-+                    if (m_mirrorAdvertBuildSig == advertSig)
-+                        m_mirrorAdvertBuildSig.clear();
-+                    if (!backend || backend != m_backend ||
-+                        m_mirrorAdvertSig == advertSig)
-+                        return;
-+                    m_backend->setMirroredRepos(ours);
-+                    m_mirrorAdvertSig = advertSig;
-+                });
-         }
-     }
- }
-@@ -3018,4 +3050,3 @@ void MainWindow::uninstallForkMesh()
- 
-     QCoreApplication::exit(0);
- }
--
-diff --git a/qt_client/src/MainWindowSettings.cpp b/qt_client/src/MainWindowSettings.cpp
-index cce14f9a..2f5d3188 100644
---- a/qt_client/src/MainWindowSettings.cpp
-+++ b/qt_client/src/MainWindowSettings.cpp
-@@ -1815,6 +1815,7 @@ void MainWindow::attachBackend(ChatBackend *backend)
-     // Force refreshRepositoryList to re-push the mirror adverts to this backend even
-     // if the repo signature hasn't changed since the last one (adhoc #83 skip cache).
-     m_mirrorAdvertSig.clear();
-+    m_mirrorAdvertBuildSig.clear();
- 
-     connect(backend, &ChatBackend::messageArrived, this, &MainWindow::onMessage);
-     connect(backend, &ChatBackend::reactionChanged, this, &MainWindow::onReaction);
-@@ -3277,4 +3278,3 @@ void MainWindow::submitVulnerabilityReport()
-         }
-     });
- }
--
-diff --git a/qt_client/tests/test_window_resize.cpp b/qt_client/tests/test_window_resize.cpp
-index 025d803a..30d66cff 100644
---- a/qt_client/tests/test_window_resize.cpp
-+++ b/qt_client/tests/test_window_resize.cpp
-@@ -198,6 +198,33 @@ void stopChildProcesses(QObject &root)
-     }
- }
- 
-+class FakeBackend : public ChatBackend
-+{
-+public:
-+    using ChatBackend::ChatBackend;
-+
-+    void sendChat(const QString &, const QString &) override {}
-+    void sendDirect(const QString &, const QString &) override {}
-+    void sendFile(const QString &, const QString &, const QString &,
-+                  const QByteArray &) override {}
-+    void sendReaction(const QString &, const QString &, const QString &) override {}
-+    void editMessage(const QString &, const QString &, const QString &) override {}
-+    void deleteMessage(const QString &, const QString &) override {}
-+    void setAvatar(const QByteArray &) override {}
-+    void sendTyping(const QString &, bool) override {}
-+    void addChannel(const QString &) override {}
-+    void setMirroredRepos(const QList<MirrorAdvert> &repos) override
-+    {
-+        adverts = repos;
-+        ++setCalls;
-+    }
-+    void shutdown() override {}
-+    QString modeName() const override { return QStringLiteral("fake"); }
-+
-+    QList<MirrorAdvert> adverts;
-+    int setCalls = 0;
-+};
-+
- } // namespace
- 
- int main(int argc, char *argv[])
-@@ -308,6 +335,54 @@ int main(int argc, char *argv[])
-               QStringLiteral("quick update pulls origin/HEAD in detached HEAD"));
-     }
- 
-+    {
-+        QTemporaryDir mirrorDir;
-+        QTemporaryDir fakeBin;
-+        if (mirrorDir.isValid() && fakeBin.isValid()) {
-+            QFile git(fakeBin.path() + QStringLiteral("/git"));
-+            const bool scriptOk = git.open(QIODevice::WriteOnly);
-+            if (scriptOk) {
-+                git.write("#!/bin/sh\n"
-+                          "if [ \"$3\" = count-objects ]; then\n"
-+                          "  sleep 1\n"
-+                          "  printf 'size: 1\\nsize-pack: 2\\n'\n"
-+                          "  exit 0\n"
-+                          "fi\n"
-+                          "case \"$3\" in\n"
-+                          "  symbolic-ref) printf 'main\\n' ;;\n"
-+                          "  rev-parse) printf '0123456789abcdef0123456789abcdef01234567\\n' ;;\n"
-+                          "  rev-list) printf '1\\n' ;;\n"
-+                          "  for-each-ref) ;;\n"
-+                          "  *) ;;\n"
-+                          "esac\n"
-+                          "exit 0\n");
-+                git.close();
-+                QFile::setPermissions(git.fileName(),
-+                                      QFileDevice::ReadOwner |
-+                                          QFileDevice::WriteOwner |
-+                                          QFileDevice::ExeOwner);
-+            }
-+
-+            const QByteArray oldPath = qgetenv("PATH");
-+            qputenv("PATH", fakeBin.path().toUtf8() + ":" + oldPath);
-+            FakeBackend backend;
-+            MainWindow advertWindow;
-+            advertWindow.testSetBackend(&backend);
-+            advertWindow.testAddPublishedRepository(QStringLiteral("me"),
-+                                                    QStringLiteral("slowrepo"),
-+                                                    mirrorDir.path());
-+            QElapsedTimer timer;
-+            timer.start();
-+            advertWindow.testRefreshRepositoryList();
-+            const qint64 elapsedMs = timer.elapsed();
-+            qputenv("PATH", oldPath);
-+            advertWindow.testSetBackend(nullptr);
-+            check(elapsedMs < 500,
-+                  QStringLiteral("refreshRepositoryList does not wait for slow "
-+                                 "git count-objects"));
-+        }
-+    }
-+
-     // Issue #214: "Build & preview" checks the PR head out into a throwaway
-     // worktree, then CMake-configures and builds it. A fresh worktree is
-     // registered with `git worktree add`; a reused one is moved with a forced
diff --git a/pulls/2/pull.md b/pulls/2/pull.md
deleted file mode 100644
index f0aa5217..00000000
--- a/pulls/2/pull.md
+++ /dev/null
@@ -1,14 +0,0 @@
----
-schema: forkmesh-pull-v1
-number: 2
-title: Agent: ForkMesh's GUI thread stalled for ~1686 ms — the event loop was blocked, whic…
-base: main
-head: agent/adhoc-78-forkmesh-s-gui-thread-stalled-for-1686-ms-the-ev
-status: open
-ts: 1783156425694
-author: F4rlozoS0ls2-Evs6Q7mLsSsZR1TVwMAsan0tdgaJoM
-authorName: mdkaifansari04
-sig: SsPjCLzyB8IiYH7tLMUvxcEcCkLL1WckCxRVyrS1yzhBWyX_K7OxuLJR0xZzPT_h7sJ6xreFgWZQSH0ZzpNMDA
----
-
-Created from a OpenAI API agent session.
diff --git a/pulls/3/changes.patch b/pulls/3/changes.patch
deleted file mode 100644
index f4f292f5..00000000
--- a/pulls/3/changes.patch
+++ /dev/null
@@ -1,280 +0,0 @@
-diff --git a/qt_client/src/MainWindowRepos.cpp b/qt_client/src/MainWindowRepos.cpp
-index f627f708..5d7c67ec 100644
---- a/qt_client/src/MainWindowRepos.cpp
-+++ b/qt_client/src/MainWindowRepos.cpp
-@@ -10,6 +10,116 @@
- 
- using namespace forkmesh::ui;
- 
-+namespace {
-+
-+struct PublishRepositorySnapshot {
-+    QString rootCommit;
-+    QString stateHash;
-+    QString publishedWebsite;
-+    QString publishedDescription;
-+    QString headBranch;
-+    QString headCommit;
-+    int issueCount = -1;
-+    int commitCount = -1;
-+    int branchCount = -1;
-+    int pullCount = -1;
-+    int discussionCount = -1;
-+    int worktreeCount = -1;
-+    int artifactCount = -1;
-+    qint64 sizeBytes = 0;
-+};
-+
-+QString hashPublishedRefs(const QByteArray &out)
-+{
-+    QStringList lines;
-+    const QStringList rows = QString::fromUtf8(out).split('\n', Qt::SkipEmptyParts);
-+    for (const QString &raw : rows) {
-+        const QString line = raw.trimmed();
-+        if (line.isEmpty() || line.endsWith(QStringLiteral("^{}")))
-+            continue;
-+        lines.append(line);
-+    }
-+    lines.sort();
-+    return QString::fromUtf8(
-+        QCryptographicHash::hash(lines.join('\n').toUtf8(),
-+                                 QCryptographicHash::Sha256)
-+            .toHex());
-+}
-+
-+QString publishRootCommit(const RepositoryRecord &repo)
-+{
-+    const QString gitDir =
-+        (!repo.localPath.trimmed().isEmpty() && QDir(repo.localPath).exists(".git"))
-+            ? repo.localPath
-+            : repo.mirrorPath;
-+    auto firstRoot = [&](const QStringList &args) -> QString {
-+        QByteArray out;
-+        if (gitDir.trimmed().isEmpty() || !runGitCapture(gitDir, args, &out, nullptr))
-+            return QString();
-+        const QStringList roots =
-+            QString::fromUtf8(out).split('\n', Qt::SkipEmptyParts);
-+        return roots.isEmpty() ? QString() : roots.last().trimmed();
-+    };
-+    QString rootCommit = firstRoot({"rev-list", "--max-parents=0", "HEAD"});
-+    if (rootCommit.isEmpty()) {
-+        const QString branch = mirrorHeadBranch(gitDir);
-+        if (!branch.isEmpty())
-+            rootCommit = firstRoot({"rev-list", "--max-parents=0", branch});
-+    }
-+    if (rootCommit.isEmpty())
-+        rootCommit = firstRoot({"rev-list", "--max-parents=0", "--all"});
-+    return rootCommit;
-+}
-+
-+QString publishStateHash(const QString &mirrorPath)
-+{
-+    if (mirrorPath.trimmed().isEmpty())
-+        return QString();
-+    QByteArray out;
-+    if (!runGitCapture(mirrorPath,
-+                       {"for-each-ref", "--format=%(objectname) %(refname)",
-+                        "refs/heads/", "refs/tags/"},
-+                       &out, nullptr))
-+        return QString();
-+    return hashPublishedRefs(out);
-+}
-+
-+PublishRepositorySnapshot collectPublishRepositorySnapshot(
-+    const RepositoryRecord &repo)
-+{
-+    PublishRepositorySnapshot snapshot;
-+    snapshot.rootCommit = publishRootCommit(repo);
-+    snapshot.stateHash = publishStateHash(repo.mirrorPath);
-+    snapshot.publishedDescription = repo.description;
-+    if (!repo.localPath.trimmed().isEmpty()) {
-+        QFile file(QDir(repo.localPath).filePath(kRepoInfoPath));
-+        if (file.open(QIODevice::ReadOnly)) {
-+            const QJsonObject info = QJsonDocument::fromJson(file.readAll()).object();
-+            snapshot.publishedWebsite =
-+                info.value(QStringLiteral("website")).toString();
-+            const QString about =
-+                info.value(QStringLiteral("about")).toString().trimmed();
-+            if (!about.isEmpty())
-+                snapshot.publishedDescription = about;
-+        }
-+    }
-+
-+    snapshot.headBranch = mirrorHeadBranch(repo.mirrorPath);
-+    snapshot.headCommit = mirrorBranchCommit(repo.mirrorPath, snapshot.headBranch);
-+    snapshot.issueCount = mirrorIssueCount(repo.mirrorPath, snapshot.headBranch);
-+    snapshot.commitCount = mirrorCommitCount(repo.mirrorPath, snapshot.headBranch);
-+    snapshot.branchCount = mirrorBranchCount(repo.mirrorPath);
-+    snapshot.pullCount = mirrorPullCount(repo.mirrorPath, snapshot.headBranch);
-+    snapshot.discussionCount =
-+        mirrorDiscussionCount(repo.mirrorPath, snapshot.headBranch);
-+    snapshot.worktreeCount = mirrorWorktreeCount(repo.localPath);
-+    snapshot.artifactCount = mirrorArtifactCount(repo.mirrorPath);
-+    snapshot.sizeBytes = mirrorRepoSizeBytes(repo.mirrorPath);
-+    return snapshot;
-+}
-+
-+} // namespace
-+
- // ------------------------------------------------------------- repositories
- 
- QString MainWindow::repositoryMirrorRoot() const
-@@ -1898,76 +2008,20 @@ void MainWindow::publishRepository(int index, bool showDialogOnError)
-     const QString owner = catalogOwner(repo);
-     const QString name = repoSegment(repo.name, QStringLiteral("repository"));
-     const QString updatedAt = QString::number(now);
--    // A stable identity for the logical repo: its first (root) commit, shared by
--    // every node mirroring it. The network page groups mirrors by this so the same
--    // repo under different owners shows as one card. Not part of the signature.
--    QString rootCommit;
--    {
--        const QString gitDir =
--            (!repo.localPath.trimmed().isEmpty() && QDir(repo.localPath).exists(".git"))
--                ? repo.localPath
--                : repo.mirrorPath;
--        // Resolve the earliest root commit. Prefer HEAD; but a bare mirror cloned
--        // from the relay can carry an unset/dangling HEAD (the relay serves
--        // git-upload-pack without advertising a symref HEAD), so "rev-list ... HEAD"
--        // fails and leaves rootCommit empty. A wrong/empty root drops that mirror
--        // into a different group key (worker repo_mirror_group_key), so the owner's
--        // mirror-nodes panel never lists it next to the source of truth — the node
--        // shows up on the mirror but not on the source (issue #243, adhoc #134).
--        auto firstRoot = [&](const QStringList &args) -> QString {
--            QByteArray out;
--            if (gitDir.trimmed().isEmpty() ||
--                !runGitCapture(gitDir, args, &out, nullptr))
--                return QString();
--            const QStringList roots =
--                QString::fromUtf8(out).split('\n', Qt::SkipEmptyParts);
--            return roots.isEmpty() ? QString() : roots.last().trimmed();
--        };
--        rootCommit = firstRoot({"rev-list", "--max-parents=0", "HEAD"});
--        if (rootCommit.isEmpty()) {
--            // HEAD is unset. Compute the root of the branch this mirror actually
--            // serves (mirrorHeadBranch: the same default branch the source's HEAD
--            // points to), NOT a blind "--all" walk. This repo has several root
--            // commits — one per independent history the mirror also holds (agent
--            // branches, imported subtrees). "rev-list --max-parents=0 --all" returns
--            // ALL of them and "roots.last()" would pick whichever an unrelated
--            // history contributes, so the mirror published a different rootCommit
--            // than the source and landed in its own group (adhoc #134). Resolving
--            // the served branch yields the source's root regardless of HEAD's state.
--            const QString branch = mirrorHeadBranch(gitDir);
--            if (!branch.isEmpty())
--                rootCommit = firstRoot({"rev-list", "--max-parents=0", branch});
--        }
--        // Last resort (no HEAD and no resolvable served branch, e.g. a truly empty
--        // ref set): fall back to --all so a single-root mirror still groups.
--        if (rootCommit.isEmpty())
--            rootCommit = firstRoot({"rev-list", "--max-parents=0", "--all"});
--    }
--    // Owner-signed fingerprint of the refs this node serves (sha256 over the
--    // canonical heads+tags advertisement). The relay pins this and refuses to
--    // serve any mirror whose live advertisement doesn't hash to it, so a tampered
--    // or rolled-back mirror can never be cloned. MUST match the worker's
--    // advertised_refs_canonical(): "<sha> <refname>" lines for refs/heads/* and
--    // refs/tags/* only, sorted, joined by '\n'.
--    const QString stateHash = mirrorStateHash(repo.mirrorPath);
--    // Repository details (about text + website) live in the committed
--    // .forkmesh/info.json, the single source of truth (issue #232). Prefer it over
--    // the locally-cached record fields so the website's About panel is filled from
--    // info.json even for a mirror that cloned the repo but never had its
--    // description typed in locally (adhoc #86).
--    QString publishedWebsite;
--    QString publishedDescription = repo.description;
--    if (!repo.localPath.trimmed().isEmpty()) {
--        QFile file(QDir(repo.localPath).filePath(kRepoInfoPath));
--        if (file.open(QIODevice::ReadOnly)) {
--            const QJsonObject info = QJsonDocument::fromJson(file.readAll()).object();
--            publishedWebsite = info.value(QStringLiteral("website")).toString();
--            const QString about =
--                info.value(QStringLiteral("about")).toString().trimmed();
--            if (!about.isEmpty())
--                publishedDescription = about;
--        }
--    }
-+    const RepositoryRecord repoSnapshot = repo;
-+    runOffThread<PublishRepositorySnapshot>(
-+        [repoSnapshot] { return collectPublishRepositorySnapshot(repoSnapshot); },
-+        [this, index, showDialogOnError, owner, name, updatedAt,
-+         repoSnapshot](PublishRepositorySnapshot snapshot) mutable {
-+    if (index < 0 || index >= m_repositories.size())
-+        return;
-+    RepositoryRecord &repo = m_repositories[index];
-+    if (repo.previewOnly || repo.owner != repoSnapshot.owner ||
-+        repo.name != repoSnapshot.name || repo.mirrorPath != repoSnapshot.mirrorPath)
-+        return;
-+
-+    QString publishedWebsite = snapshot.publishedWebsite;
-+    QString publishedDescription = snapshot.publishedDescription;
-     if (index == m_repoDetailIndex) {
-         if (publishedWebsite.isEmpty())
-             publishedWebsite = m_repoInfo.website;
-@@ -1982,15 +2036,15 @@ void MainWindow::publishRepository(int index, bool showDialogOnError)
-     // everything but sync time and size (adhoc #56). The HEAD/issue figures mirror
-     // the live advert (setMirroredRepos); platform/version/id come from our own
-     // roster entry (the same values makeMessage broadcasts).
--    const QString headBranch = mirrorHeadBranch(repo.mirrorPath);
--    const QString headCommit = mirrorBranchCommit(repo.mirrorPath, headBranch);
--    const int issueCount = mirrorIssueCount(repo.mirrorPath, headBranch);
--    const int commitCount = mirrorCommitCount(repo.mirrorPath, headBranch);
--    const int branchCount = mirrorBranchCount(repo.mirrorPath);
--    const int pullCount = mirrorPullCount(repo.mirrorPath, headBranch);
--    const int discussionCount = mirrorDiscussionCount(repo.mirrorPath, headBranch);
--    const int worktreeCount = mirrorWorktreeCount(repo.localPath);
--    const int artifactCount = mirrorArtifactCount(repo.mirrorPath);
-+    const QString &headBranch = snapshot.headBranch;
-+    const QString &headCommit = snapshot.headCommit;
-+    const int issueCount = snapshot.issueCount;
-+    const int commitCount = snapshot.commitCount;
-+    const int branchCount = snapshot.branchCount;
-+    const int pullCount = snapshot.pullCount;
-+    const int discussionCount = snapshot.discussionCount;
-+    const int worktreeCount = snapshot.worktreeCount;
-+    const int artifactCount = snapshot.artifactCount;
-     QString selfPlatform, selfVersion, selfNodeId;
-     for (const MemberInfo &member : std::as_const(m_homeRoster)) {
-         if (member.self) {
-@@ -2033,11 +2087,10 @@ void MainWindow::publishRepository(int index, bool showDialogOnError)
-                          {"hostedSince", QString::number(repo.hostedSinceMs)},
-                          {"lastSync", QString::number(repo.lastSyncMs)},
-                          {"updatedAt", updatedAt},
--                         {"rootCommit", rootCommit},
-+                         {"rootCommit", snapshot.rootCommit},
-                          // On-disk mirror size so the network page can show how
-                          // much data each owner/repo is hosting. Not signed.
--                         {"sizeBytes",
--                          QString::number(mirrorRepoSizeBytes(repo.mirrorPath))},
-+                         {"sizeBytes", QString::number(snapshot.sizeBytes)},
-                          {"visibility", repo.isPrivate
-                                             ? QStringLiteral("private")
-                                             : QStringLiteral("public")},
-@@ -2053,11 +2106,11 @@ void MainWindow::publishRepository(int index, bool showDialogOnError)
-     metadata.insert("catalogSig", m_profileIdentity.signData(catalogCanonical));
-     // Attest the served refs so the relay can detect a tampered/stale mirror.
-     // Signed with the same account key the relay verifies for the catalog write.
--    if (!stateHash.isEmpty()) {
--        metadata.insert("stateHash", stateHash);
-+    if (!snapshot.stateHash.isEmpty()) {
-+        metadata.insert("stateHash", snapshot.stateHash);
-         const QByteArray stateCanonical =
--            ("forkmesh-repostate-v1\n" + owner + "\n" + name + "\n" + stateHash +
--             "\n" + updatedAt)
-+            ("forkmesh-repostate-v1\n" + owner + "\n" + name + "\n" +
-+             snapshot.stateHash + "\n" + updatedAt)
-                 .toUtf8();
-         metadata.insert("stateSig", m_profileIdentity.signData(stateCanonical));
-     }
-@@ -2119,6 +2172,7 @@ void MainWindow::publishRepository(int index, bool showDialogOnError)
-                 if (showDialogOnError)
-                     flashMessage(message, /*error=*/true);
-             });
-+        });
- }
- 
- namespace {
-@@ -3018,4 +3072,3 @@ void MainWindow::uninstallForkMesh()
- 
-     QCoreApplication::exit(0);
- }
--
diff --git a/pulls/3/pull.md b/pulls/3/pull.md
deleted file mode 100644
index 4d949ccf..00000000
--- a/pulls/3/pull.md
+++ /dev/null
@@ -1,14 +0,0 @@
----
-schema: forkmesh-pull-v1
-number: 3
-title: Agent: ForkMesh's GUI thread stalled for ~1933 ms — the event loop was blocked, whic…
-base: main
-head: agent/adhoc-79-forkmesh-s-gui-thread-stalled-for-1933-ms-the-ev
-status: open
-ts: 1783156638154
-author: F4rlozoS0ls2-Evs6Q7mLsSsZR1TVwMAsan0tdgaJoM
-authorName: mdkaifansari04
-sig: LCNn3fEcM6VGe3RQe1c2fyq_SarPIKTYrNcVWCpmpSV34vpgertsrvIJ-WKPbsj8mjnF9tatqGr5zoZnMstXBA
----
-
-Created from a OpenAI API agent session.
diff --git a/pulls/4/changes.patch b/pulls/4/changes.patch
deleted file mode 100644
index e251e842..00000000
--- a/pulls/4/changes.patch
+++ /dev/null
@@ -1,239 +0,0 @@
-diff --git a/qt_client/src/MainWindow.h b/qt_client/src/MainWindow.h
-index c066b25f..065a4f9c 100644
---- a/qt_client/src/MainWindow.h
-+++ b/qt_client/src/MainWindow.h
-@@ -394,6 +394,10 @@ public:
-     }
-     QString testAgentStatusCellText(int sessionId) const;
-     bool testAgentSessionMerged(int sessionId) const;
-+    void testImportPatchAsPullFromPath(const QString &path)
-+    {
-+        importPatchAsPullFromPath(path);
-+    }
- #endif
- 
-     // --- Headless / CLI support (HeadlessConsole) ------------------------------
-@@ -1054,6 +1058,7 @@ private:
-     void submitPullReview(const QString &state);    // approve / request changes
-     void promptNewPull();
-     void importPatchAsPull(); // read a .patch/.diff file and open it as a PR
-+    void importPatchAsPullFromPath(const QString &path);
-     void promptNewPullFromDirectory();
-     void promptNewPullFromSource(const QString &sourceDir,
-                                  const QString &preferredBase = QString(),
-diff --git a/qt_client/src/MainWindowPulls.cpp b/qt_client/src/MainWindowPulls.cpp
-index 84425c8e..171332d1 100644
---- a/qt_client/src/MainWindowPulls.cpp
-+++ b/qt_client/src/MainWindowPulls.cpp
-@@ -3552,6 +3552,14 @@ void MainWindow::promptNewPull()
- }
- 
- void MainWindow::importPatchAsPull()
-+{
-+    const QString path = QFileDialog::getOpenFileName(
-+        this, "Import patch as pull request", QDir::homePath(),
-+        "Patch files (*.patch *.diff);;All files (*)");
-+    importPatchAsPullFromPath(path);
-+}
-+
-+void MainWindow::importPatchAsPullFromPath(const QString &path)
- {
-     if (m_repoDetailIndex < 0 || m_repoDetailIndex >= m_repositories.size())
-         return;
-@@ -3562,9 +3570,6 @@ void MainWindow::importPatchAsPull()
-             "Importing a patch needs a writable local checkout of this repo.", true);
-         return;
-     }
--    const QString path = QFileDialog::getOpenFileName(
--        this, "Import patch as pull request", QDir::homePath(),
--        "Patch files (*.patch *.diff);;All files (*)");
-     if (path.isEmpty())
-         return;
-     QFile file(path);
-@@ -3598,25 +3603,6 @@ void MainWindow::importPatchAsPull()
-     const QStringList branches = repoBranches();
-     const QString base = repoDefaultBranch(branches);
- 
--    // Sanity-check that the patch applies to the base before opening the PR.
--    QString applyErr;
--    QProcess check;
--    check.setProgram("git");
--    check.setArguments({"-C", dir, "apply", "--check", "--3way", path});
--    check.start();
--    check.waitForFinished(8000);
--    if (check.exitStatus() != QProcess::NormalExit || check.exitCode() != 0) {
--        const QString detail =
--            QString::fromUtf8(check.readAllStandardError()).trimmed();
--        if (QMessageBox::warning(
--                this, "Import patch",
--                QStringLiteral("This patch does not apply cleanly onto %1:\n\n%2\n\n"
--                               "Open the pull request anyway?")
--                    .arg(base, detail.isEmpty() ? "(no details)" : detail),
--                QMessageBox::Yes | QMessageBox::No, QMessageBox::No) != QMessageBox::Yes)
--            return;
--    }
--
-     // Synthesize a head label from the title; the patch itself carries the change.
-     QString head = QStringLiteral("imported/") +
-                    title.toLower().replace(QRegularExpression(QStringLiteral("[^a-z0-9]+")),
-@@ -3625,21 +3611,66 @@ void MainWindow::importPatchAsPull()
-     if (head.endsWith(QLatin1Char('-')))
-         head.chop(1);
- 
--    QString error;
--    const int number = store.createPull(
--        title, QStringLiteral("Imported from patch file `%1`.").arg(QFileInfo(path).fileName()),
--        base, head, patch, QString(), /*branchBacked=*/false, &error);
--    if (number < 0) {
--        setRepoDetailNotice(error.isEmpty() ? "Could not create the pull request."
-+    setRepoDetailNotice("Checking patch...");
-+    const int repoIndex = m_repoDetailIndex;
-+    auto applies = std::make_shared<bool>(false);
-+    auto detail = std::make_shared<QString>();
-+    QThread *worker = QThread::create([dir, path, applies, detail] {
-+        QProcess check;
-+        check.setProgram("git");
-+        check.setArguments({"-C", dir, "apply", "--check", "--3way", path});
-+        check.start();
-+        const bool finished = check.waitForFinished(8000);
-+        *applies = finished && check.exitStatus() == QProcess::NormalExit &&
-+                   check.exitCode() == 0;
-+        if (!*applies) {
-+            *detail = finished
-+                          ? QString::fromUtf8(check.readAllStandardError()).trimmed()
-+                          : QStringLiteral("git apply --check timed out");
-+        }
-+    });
-+    connect(worker, &QThread::finished, worker, &QObject::deleteLater);
-+    connect(worker, &QThread::finished, this,
-+            [this, repoIndex, store, path, title, base, head, patch, applies,
-+             detail]() mutable {
-+                if (m_repoDetailIndex != repoIndex)
-+                    return;
-+                if (!*applies) {
-+                    if (QMessageBox::warning(
-+                            this, "Import patch",
-+                            QStringLiteral("This patch does not apply cleanly onto %1:\n\n%2\n\n"
-+                                           "Open the pull request anyway?")
-+                                .arg(base, detail->isEmpty() ? "(no details)" : *detail),
-+                            QMessageBox::Yes | QMessageBox::No,
-+                            QMessageBox::No) != QMessageBox::Yes) {
-+                        setRepoDetailNotice("Patch import cancelled.", true);
-+                        return;
-+                    }
-+                }
-+
-+                QString error;
-+                const int number = store.createPull(
-+                    title,
-+                    QStringLiteral("Imported from patch file `%1`.")
-+                        .arg(QFileInfo(path).fileName()),
-+                    base, head, patch, QString(), /*branchBacked=*/false, &error);
-+                if (number < 0) {
-+                    setRepoDetailNotice(error.isEmpty()
-+                                            ? "Could not create the pull request."
-                                             : error,
--                            true);
--        return;
--    }
--    logSystem(QStringLiteral("Imported patch %1 as pull #%2.").arg(path).arg(number));
--    setRepoDetailNotice(QStringLiteral("Imported patch as pull #%1.").arg(number));
--    m_currentPullNumber = number;
--    switchToPullTab(number);
--    propagateRepoUpdate(m_repoDetailIndex);
-+                                        true);
-+                    return;
-+                }
-+                logSystem(QStringLiteral("Imported patch %1 as pull #%2.")
-+                              .arg(path)
-+                              .arg(number));
-+                setRepoDetailNotice(QStringLiteral("Imported patch as pull #%1.")
-+                                        .arg(number));
-+                m_currentPullNumber = number;
-+                switchToPullTab(number);
-+                propagateRepoUpdate(m_repoDetailIndex);
-+            });
-+    worker->start();
- }
- 
- void MainWindow::promptNewPullFromDirectory()
-@@ -7100,4 +7131,3 @@ void MainWindow::pollOwnedInboxes()
-         drainCommitInboxFor(repo, /*interactive=*/false);
-     }
- }
--
-diff --git a/qt_client/tests/test_window_resize.cpp b/qt_client/tests/test_window_resize.cpp
-index 025d803a..7df0a1d8 100644
---- a/qt_client/tests/test_window_resize.cpp
-+++ b/qt_client/tests/test_window_resize.cpp
-@@ -18,6 +18,7 @@
- #include <QSettings>
- #include <QStandardPaths>
- #include <QTemporaryDir>
-+#include <QTextStream>
- #include <QWidget>
- 
- #include <algorithm>
-@@ -983,6 +984,62 @@ int main(int argc, char *argv[])
-         }
-     }
- 
-+    // Importing a patch must not synchronously wait on `git apply --check` on the
-+    // GUI thread. A fake git that sleeps before delegating makes the old blocking
-+    // path take >1s; the fixed path returns immediately and finishes later.
-+    {
-+        QTemporaryDir importRepo;
-+        QTemporaryDir fakeBin;
-+        QTemporaryDir patchDir;
-+        if (initGitRepo(importRepo) && fakeBin.isValid() && patchDir.isValid()) {
-+            QFile wrapper(fakeBin.path() + QStringLiteral("/git"));
-+            if (wrapper.open(QIODevice::WriteOnly | QIODevice::Text)) {
-+                QTextStream ts(&wrapper);
-+                ts << "#!/bin/sh\n"
-+                   << "if [ \"$3\" = apply ] && [ \"$4\" = --check ]; then sleep 1; fi\n"
-+                   << "exec /usr/bin/git \"$@\"\n";
-+                wrapper.close();
-+                wrapper.setPermissions(wrapper.permissions() | QFile::ExeOwner |
-+                                       QFile::ExeGroup | QFile::ExeOther);
-+            }
-+            QFile patch(patchDir.path() + QStringLiteral("/change.patch"));
-+            if (patch.open(QIODevice::WriteOnly | QIODevice::Text)) {
-+                QTextStream ts(&patch);
-+                ts << "diff --git a/new.txt b/new.txt\n"
-+                   << "new file mode 100644\n"
-+                   << "index 0000000..1269488\n"
-+                   << "--- /dev/null\n"
-+                   << "+++ b/new.txt\n"
-+                   << "@@ -0,0 +1 @@\n"
-+                   << "+hello\n";
-+                patch.close();
-+            }
-+
-+            MainWindow importWindow;
-+            importWindow.show();
-+            QApplication::processEvents();
-+            const int idx = importWindow.testAddLocalRepository(
-+                "me", "importrepo", importRepo.path());
-+            importWindow.testOpenRepository(idx);
-+            QApplication::processEvents();
-+
-+            const QByteArray oldPath = qgetenv("PATH");
-+            qputenv("PATH", (fakeBin.path() + QLatin1Char(':') +
-+                              QString::fromUtf8(oldPath)).toUtf8());
-+            QElapsedTimer timer;
-+            timer.start();
-+            importWindow.testImportPatchAsPullFromPath(patch.fileName());
-+            const qint64 elapsed = timer.elapsed();
-+            qputenv("PATH", oldPath);
-+
-+            check(elapsed < 500,
-+                  QString("import patch returns before slow apply check "
-+                          "(elapsed=%1ms)")
-+                      .arg(elapsed));
-+            stopChildProcesses(importWindow);
-+        }
-+    }
-+
-     // Issue #287: the headless "mirrors" view leads with this node's own CPU and
-     // memory so an operator watching a durable daemon can see its load, and the
-     // "status" view carries the same Load line.
diff --git a/pulls/4/pull.md b/pulls/4/pull.md
deleted file mode 100644
index 317d44e1..00000000
--- a/pulls/4/pull.md
+++ /dev/null
@@ -1,14 +0,0 @@
----
-schema: forkmesh-pull-v1
-number: 4
-title: Agent: ForkMesh's GUI thread stalled for ~1511 ms — the event loop was blocked, whic…
-base: main
-head: agent/adhoc-80-forkmesh-s-gui-thread-stalled-for-1511-ms-the-ev
-status: open
-ts: 1783156792854
-author: F4rlozoS0ls2-Evs6Q7mLsSsZR1TVwMAsan0tdgaJoM
-authorName: mdkaifansari04
-sig: u3t807Vk545AEIsoDccwdLK-A-4Az0Nqmq2yokkhEtcCb7l8EPwZJJVrslcQzH5qRzkgzDiG-lXnvwiOpdsECQ
----
-
-Created from a OpenAI API agent session.
diff --git a/pulls/5/changes.patch b/pulls/5/changes.patch
deleted file mode 100644
index 6969e6ba..00000000
--- a/pulls/5/changes.patch
+++ /dev/null
@@ -1,169 +0,0 @@
-diff --git a/qt_client/src/MainWindow.h b/qt_client/src/MainWindow.h
-index c066b25f..27839bed 100644
---- a/qt_client/src/MainWindow.h
-+++ b/qt_client/src/MainWindow.h
-@@ -291,6 +291,14 @@ public:
-                    ? QString()
-                    : repoAgentGitDir(m_repositories.at(index));
-     }
-+    qint64 testCachedMirrorRepoSizeBytes(int index, bool refreshIfStale)
-+    {
-+        return (index < 0 || index >= m_repositories.size())
-+                   ? -1
-+                   : cachedMirrorRepoSizeBytes(m_repositories.at(index),
-+                                               refreshIfStale);
-+    }
-+    int testMirrorRepoSizeRefreshes() const { return m_testMirrorRepoSizeRefreshes; }
-     void testPublishRepository(int index) { publishRepository(index, false); }
-     void testStartRepoHosts() { startRepoHosts(); }
-     void testStopRepoHosts() { stopRepoHosts(); }
-@@ -1597,6 +1605,8 @@ private:
-     bool proposePullFromMirrorEdit(const QString &cleanPath, const QString &content);
-     void loadRepoInfo();
-     void loadBranchesAndTags();
-+    qint64 cachedMirrorRepoSizeBytes(const RepositoryRecord &repo,
-+                                     bool refreshIfStale = true);
-     QStringList repoBranches() const;
-     QString repoDefaultBranch(const QStringList &branches) const;
-     QWidget *buildBranchesTab();
-@@ -4215,6 +4225,16 @@ private:
-     // onRequestServed (adhoc #83); skip the rebuild while the signature is
-     // unchanged. Reset by attachBackend so a freshly attached backend is re-pushed.
-     QString m_mirrorAdvertSig;
-+    struct MirrorRepoSizeCacheEntry {
-+        qint64 syncMs = -1;
-+        qint64 bytes = 0;
-+        bool valid = false;
-+    };
-+    QHash<QString, MirrorRepoSizeCacheEntry> m_mirrorRepoSizeCache;
-+    QSet<QString> m_mirrorRepoSizeRefreshes;
-+#ifdef FORKMESH_WINDOW_TESTS
-+    int m_testMirrorRepoSizeRefreshes = 0;
-+#endif
-     int m_repoOpenPending = -1;        // repo index queued by openRepoDetailDeferred
-     // True while a user-driven repo load (a node switch or opening a repo) runs,
-     // so nodeSwitchStep narrates progress for both, not just node switches.
-diff --git a/qt_client/src/MainWindowReleases.cpp b/qt_client/src/MainWindowReleases.cpp
-index eaef6ac5..a37ab863 100644
---- a/qt_client/src/MainWindowReleases.cpp
-+++ b/qt_client/src/MainWindowReleases.cpp
-@@ -1004,7 +1004,7 @@ void MainWindow::loadMirrorNodesPanel()
-     selfAdvert.branch = mirrorHeadBranch(localMirror);
-     selfAdvert.commit = mirrorBranchCommit(localMirror, selfAdvert.branch);
-     selfAdvert.updatedMs = repo.lastSyncMs;
--    selfAdvert.sizeBytes = mirrorRepoSizeBytes(localMirror);
-+    selfAdvert.sizeBytes = cachedMirrorRepoSizeBytes(repo);
-     selfAdvert.issueCount = mirrorIssueCount(localMirror, selfAdvert.branch);
-     selfAdvert.commitCount = mirrorCommitCount(localMirror, selfAdvert.branch);
-     selfAdvert.branchCount = mirrorBranchCount(localMirror);
-diff --git a/qt_client/src/MainWindowRepos.cpp b/qt_client/src/MainWindowRepos.cpp
-index f627f708..8db26cf9 100644
---- a/qt_client/src/MainWindowRepos.cpp
-+++ b/qt_client/src/MainWindowRepos.cpp
-@@ -250,6 +250,50 @@ QStringList MainWindow::mentionCandidateNames() const
-     return names;
- }
- 
-+qint64 MainWindow::cachedMirrorRepoSizeBytes(const RepositoryRecord &repo,
-+                                             bool refreshIfStale)
-+{
-+    const QString mirrorPath = repo.mirrorPath.trimmed();
-+    if (mirrorPath.isEmpty() || !QDir(mirrorPath).exists()) {
-+        m_mirrorRepoSizeCache.remove(mirrorPath);
-+        m_mirrorRepoSizeRefreshes.remove(mirrorPath);
-+        return 0;
-+    }
-+
-+    const qint64 syncMs = repo.lastSyncMs;
-+    const auto cached = m_mirrorRepoSizeCache.constFind(mirrorPath);
-+    if (cached != m_mirrorRepoSizeCache.cend() && cached->valid &&
-+        cached->syncMs == syncMs)
-+        return cached->bytes;
-+
-+    const qint64 fallback =
-+        (cached != m_mirrorRepoSizeCache.cend() && cached->valid) ? cached->bytes : 0;
-+    if (!refreshIfStale || m_mirrorRepoSizeRefreshes.contains(mirrorPath))
-+        return fallback;
-+
-+    m_mirrorRepoSizeRefreshes.insert(mirrorPath);
-+#ifdef FORKMESH_WINDOW_TESTS
-+    ++m_testMirrorRepoSizeRefreshes;
-+#endif
-+    runGitDetached(mirrorPath, {"count-objects", "-v"},
-+                   [this, mirrorPath, syncMs](bool ok, const QByteArray &out) {
-+                       m_mirrorRepoSizeRefreshes.remove(mirrorPath);
-+                       const qint64 bytes =
-+                           ok ? parseCountObjectsSizeBytes(out) : 0;
-+                       m_mirrorRepoSizeCache.insert(
-+                           mirrorPath,
-+                           MirrorRepoSizeCacheEntry{syncMs, bytes, true});
-+                       m_mirrorAdvertSig.clear();
-+                       refreshRepositoryList();
-+                       if (m_repoDetailIndex >= 0 &&
-+                           m_repoDetailIndex < m_repositories.size() &&
-+                           m_repositories.at(m_repoDetailIndex).mirrorPath.trimmed() ==
-+                               mirrorPath)
-+                           loadMirrorNodesPanel();
-+                   });
-+    return fallback;
-+}
-+
- void MainWindow::refreshRepositoryList()
- {
-     // Re-entrancy guard (adhoc #247): the periodic m_homeStatsTimer fires this once
-@@ -493,7 +537,7 @@ void MainWindow::refreshRepositoryList()
-             advert.commit = mirrorBranchCommit(repo.mirrorPath, advert.branch);
-             advert.updatedMs = repo.lastSyncMs;
-             // On-disk mirror size so peers can show how much data we're holding.
--            advert.sizeBytes = mirrorRepoSizeBytes(repo.mirrorPath);
-+            advert.sizeBytes = cachedMirrorRepoSizeBytes(repo);
-             // Issues we're mirroring, so peers can show the count per node.
-             advert.issueCount = mirrorIssueCount(repo.mirrorPath, advert.branch);
-             // More tallies the Mirror nodes view shows per node: history depth,
-@@ -2037,7 +2081,7 @@ void MainWindow::publishRepository(int index, bool showDialogOnError)
-                          // On-disk mirror size so the network page can show how
-                          // much data each owner/repo is hosting. Not signed.
-                          {"sizeBytes",
--                          QString::number(mirrorRepoSizeBytes(repo.mirrorPath))},
-+                          QString::number(cachedMirrorRepoSizeBytes(repo))},
-                          {"visibility", repo.isPrivate
-                                             ? QStringLiteral("private")
-                                             : QStringLiteral("public")},
-@@ -3018,4 +3062,3 @@ void MainWindow::uninstallForkMesh()
- 
-     QCoreApplication::exit(0);
- }
--
-diff --git a/qt_client/tests/test_window_resize.cpp b/qt_client/tests/test_window_resize.cpp
-index 025d803a..f41449d1 100644
---- a/qt_client/tests/test_window_resize.cpp
-+++ b/qt_client/tests/test_window_resize.cpp
-@@ -1113,6 +1113,28 @@ int main(int argc, char *argv[])
-                              "mirror has no agent/looper git dir"));
-     }
- 
-+    // adhoc #77: the mirror-size badge/adverts must not synchronously run
-+    // `git count-objects -v` on every GUI refresh. The first call may kick an
-+    // async refresh, but unchanged repo state should be served from cache.
-+    {
-+        QTemporaryDir mirrorDir;
-+        const bool madeBare =
-+            mirrorDir.isValid() &&
-+            runGitChecked(mirrorDir.path(), {"init", "--bare", "-q"});
-+        const int mirrorIdx =
-+            window.testAddPublishedRepository("someone", "sizerepo", mirrorDir.path());
-+        const qint64 first =
-+            window.testCachedMirrorRepoSizeBytes(mirrorIdx, true);
-+        for (int i = 0; i < 20 && window.testMirrorRepoSizeRefreshes() == 0; ++i)
-+            QApplication::processEvents();
-+        const qint64 second =
-+            window.testCachedMirrorRepoSizeBytes(mirrorIdx, true);
-+        check(madeBare && first == 0 && second >= 0 &&
-+                  window.testMirrorRepoSizeRefreshes() == 1,
-+              QStringLiteral("mirror repo size uses an async cache instead of "
-+                             "rerunning count-objects on unchanged GUI refreshes"));
-+    }
-+
-     // adhoc #38: the issue looper skips issues that are already assigned (a
-     // looper on this or another mirror claimed them) so two loopers never work
-     // the same task, and otherwise picks the highest-priority open issue with no
diff --git a/pulls/5/pull.md b/pulls/5/pull.md
deleted file mode 100644
index 2aeee013..00000000
--- a/pulls/5/pull.md
+++ /dev/null
@@ -1,14 +0,0 @@
----
-schema: forkmesh-pull-v1
-number: 5
-title: Agent: ForkMesh's GUI thread stalled for ~3181 ms — the event loop was blocked, whic…
-base: main
-head: agent/adhoc-77-forkmesh-s-gui-thread-stalled-for-3181-ms-the-ev
-status: open
-ts: 1783156801388
-author: F4rlozoS0ls2-Evs6Q7mLsSsZR1TVwMAsan0tdgaJoM
-authorName: mdkaifansari04
-sig: _H_fPjgaIlRlCvLZNDh2V_pSkmUhFhA6cCTROi0_4qKAn1xZUa1TO39WNgOVg-WLxYvU4xFwIgS0yO3GXbA5AA
----
-
-Created from a OpenAI API agent session.
