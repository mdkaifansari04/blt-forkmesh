diff --git a/cloudflare_worker/public/network/index.html b/cloudflare_worker/public/network/index.html
index 4643179..f325afd 100644
--- a/cloudflare_worker/public/network/index.html
+++ b/cloudflare_worker/public/network/index.html
@@ -9,25 +9,69 @@
     <script src="../static-page.js" defer></script>
     <style>
       .online-graph {
-        display: flex; align-items: flex-end; gap: 4px;
-        height: 160px; padding: 12px 4px 0;
+        display: grid;
+        gap: 12px;
+        padding: 12px 0 0;
       }
-      .online-graph .bar-col {
-        flex: 1 1 0; display: flex; flex-direction: column;
-        align-items: center; justify-content: flex-end; height: 100%;
+      .online-node-row {
+        display: grid;
+        grid-template-columns: minmax(88px, 140px) 1fr minmax(42px, auto);
+        gap: 10px;
+        align-items: center;
+      }
+      .online-node-label {
+        overflow: hidden;
+        text-overflow: ellipsis;
+        white-space: nowrap;
+        color: var(--text);
+        font-weight: 700;
+        font-size: 13px;
+      }
+      .online-node-cells {
+        display: grid;
+        grid-template-columns: repeat(24, minmax(4px, 1fr));
+        gap: 3px;
         min-width: 0;
       }
-      .online-graph .bar {
-        width: 70%; min-height: 2px; border-radius: 4px 4px 2px 2px;
-        background: linear-gradient(180deg, var(--accent-bright), var(--accent));
-        transition: height .3s ease;
+      .online-cell {
+        height: 22px;
+        border-radius: 4px;
+        background: color-mix(in srgb, var(--border) 72%, transparent);
+        border: 1px solid color-mix(in srgb, var(--border) 70%, transparent);
       }
-      .online-graph .bar.empty { background: var(--border); opacity: .5; }
-      .online-graph .bar-label {
-        margin-top: 6px; font-size: 10px; color: var(--muted-2);
+      .online-cell[data-level="1"] { background: color-mix(in srgb, var(--accent) 32%, transparent); }
+      .online-cell[data-level="2"] { background: color-mix(in srgb, var(--accent) 58%, transparent); }
+      .online-cell[data-level="3"] { background: var(--accent); }
+      .online-node-total {
+        color: var(--muted);
+        font-size: 12px;
+        text-align: right;
         white-space: nowrap;
       }
+      .online-axis {
+        display: grid;
+        grid-template-columns: minmax(88px, 140px) 1fr minmax(42px, auto);
+        gap: 10px;
+        align-items: center;
+        color: var(--muted-2);
+        font-size: 10px;
+      }
+      .online-axis-hours {
+        display: grid;
+        grid-template-columns: repeat(24, minmax(4px, 1fr));
+        gap: 3px;
+      }
       .online-graph-empty { color: var(--muted); padding: 24px 4px; }
+      @media (max-width: 720px) {
+        .online-node-row,
+        .online-axis {
+          grid-template-columns: 1fr;
+          gap: 6px;
+        }
+        .online-node-total {
+          text-align: left;
+        }
+      }
     </style>
   </head>
   <body>
@@ -189,39 +233,86 @@
           return;
         }
         async function render() {
-          let hours;
+          let data;
           try {
             const res = await fetch("/api/network/online-history", {
               headers: { accept: "application/json" },
             });
             if (!res.ok) throw new Error("HTTP " + res.status);
-            hours = (await res.json()).hours || [];
+            data = await res.json();
           } catch (_) {
             root.innerHTML =
               '<div class="online-graph-empty">Activity unavailable right now.</div>';
             return;
           }
-          const max = Math.max(1, ...hours.map((h) => Number(h.nodeMinutes) || 0));
+          const hours = Array.isArray(data.hours) ? data.hours : [];
+          let nodes = Array.isArray(data.nodes) ? data.nodes : [];
+          if (!nodes.length) {
+            const total = hours.reduce(
+              (sum, h) => sum + (Number(h.nodeMinutes) || 0),
+              0,
+            );
+            if (!total) {
+              root.innerHTML =
+                '<div class="online-graph-empty">No node activity sampled yet.</div>';
+              return;
+            }
+            nodes = [{
+              label: "all nodes",
+              totalMinutes: total,
+              hours: hours.map((h) => ({
+                hourTs: h.hourTs,
+                minutes: Number(h.nodeMinutes) || 0,
+              })),
+            }];
+          }
+
           root.innerHTML = "";
+          const axis = document.createElement("div");
+          axis.className = "online-axis";
+          axis.appendChild(document.createElement("span"));
+          const axisHours = document.createElement("div");
+          axisHours.className = "online-axis-hours";
           hours.forEach((h, i) => {
-            const minutes = Number(h.nodeMinutes) || 0;
-            const hour = new Date(Number(h.hourTs)).getHours();
-            const col = document.createElement("div");
-            col.className = "bar-col";
-            const bar = document.createElement("div");
-            bar.className = "bar" + (minutes === 0 ? " empty" : "");
-            bar.style.height = Math.round((minutes / max) * 100) + "%";
-            bar.title = minutes + " host-minutes online · " +
-              String(hour).padStart(2, "0") + ":00";
-            col.appendChild(bar);
-            // Label every 6 hours to avoid crowding.
-            if (i % 6 === 0) {
-              const label = document.createElement("div");
-              label.className = "bar-label";
-              label.textContent = String(hour).padStart(2, "0") + "h";
-              col.appendChild(label);
-            }
-            root.appendChild(col);
+            const tick = document.createElement("span");
+            tick.textContent = i % 6 === 0
+              ? String(new Date(Number(h.hourTs)).getHours()).padStart(2, "0")
+              : "";
+            axisHours.appendChild(tick);
+          });
+          axis.appendChild(axisHours);
+          const axisTotal = document.createElement("span");
+          axisTotal.textContent = "min";
+          axis.appendChild(axisTotal);
+          root.appendChild(axis);
+
+          nodes.forEach((node) => {
+            const row = document.createElement("div");
+            row.className = "online-node-row";
+            const label = document.createElement("div");
+            label.className = "online-node-label";
+            label.textContent = node.label || "node";
+            row.appendChild(label);
+
+            const cells = document.createElement("div");
+            cells.className = "online-node-cells";
+            (node.hours || []).forEach((h) => {
+              const minutes = Number(h.minutes) || 0;
+              const cell = document.createElement("div");
+              cell.className = "online-cell";
+              cell.dataset.level = minutes >= 45 ? "3" : minutes >= 20 ? "2" :
+                minutes > 0 ? "1" : "0";
+              const hour = new Date(Number(h.hourTs)).getHours();
+              cell.title = `${node.label || "node"} · ${minutes} min · ${String(hour).padStart(2, "0")}:00`;
+              cells.appendChild(cell);
+            });
+            row.appendChild(cells);
+
+            const total = document.createElement("div");
+            total.className = "online-node-total";
+            total.textContent = `${Number(node.totalMinutes) || 0}`;
+            row.appendChild(total);
+            root.appendChild(row);
           });
         }
         render();
diff --git a/cloudflare_worker/src/entry.py b/cloudflare_worker/src/entry.py
index b9577eb..44aa6df 100644
--- a/cloudflare_worker/src/entry.py
+++ b/cloudflare_worker/src/entry.py
@@ -336,6 +336,53 @@ async def network_stats(env):
 
 ONLINE_SAMPLE_WINDOW_MS = 2 * 60 * 1000  # treat a node seen in the last 2 min as online
 ONLINE_HISTORY_RETAIN_MS = 48 * 60 * 60 * 1000
+ONLINE_HISTORY_MAX_NODES = 50
+
+
+def _short_presence_label(prefix, key):
+    return prefix + " " + str(key or "")[:8]
+
+
+async def _live_online_nodes(env, now):
+    nodes = {}
+    host_rows = await d1_all(
+        env,
+        """SELECT hp.repo_bi, r.owner_bi, r.data
+             FROM host_presence hp
+             LEFT JOIN repositories r ON r.key_bi = hp.repo_bi
+             WHERE hp.ts >= ?""",
+        now - HOST_PRESENCE_STALE_MS,
+    )
+    for row in host_rows:
+        node_key = row.get("owner_bi") or row.get("repo_bi")
+        if not node_key:
+            continue
+        label = ""
+        if row.get("data"):
+            rec = await decrypt_row(env, row["data"])
+            if rec:
+                label = clean_string(rec.get("owner", ""), MAX_NODE_NAME)
+        nodes[node_key] = label or nodes.get(node_key) or _short_presence_label("repo", node_key)
+
+    acct_rows = await d1_all(
+        env,
+        """SELECT ap.name_bi, a.name, a.data
+             FROM account_presence ap
+             LEFT JOIN accounts a ON a.name_bi = ap.name_bi
+             WHERE ap.ts >= ?""",
+        now - ONLINE_SAMPLE_WINDOW_MS,
+    )
+    for row in acct_rows:
+        node_key = row.get("name_bi")
+        if not node_key:
+            continue
+        label = clean_string(row.get("name", ""), MAX_NODE_NAME)
+        if not label and row.get("data"):
+            rec = await decrypt_row(env, row["data"])
+            if rec:
+                label = clean_string(rec.get("name", ""), MAX_NODE_NAME)
+        nodes[node_key] = label or nodes.get(node_key) or _short_presence_label("node", node_key)
+    return nodes
 
 
 async def record_online_sample(env):
@@ -347,17 +394,8 @@ async def record_online_sample(env):
     # host was online; counting hosts fixes that.
     await ensure_schema(env)
     now = int(Date.now())
-    host_row = await d1_first(
-        env, "SELECT COUNT(*) AS n FROM host_presence WHERE ts >= ?",
-        now - HOST_PRESENCE_STALE_MS,
-    )
-    acct_row = await d1_first(
-        env, "SELECT COUNT(*) AS n FROM account_presence WHERE ts >= ?",
-        now - ONLINE_SAMPLE_WINDOW_MS,
-    )
-    hosts = int((host_row or {}).get("n", 0) or 0)
-    accounts = int((acct_row or {}).get("n", 0) or 0)
-    online = max(hosts, accounts)
+    nodes = await _live_online_nodes(env, now)
+    online = len(nodes)
     hour_ts = (now // 3600000) * 3600000
     await d1_run(
         env,
@@ -365,10 +403,24 @@ async def record_online_sample(env):
         "ON CONFLICT(hour_ts) DO UPDATE SET node_minutes = node_minutes + ?",
         hour_ts, online, online,
     )
+    for node_key, label in nodes.items():
+        await d1_run(
+            env,
+            """INSERT INTO online_hourly_nodes
+                 (hour_ts, node_key, label, node_minutes) VALUES (?, ?, ?, 1)
+               ON CONFLICT(hour_ts, node_key) DO UPDATE SET
+                 node_minutes = node_minutes + 1,
+                 label = excluded.label""",
+            hour_ts, node_key, label,
+        )
     await d1_run(
         env, "DELETE FROM online_hourly WHERE hour_ts < ?",
         hour_ts - ONLINE_HISTORY_RETAIN_MS,
     )
+    await d1_run(
+        env, "DELETE FROM online_hourly_nodes WHERE hour_ts < ?",
+        hour_ts - ONLINE_HISTORY_RETAIN_MS,
+    )
 
 
 async def online_history(env):
@@ -386,7 +438,48 @@ async def online_history(env):
     series = [{"hourTs": start + i * 3600000,
                "nodeMinutes": by_hour.get(start + i * 3600000, 0)}
               for i in range(24)]
-    return json_response({"ok": True, "hours": series}, cache_seconds=30)
+    node_rows = await d1_all(
+        env,
+        """SELECT node_key, label, hour_ts, node_minutes
+             FROM online_hourly_nodes
+             WHERE hour_ts >= ?
+             ORDER BY node_key, hour_ts""",
+        start,
+    )
+    grouped = {}
+    for row in node_rows:
+        node_key = str(row.get("node_key") or "")
+        if not node_key:
+            continue
+        item = grouped.setdefault(node_key, {
+            "label": clean_string(row.get("label", ""), MAX_NODE_NAME) or
+                     _short_presence_label("node", node_key),
+            "hours": {},
+            "total": 0,
+        })
+        minutes = int(row.get("node_minutes") or 0)
+        hour_ts = int(row.get("hour_ts") or 0)
+        item["hours"][hour_ts] = minutes
+        item["total"] += minutes
+        if row.get("label"):
+            item["label"] = clean_string(row.get("label", ""), MAX_NODE_NAME)
+    nodes = []
+    for node_key, item in grouped.items():
+        nodes.append({
+            "id": node_key[:12],
+            "label": item["label"],
+            "totalMinutes": item["total"],
+            "hours": [
+                {"hourTs": start + i * 3600000,
+                 "minutes": item["hours"].get(start + i * 3600000, 0)}
+                for i in range(24)
+            ],
+        })
+    nodes.sort(key=lambda n: (-int(n["totalMinutes"]), str(n["label"])))
+    return json_response(
+        {"ok": True, "hours": series, "nodes": nodes[:ONLINE_HISTORY_MAX_NODES]},
+        cache_seconds=30,
+    )
 
 
 def method_name(request):
@@ -541,6 +634,11 @@ SCHEMA_STATEMENTS = [
     # node_minutes is "node-minutes online" that hour (one node online all hour
     # = 60). hour_ts is the epoch-ms start of the hour.
     "CREATE TABLE IF NOT EXISTS online_hourly (hour_ts INTEGER PRIMARY KEY, node_minutes INTEGER NOT NULL DEFAULT 0)",
+    """CREATE TABLE IF NOT EXISTS online_hourly_nodes (
+        hour_ts INTEGER NOT NULL, node_key TEXT NOT NULL, label TEXT NOT NULL,
+        node_minutes INTEGER NOT NULL DEFAULT 0,
+        PRIMARY KEY (hour_ts, node_key))""",
+    "CREATE INDEX IF NOT EXISTS idx_online_hourly_nodes_key ON online_hourly_nodes(node_key)",
     # Retained chat history: the relay keeps the last few days of *encrypted*
     # durable messages per room so a node joining later sees some history even
     # when no peer is online to replay it. body is the opaque encrypted envelope
