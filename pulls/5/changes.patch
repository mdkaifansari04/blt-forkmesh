diff --git a/cloudflare_worker/src/entry.py b/cloudflare_worker/src/entry.py
index b121e27b..993920e5 100644
--- a/cloudflare_worker/src/entry.py
+++ b/cloudflare_worker/src/entry.py
@@ -273,6 +273,8 @@ def pull_comment_content(ev):
         return "\x00".join([
             ev.get("path", ""), ev.get("side", ""), line, ev.get("body", ""),
         ])
+    if t == "status":
+        return ev.get("status", "")
     return ""
 
 
@@ -283,7 +285,7 @@ async def verify_pull_comment_event(number, ev):
     signature = ev.get("sig", "")
     event_type = ev.get("type", "")
     if not author or not signature or \
-            event_type not in ("comment", "review", "line-comment"):
+            event_type not in ("comment", "review", "line-comment", "status"):
         return False
     try:
         ts = int(ev.get("ts", 0))
diff --git a/flutter_app/lib/screens/repo_detail_screen.dart b/flutter_app/lib/screens/repo_detail_screen.dart
index 15cb3f39..0ebb3644 100644
--- a/flutter_app/lib/screens/repo_detail_screen.dart
+++ b/flutter_app/lib/screens/repo_detail_screen.dart
@@ -276,6 +276,21 @@ Future<void> showPullActions(BuildContext context, Repository repo, PullRequest
           title: const Text('Request changes'),
           onTap: () => _review(context, ctx, inbox, repo, pr, 'request-changes'),
         ),
+        if (pr.status != 'merged')
+          ListTile(
+            leading: Icon(pr.status == 'open' ? Icons.do_not_disturb_on_outlined : Icons.refresh,
+                color: pr.status == 'open' ? FmColors.danger : FmColors.success),
+            title: Text(pr.status == 'open' ? 'Close PR' : 'Reopen PR'),
+            onTap: () {
+              Navigator.pop(ctx);
+              _run(
+                context,
+                () => inbox.setPullStatus(repo.owner, repo.name, pr.number,
+                    pr.status == 'open' ? 'closed' : 'open'),
+                _pendingNote,
+              );
+            },
+          ),
       ]),
     ),
   );
diff --git a/flutter_app/lib/services/inbox_service.dart b/flutter_app/lib/services/inbox_service.dart
index e127a137..7ae786df 100644
--- a/flutter_app/lib/services/inbox_service.dart
+++ b/flutter_app/lib/services/inbox_service.dart
@@ -171,6 +171,8 @@ class InboxService {
       case 'line-comment':
         final line = '${(ev['line'] as num?)?.toInt() ?? 0}';
         return [ev['path'] ?? '', ev['side'] ?? '', line, ev['body'] ?? ''].join('\x00');
+      case 'status':
+        return (ev['status'] ?? '').toString();
       default:
         return '';
     }
@@ -202,6 +204,12 @@ class InboxService {
     await _post(_repoEndpoint(owner, name, 'pulls'), {'number': number, 'event': event});
   }
 
+  /// [status] is "open" | "closed".
+  Future<void> setPullStatus(String owner, String name, int number, String status) async {
+    final event = await _signedPullComment(number, 'status', {'status': status});
+    await _post(_repoEndpoint(owner, name, 'pulls'), {'number': number, 'event': event});
+  }
+
   // ---- transport ----------------------------------------------------------
 
   Future<void> _post(Uri uri, Map<String, dynamic> body) async {
