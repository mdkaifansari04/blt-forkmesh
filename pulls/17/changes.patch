diff --git a/qt_client/src/AgentStore.h b/qt_client/src/AgentStore.h
index 07c0ae26..a5180004 100644
--- a/qt_client/src/AgentStore.h
+++ b/qt_client/src/AgentStore.h
@@ -32,8 +32,9 @@ struct AgentSession {
     QString model;
     // Permission mode this session runs under, as the human-readable label from
     // the composer's mode selector ("Ask before edits" / "Edit automatically" /
-    // "Plan mode" / "Auto mode"). Captured when a follow-up prompt is sent so the
-    // next resume honors the live selection, and shown in the agent header. Empty
+    // "Plan mode" / "Auto mode"). Captured when a follow-up prompt is sent or a
+    // session is continued so the next run honors the live selection, and shown in
+    // the agent header. Empty
     // falls back to the global kClaudeAutoModeSetting. Only "Auto mode" skips the
     // CLI's permission prompts today (see agentModeSkipsPermissions).
     QString mode;
diff --git a/qt_client/src/MainWindow.h b/qt_client/src/MainWindow.h
index ba577e0c..af4902a0 100644
--- a/qt_client/src/MainWindow.h
+++ b/qt_client/src/MainWindow.h
@@ -1530,6 +1530,9 @@ private:
     // agent detail composer's Send button and the footer quick-add's up-arrow
     // ("send to the visible agent") button.
     void sendPromptToSelectedAgent(const QString &prompt);
+    // Copy the footer's live agent/model/mode choices onto the selected session
+    // before continuing it or sending its next turn.
+    void syncSelectedAgentSessionOptions();
     // Same as sendPromptToSelectedAgent, but for an arbitrary session id
     // (adhoc #182: the website can steer any of this node's agent sessions).
     void sendPromptToAgentSession(int sessionId, const QString &prompt);
diff --git a/qt_client/src/MainWindowAgents.cpp b/qt_client/src/MainWindowAgents.cpp
index 995df24b..3f749d14 100644
--- a/qt_client/src/MainWindowAgents.cpp
+++ b/qt_client/src/MainWindowAgents.cpp
@@ -1183,22 +1183,25 @@ QWidget *MainWindow::buildAgentsTab()
     return page;
 }
 
-// Steer the currently-selected agent session (m_selectedAgentSessionId) with a
-// follow-up message. Shared by the agent detail page's "Send" composer and the
-// footer quick-add's up-arrow ("send to the visible agent") button.
-void MainWindow::sendPromptToSelectedAgent(const QString &prompt)
+// Preserve the footer's live selections before starting the selected session's
+// next turn. This is deliberately limited to the selected session: callers that
+// resume an arbitrary session (for example, a website-queued prompt) must not
+// inherit whichever values happen to be visible in this window.
+void MainWindow::syncSelectedAgentSessionOptions()
 {
-    // The composer's model dropdown is the user's live choice for what runs
-    // next; without this the session kept coasting on whatever model it
-    // happened to launch with, so switching the dropdown before following up
-    // on an idle/stopped agent silently did nothing. Only a restart (the
-    // no-live-process branch in sendPromptToAgentSession below) actually picks
-    // the new model up — a still-running process can't be retargeted mid-turn
-    // — but stashing it on the session now means the very next resume honors it.
-    if (AgentSession *session = findAgentSession(m_selectedAgentSessionId);
-        session && (session->provider == QLatin1String("claude-code") ||
-                    agentIsCodexProvider(session->provider))) {
-        bool changed = false;
+    AgentSession *session = findAgentSession(m_selectedAgentSessionId);
+    if (!session || !m_quickAddAgentProvider)
+        return;
+
+    const QString provider = m_quickAddAgentProvider->currentData().toString();
+    // "Manual" creates an issue rather than running an agent, so it cannot be
+    // used to continue a session.
+    if (provider.isEmpty() || provider == QLatin1String("manual"))
+        return;
+
+    bool changed = session->provider != provider;
+    session->provider = provider;
+    if (provider == QLatin1String("claude-code") || agentIsCodexProvider(provider)) {
         if (m_quickAddClaudeModel) {
             const QString chosen = selectedModelComboValue(m_quickAddClaudeModel);
             if (session->model != chosen) {
@@ -1206,11 +1209,6 @@ void MainWindow::sendPromptToSelectedAgent(const QString &prompt)
                 changed = true;
             }
         }
-        // The composer's mode dropdown is the user's live permission-mode choice
-        // for what runs next; capture it the same way as the model so the next
-        // resume honors it (a still-running turn can't be retargeted, but the
-        // stored label is picked up on the following resume) and the detail
-        // header shows which mode this session runs.
         if (m_quickAddModeSelector) {
             const QString chosenMode = m_quickAddModeSelector->currentText();
             if (session->mode != chosenMode) {
@@ -1218,9 +1216,25 @@ void MainWindow::sendPromptToSelectedAgent(const QString &prompt)
                 changed = true;
             }
         }
-        if (changed && m_agentStore)
-            m_agentStore->saveSession(*session);
+    } else {
+        // API agents do not use the structured CLI model/mode controls. Do not
+        // carry a stale Codex/Claude selection into their runner configuration.
+        if (!session->model.isEmpty() || !session->mode.isEmpty()) {
+            session->model.clear();
+            session->mode.clear();
+            changed = true;
+        }
     }
+    if (changed && m_agentStore)
+        m_agentStore->saveSession(*session);
+}
+
+// Steer the currently-selected agent session (m_selectedAgentSessionId) with a
+// follow-up message. Shared by the agent detail page's "Send" composer and the
+// footer quick-add's up-arrow ("send to the visible agent") button.
+void MainWindow::sendPromptToSelectedAgent(const QString &prompt)
+{
+    syncSelectedAgentSessionOptions();
     sendPromptToAgentSession(m_selectedAgentSessionId, prompt);
 }
 
@@ -4419,6 +4433,7 @@ QString MainWindow::saveNewAgentPromptImage(const QImage &image)
 
 void MainWindow::continueSelectedAgentSession()
 {
+    syncSelectedAgentSessionOptions();
     continueAgentSession(m_selectedAgentSessionId);
 }
 
