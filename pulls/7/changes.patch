diff --git a/qt_client/src/MainWindow.h b/qt_client/src/MainWindow.h
index cf0ed1fc..08523820 100644
--- a/qt_client/src/MainWindow.h
+++ b/qt_client/src/MainWindow.h
@@ -301,6 +301,10 @@ public:
     // combo as a user would, so tests can assert the default seeds/updates them.
     QString testQuickAddAgentProvider() const;
     QString testIssueAgentProvider() const;
+    QString testIssuePrioritizeAgentProvider() const;
+    bool testHasIssuePrioritizeAgentPicker() const;
+    QString testPrioritizePrompt() const;
+    void testSetPrioritizePrompt(const QString &prompt);
     void testSetDefaultAgentProvider(const QString &provider);
     // issue #272: open the Worktrees tab on a branch, rebuild the panel (as an
     // "Update from main" merge does), and read back which worktree stays selected
@@ -3373,9 +3377,9 @@ private:
     QPushButton *m_issueListNewButton = nullptr;
     QPushButton *m_issueSyncButton = nullptr;
     QPushButton *m_issuePrioritizeButton = nullptr;
-    // Agent picker sitting next to "Prioritize from README" so the run can use
-    // any provider, not just the saved default. Seeded from the default agent.
-    QComboBox *m_issuePrioritizeAgentCombo = nullptr;
+    // Agent picker for issue completeness analysis. README prioritization uses
+    // the saved default agent directly.
+    QComboBox *m_issueCompletenessAgentCombo = nullptr;
     bool m_prioritizeInFlight = false;
     // Adhoc #139: "Analyze completeness" button next to "Prioritize from README".
     // Shares the agent picker above; guarded by its own in-flight flag.
diff --git a/qt_client/src/MainWindowIssues.cpp b/qt_client/src/MainWindowIssues.cpp
index 3eb21289..a9903766 100644
--- a/qt_client/src/MainWindowIssues.cpp
+++ b/qt_client/src/MainWindowIssues.cpp
@@ -162,24 +162,24 @@ QWidget *MainWindow::buildIssuesSection()
     connect(m_issuePrioritizeButton, &QPushButton::clicked, this,
             &MainWindow::prioritizeIssuesFromReadme);
 
-    // Agent picker next to the button so a run can target any provider, not just
-    // the saved default. Seeded from the default agent (Settings -> Agents).
-    m_issuePrioritizeAgentCombo = new QComboBox;
-    m_issuePrioritizeAgentCombo->setObjectName("issueControlSm");
-    m_issuePrioritizeAgentCombo->addItem(QStringLiteral("OpenAI API"),
-                                         QStringLiteral("openai"));
-    m_issuePrioritizeAgentCombo->addItem(QStringLiteral("Claude API"),
-                                         QStringLiteral("claude-api"));
-    m_issuePrioritizeAgentCombo->addItem(QStringLiteral("Claude Code"),
-                                         QStringLiteral("claude-code"));
-    selectDefaultAgentProvider(m_issuePrioritizeAgentCombo);
-    m_issuePrioritizeAgentCombo->setToolTip(
-        "Agent that ranks/reviews the issues. Defaults to your default agent "
+    // Agent picker for completeness analysis. The README prioritizer above uses
+    // the saved default agent directly (Settings -> Agents).
+    m_issueCompletenessAgentCombo = new QComboBox;
+    m_issueCompletenessAgentCombo->setObjectName("issueControlSm");
+    m_issueCompletenessAgentCombo->addItem(QStringLiteral("OpenAI API"),
+                                           QStringLiteral("openai"));
+    m_issueCompletenessAgentCombo->addItem(QStringLiteral("Claude API"),
+                                           QStringLiteral("claude-api"));
+    m_issueCompletenessAgentCombo->addItem(QStringLiteral("Claude Code"),
+                                           QStringLiteral("claude-code"));
+    selectDefaultAgentProvider(m_issueCompletenessAgentCombo);
+    m_issueCompletenessAgentCombo->setToolTip(
+        "Agent that reviews issue completeness. Defaults to your default agent "
         "(Settings \xE2\x86\x92 Agents).");
 
-    // Adhoc #139: sibling of "Prioritize from README" that, instead of ranking,
-    // asks the same picked agent to judge how complete/actionable each open issue
-    // is and shows the verdict in a report dialog. Reuses the agent picker above.
+    // Adhoc #139: sibling of "Prioritize from README" that asks the picked agent
+    // to judge how complete/actionable each open issue is and shows the verdict
+    // in a report dialog.
     m_issueCompletenessButton = new QPushButton("Analyze completeness");
     m_issueCompletenessButton->setObjectName("ghostButton");
     m_issueCompletenessButton->setProperty("buttonSize", "sm");
@@ -193,9 +193,9 @@ QWidget *MainWindow::buildIssuesSection()
             &MainWindow::analyzeIssueCompleteness);
 
     // Place them right after the "Issues" heading, ahead of the view-tab toggles:
-    // prioritize, then completeness, then the shared agent picker.
+    // prioritize, then completeness, then the completeness agent picker.
     headingRow->insertWidget(1, m_issuePrioritizeButton);
-    headingRow->insertWidget(2, m_issuePrioritizeAgentCombo);
+    headingRow->insertWidget(2, m_issueCompletenessAgentCombo);
     headingRow->insertWidget(2, m_issueCompletenessButton);
 
     // Bulk bounty: pledge the same amount on every open issue at once. Bounties
@@ -4933,12 +4933,8 @@ void MainWindow::prioritizeIssuesFromReadme()
     if (readme.size() > 8000)
         readme = readme.left(8000) + QStringLiteral("\n\n[README truncated]");
 
-    // Use the agent picked in the dropdown next to the button; fall back to the
-    // saved default agent when the picker isn't built yet.
-    const QString provider = m_issuePrioritizeAgentCombo
-                                 ? m_issuePrioritizeAgentCombo->currentData()
-                                       .toString()
-                                 : defaultAgentProvider();
+    // Issue #286: README prioritization always uses the saved default agent.
+    const QString provider = defaultAgentProvider();
     const bool claude = agentIsClaudeProvider(provider);
     const bool claudeCode = provider == QLatin1String("claude-code");
     // "Claude Code" (issue #294) authenticates with the claude.ai subscription
@@ -5177,10 +5173,10 @@ void MainWindow::analyzeIssueCompleteness()
     if (fileTree.size() > 12000)
         fileTree = fileTree.left(12000) + QStringLiteral("\n[file list truncated]");
 
-    // Use the agent picked in the dropdown shared with "Prioritize from README";
+    // Use the agent picked in the completeness dropdown;
     // fall back to the saved default agent when the picker isn't built yet.
-    const QString provider = m_issuePrioritizeAgentCombo
-                                 ? m_issuePrioritizeAgentCombo->currentData()
+    const QString provider = m_issueCompletenessAgentCombo
+                                 ? m_issueCompletenessAgentCombo->currentData()
                                        .toString()
                                  : defaultAgentProvider();
     const bool claude = agentIsClaudeProvider(provider);
@@ -6643,4 +6639,3 @@ QString MainWindow::selfNodeStats() const
            formatDuration(totalMs) +
            (key.isEmpty() ? QString() : " \xC2\xB7 key " + key);
 }
-
diff --git a/qt_client/src/MainWindowSettings.cpp b/qt_client/src/MainWindowSettings.cpp
index 0ced55f2..1d93a941 100644
--- a/qt_client/src/MainWindowSettings.cpp
+++ b/qt_client/src/MainWindowSettings.cpp
@@ -498,7 +498,7 @@ QWidget *MainWindow::buildSettingsSection()
                 // Keep the live pickers in step with the new default.
                 selectDefaultAgentProvider(m_quickAddAgentProvider);
                 selectDefaultAgentProvider(m_issueAgentProvider);
-                selectDefaultAgentProvider(m_issuePrioritizeAgentCombo);
+                selectDefaultAgentProvider(m_issueCompletenessAgentCombo);
             });
 
     // When the watchdog catches the GUI thread freezing, hand the captured
diff --git a/qt_client/src/MainWindowSetup.cpp b/qt_client/src/MainWindowSetup.cpp
index e6c09290..5af850a9 100644
--- a/qt_client/src/MainWindowSetup.cpp
+++ b/qt_client/src/MainWindowSetup.cpp
@@ -533,6 +533,28 @@ QString MainWindow::testIssueAgentProvider() const
                                 : QString();
 }
 
+QString MainWindow::testIssuePrioritizeAgentProvider() const
+{
+    return defaultAgentProvider();
+}
+
+bool MainWindow::testHasIssuePrioritizeAgentPicker() const
+{
+    return false;
+}
+
+QString MainWindow::testPrioritizePrompt() const
+{
+    return prioritizePromptSetting();
+}
+
+void MainWindow::testSetPrioritizePrompt(const QString &prompt)
+{
+    QSettings().setValue(kPrioritizePromptSetting, prompt);
+    if (m_prioritizePromptEdit)
+        m_prioritizePromptEdit->setPlainText(prompt);
+}
+
 QString MainWindow::testWorktreeAheadBehindText(const QString &branch) const
 {
     if (!m_worktreesTable)
@@ -2181,4 +2203,3 @@ void MainWindow::updateRebuildRestart()
         recloneFromMirror();
     }
 }
-
diff --git a/qt_client/tests/test_window_resize.cpp b/qt_client/tests/test_window_resize.cpp
index e78b186e..edb102f7 100644
--- a/qt_client/tests/test_window_resize.cpp
+++ b/qt_client/tests/test_window_resize.cpp
@@ -810,14 +810,33 @@ int main(int argc, char *argv[])
               QString("default agent seeds the pickers (quick-add %1, issue %2)")
                   .arg(seeded.testQuickAddAgentProvider(),
                        seeded.testIssueAgentProvider()));
+        check(seeded.testIssuePrioritizeAgentProvider() ==
+                  QStringLiteral("claude-code") &&
+                  !seeded.testHasIssuePrioritizeAgentPicker(),
+              QStringLiteral("README prioritization uses the default agent "
+                             "without a separate issue toolbar picker (#286)"));
 
         seeded.testSetDefaultAgentProvider(QStringLiteral("claude-api"));
         check(seeded.testQuickAddAgentProvider() == QStringLiteral("claude-api") &&
-                  seeded.testIssueAgentProvider() == QStringLiteral("claude-api"),
-              QString("changing the default updates the live pickers "
-                      "(quick-add %1, issue %2)")
+                  seeded.testIssueAgentProvider() == QStringLiteral("claude-api") &&
+                  seeded.testIssuePrioritizeAgentProvider() ==
+                      QStringLiteral("claude-api"),
+              QString("changing the default updates the live pickers and README "
+                      "prioritizer (quick-add %1, issue %2, prioritize %3)")
                   .arg(seeded.testQuickAddAgentProvider(),
-                       seeded.testIssueAgentProvider()));
+                       seeded.testIssueAgentProvider(),
+                       seeded.testIssuePrioritizeAgentProvider()));
+        const QString customPrompt =
+            QStringLiteral("Rank issues by README launch blockers only.");
+        seeded.testSetPrioritizePrompt(customPrompt);
+        check(seeded.testPrioritizePrompt() == customPrompt,
+              QStringLiteral("Settings stores the editable README prioritize "
+                             "prompt (#286)"));
+        seeded.testSetPrioritizePrompt(QStringLiteral("   "));
+        check(seeded.testPrioritizePrompt().contains(QStringLiteral("README")) &&
+                  seeded.testPrioritizePrompt().contains(QStringLiteral("JSON")),
+              QStringLiteral("clearing the README prioritize prompt falls back "
+                             "to the built-in instruction (#286)"));
         stopChildProcesses(seeded);
     }
 
