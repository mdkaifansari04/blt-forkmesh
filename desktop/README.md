# ForkMesh Qt client

Build and launch from the repository root so ForkMesh records the whole checkout
as its workspace:

```sh
./desktop/run.sh
```

Run the sub-minute critical client contracts:

```sh
./desktop/run.sh test
```

Changing the project? Read [Contributing](../CONTRIBUTING.md) before opening a
pull request.
